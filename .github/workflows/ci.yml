name: CI

on:
  pull_request:
    branches: [main, 'integration/**']
  # merge_group retired 2026-06-18: Graphite owns the merge queue; GitHub never
  # creates a merge_group, so this trigger would never fire. See .github/MERGE_QUEUE.md.
  push:
    branches: [main]
    paths-ignore:
      - '**/*.md'
      - 'docs/**'
      - 'apps/web/public/product-screenshots/**'
  workflow_dispatch:

permissions:
  contents: read
  actions: read

concurrency:
  # On main, scope the concurrency group per-commit so rapid merges cannot
  # cancel each other's deploy pipelines. GitHub evaluates cancel-in-progress
  # expressions as strings and treats "false" as truthy, which was cancelling
  # every main CI run when the next merge landed and starving production
  # promotion. PRs use a simple pr-N group so stale runs are auto-cancelled
  # as soon as new commits push (JOV-4201).
  group: >-
    ${{ github.workflow }}-${{
      (github.event_name == 'pull_request')
        && format('pr-{0}', github.event.pull_request.number)
        || ((github.event_name == 'push' && github.ref == 'refs/heads/main')
           && github.sha
           || github.ref)
    }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

env:
  TURBO_TOKEN: ${{ secrets.TURBO_TOKEN }}
  TURBO_TEAM: ${{ secrets.TURBO_TEAM }}
  TURBO_CACHE: local:rw,remote:rw
  # Larger runner for CPU-heavy jobs (typecheck, build, tests).
  # Set to a GitHub-hosted larger runner label (e.g. "ubuntu-4core") for burst throughput,
  # or leave as "ubuntu-latest" for default 2-core runners.
  FAST_RUNNER: ${{ vars.CI_FAST_RUNNER || 'ubuntu-latest' }}
  # JOV-2497: four-slot cross-PR pool caps Neon branch *creation* (one endpoint per branch).
  # Only jobs that call neon-create-branch-with-retry use the pool — artifact consumers
  # (Lighthouse/A11y/Mobile Overflow) reuse neon-db and must NOT share the pool or
  # sibling matrix jobs cancel each other within the same workflow run.
  # Pool groups include github.job so branch-creator siblings in one workflow (neon-db,
  # dashboard Lighthouse, E2E smoke fallback, admin smoke) do not queue-cancel each other.

jobs:
  # ── CI Optimizer + Centralized path-change detection (merged) ──────────
  # Merged optimize_ci + ci-path-changes into one job to eliminate the serial
  # dependency chain that added ~5 min to every CI run. Graphite CI Optimizer
  # runs first (fail-open), then path detection.
  ci-path-changes:
    name: Path Changes
    # Hosted-only root of the PR DAG. Self-hosted cancel/hang here cascades into
    # SKIPPED risk/ci-fast/unit leaves and a red PR Ready (JOV-3464 drain).
    runs-on: ubuntu-latest
    timeout-minutes: 5
    outputs:
      # Prefer real path detect; fall back to graphite-skip defaults so job
      # outputs are never empty when Path Changes succeeds (empty has_code_changes
      # was treated as docs-only and skip-cascaded ci-fast / unit leaves).
      skip: ${{ steps.graphite.outputs.skip || 'false' }}
      run_build: ${{ steps.detect.outputs.run_build || steps.graphite_skip.outputs.run_build || 'false' }}
      run_test: ${{ steps.detect.outputs.run_test || steps.graphite_skip.outputs.run_test || 'false' }}
      run_public_lighthouse: ${{ steps.detect.outputs.run_public_lighthouse || steps.graphite_skip.outputs.run_public_lighthouse || 'false' }}
      run_dashboard_lighthouse: ${{ steps.detect.outputs.run_dashboard_lighthouse || steps.graphite_skip.outputs.run_dashboard_lighthouse || 'false' }}
      run_onboarding_lighthouse: ${{ steps.detect.outputs.run_onboarding_lighthouse || steps.graphite_skip.outputs.run_onboarding_lighthouse || 'false' }}
      run_admin_lighthouse: ${{ steps.detect.outputs.run_admin_lighthouse || steps.graphite_skip.outputs.run_admin_lighthouse || 'false' }}
      run_chat_lighthouse: ${{ steps.detect.outputs.run_chat_lighthouse || steps.graphite_skip.outputs.run_chat_lighthouse || 'false' }}
      run_promptfoo_evals: ${{ steps.detect.outputs.run_promptfoo_evals || steps.graphite_skip.outputs.run_promptfoo_evals || 'false' }}
      run_golden_eval_set: ${{ steps.detect.outputs.run_golden_eval_set || steps.graphite_skip.outputs.run_golden_eval_set || 'false' }}
      run_e2e: ${{ steps.detect.outputs.run_e2e || steps.graphite_skip.outputs.run_e2e || 'false' }}
      run_golden_path: ${{ steps.detect.outputs.run_golden_path || steps.graphite_skip.outputs.run_golden_path || 'false' }}
      run_drizzle: ${{ steps.detect.outputs.run_drizzle || steps.graphite_skip.outputs.run_drizzle || 'false' }}
      run_neon: ${{ steps.detect.outputs.run_neon || steps.graphite_skip.outputs.run_neon || 'false' }}
      has_code_changes: ${{ steps.detect.outputs.has_code_changes || steps.graphite_skip.outputs.has_code_changes || 'false' }}
    steps:
      - id: graphite-token
        name: Check Graphite token configuration
        env:
          GRAPHITE_TOKEN: ${{ secrets.GRAPHITE_TOKEN }}
        run: |
          if [ -z "${GRAPHITE_TOKEN:-}" ]; then
            echo "configured=false" >> "$GITHUB_OUTPUT"
            echo "::warning::GRAPHITE_TOKEN is not configured; Graphite CI Optimizations will not scan PRs."
          else
            echo "configured=true" >> "$GITHUB_OUTPUT"
          fi

      - id: graphite
        if: steps.graphite-token.outputs.configured == 'true'
        continue-on-error: true
        uses: withgraphite/graphite-ci-action@402a89bc8aa6db18ae1f9c61953d001d5f9d828f # v0.0.11
        with:
          graphite_token: ${{ secrets.GRAPHITE_TOKEN }}

      - name: Skip CI (Graphite optimizer)
        id: graphite_skip
        if: steps.graphite.outputs.skip == 'true'
        run: |
          echo "Graphite CI optimizer requested skip — CI will be skipped."
          for t in run_build run_test run_public_lighthouse run_dashboard_lighthouse run_onboarding_lighthouse run_admin_lighthouse run_chat_lighthouse run_promptfoo_evals run_golden_eval_set run_e2e run_golden_path run_drizzle run_neon has_code_changes; do
            echo "$t=false" >> "$GITHUB_OUTPUT"
          done

      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        if: steps.graphite.outputs.skip != 'true'
        with:
          # Blobless partial clone: full commit graph for merge-base/diff
          # (names only), no historical file contents. Cuts the clone from
          # ~30-40s (worse under runner throttling) to seconds on this repo;
          # any blob that is actually needed is lazy-fetched on demand.
          fetch-depth: 0
          filter: blob:none
      - name: Detect path changes for all job types
        if: steps.graphite.outputs.skip != 'true'
        id: detect
        shell: bash
        run: |
          set -euo pipefail
          IS_FORK="${{ github.event_name == 'pull_request' && github.event.pull_request.head.repo.fork }}"
          # Determine changed files
          if [[ "${{ github.event_name }}" == "pull_request" ]]; then
            # Fail closed on base resolution: a failed fetch used to leave an
            # empty CHANGED_FILES list that looked like docs-only and skip-
            # cascaded ci-fast / unit leaves while risk still ran.
            git fetch --no-tags origin "${{ github.base_ref }}"
            if ! git rev-parse --verify "origin/${{ github.base_ref }}" >/dev/null 2>&1; then
              echo "::error::Could not resolve origin/${{ github.base_ref }} after fetch"
              exit 1
            fi
            CHANGED_FILES=$(git diff --name-only "origin/${{ github.base_ref }}" HEAD || true)
            if [[ -z "${CHANGED_FILES//[$'\t\r\n' ]/}" ]]; then
              echo "Empty changed-file list vs origin/${{ github.base_ref }} — no path triggers"
              for t in run_build run_test run_public_lighthouse run_dashboard_lighthouse run_onboarding_lighthouse run_admin_lighthouse run_chat_lighthouse run_promptfoo_evals run_golden_eval_set run_e2e run_golden_path run_drizzle run_neon has_code_changes; do
                echo "$t=false" >> "$GITHUB_OUTPUT"
              done
              exit 0
            fi
          elif [[ "${{ github.event_name }}" == "workflow_dispatch" ]]; then
            # Manual dispatch: run everything
            echo "Manual dispatch: running all checks"
            for t in run_build run_test run_public_lighthouse run_dashboard_lighthouse run_onboarding_lighthouse run_admin_lighthouse run_chat_lighthouse run_promptfoo_evals run_golden_eval_set run_e2e run_golden_path run_drizzle run_neon has_code_changes; do
              echo "$t=true" >> "$GITHUB_OUTPUT"
            done
            exit 0
          else
            CHANGED_FILES=$(git diff --name-only HEAD~1 HEAD 2>/dev/null || echo "")
          fi

          FULL_CI_LABEL="${{ github.event_name == 'pull_request' && contains(github.event.pull_request.labels.*.name, 'testing') }}"

          PROMPTFOO_INFRA_TRIGGER_PATTERN='^(\.github/workflows/evals-periodic\.yml|\.github/workflows/README\.md|apps/web/tests/eval/promptfoo/|apps/web/tests/eval/golden/)'
          PROMPTFOO_INFRA_ALLOWED_PATTERN='^(\.github/workflows/ci\.yml|\.github/workflows/evals-periodic\.yml|\.github/workflows/README\.md|apps/web/tests/eval/promptfoo/.*|apps/web/tests/eval/golden/.*|apps/web/vitest\.config\.golden-eval\.mts|apps/web/scripts/run-golden-eval-set-ci\.sh|apps/web/package.*\.json|package.*\.json|pnpm-lock\.yaml)$'
          if echo "$CHANGED_FILES" | grep -q -E "$PROMPTFOO_INFRA_TRIGGER_PATTERN" &&
            ! echo "$CHANGED_FILES" | grep -v -E "$PROMPTFOO_INFRA_ALLOWED_PATTERN" | grep -q .; then
            echo "Promptfoo eval-infrastructure-only change: running deterministic eval CI and core static checks, even if the PR has the testing label."
            echo "has_code_changes=true" >> "$GITHUB_OUTPUT"
            echo "run_promptfoo_evals=true" >> "$GITHUB_OUTPUT"
            echo "run_golden_eval_set=true" >> "$GITHUB_OUTPUT"
            for t in run_build run_test run_public_lighthouse run_dashboard_lighthouse run_onboarding_lighthouse run_admin_lighthouse run_chat_lighthouse run_e2e run_golden_path run_drizzle run_neon; do
              echo "$t=false" >> "$GITHUB_OUTPUT"
            done
            exit 0
          fi

          # Force run all if the 'testing' label is present. Promptfoo eval-infra-only
          # changes are handled above so the deterministic no-cost lane cannot fan out
          # into Neon, Lighthouse, or Playwright work by label accident.
          if [[ "$FULL_CI_LABEL" == "true" ]]; then
            echo "Forcing full CI run due to 'testing' label"
            for t in run_build run_test run_public_lighthouse run_dashboard_lighthouse run_onboarding_lighthouse run_admin_lighthouse run_chat_lighthouse run_promptfoo_evals run_golden_eval_set run_e2e run_golden_path run_drizzle run_neon has_code_changes; do
              echo "$t=true" >> "$GITHUB_OUTPUT"
            done
            exit 0
          fi

          # Check if only docs changed (exclude .github/ — workflow changes must run CI)
          if ! echo "$CHANGED_FILES" | grep -q -E '^\.github/' &&
            ! echo "$CHANGED_FILES" | grep -q -v -E '\.(md|txt)$'; then
            echo "Only documentation files changed"
            for t in run_build run_test run_public_lighthouse run_dashboard_lighthouse run_onboarding_lighthouse run_admin_lighthouse run_chat_lighthouse run_promptfoo_evals run_golden_eval_set run_e2e run_golden_path run_drizzle run_neon has_code_changes; do
              echo "$t=false" >> "$GITHUB_OUTPUT"
            done
            exit 0
          fi

          echo "has_code_changes=true" >> "$GITHUB_OUTPUT"

          # Build paths
          BUILD_PATTERN='^(apps/web/app/|apps/web/components/|apps/web/lib/|apps/web/hooks/|apps/web/types/|apps/web/constants/|apps/web/package.*\.json|apps/web/next\.config\.js|apps/web/tailwind\.config\.|apps/web/postcss\.config\.|apps/web/instrumentation.*\.ts|apps/web/sentry\..*\.config\.ts|apps/web/middleware\.ts|package.*\.json|turbo\.json)'
          if echo "$CHANGED_FILES" | grep -q -E "$BUILD_PATTERN"; then
            RUN_BUILD=true
            echo "run_build=true" >> "$GITHUB_OUTPUT"
          else
            RUN_BUILD=false
            echo "run_build=false" >> "$GITHUB_OUTPUT"
          fi

          # Test paths
          TEST_PATTERN='^(apps/web/app/|apps/web/components/|apps/web/lib/|apps/web/hooks/|apps/web/types/|apps/web/tests/unit/|apps/web/__tests__/|packages/|.*\.test\.|.*\.spec\.|apps/web/package.*\.json|package.*\.json)'
          if echo "$CHANGED_FILES" | grep -q -E "$TEST_PATTERN"; then
            RUN_TEST=true
            echo "run_test=true" >> "$GITHUB_OUTPUT"
          else
            RUN_TEST=false
            echo "run_test=false" >> "$GITHUB_OUTPUT"
          fi

          # Screenshot-only product exports live under public/product-screenshots and
          # must not force public Lighthouse + Neon on main (they are marketing assets,
          # not route code). Filter that tree out before matching public/* paths.
          PUBLIC_LIGHTHOUSE_PATTERN='^(\.github/workflows/ci\.yml|apps/web/\.lighthouserc\.pr\.json|apps/web/\.lighthouserc\.public-launch\.json|apps/web/app/\(home\)/|apps/web/app/\(marketing\)/|apps/web/app/\(dynamic\)/legal/|apps/web/content/legal/|apps/web/app/\[username\]/|apps/web/app/layout\.tsx|apps/web/app/globals\.css|apps/web/public/|apps/web/styles/|apps/web/components/features/home/|apps/web/components/features/marketing/|apps/web/components/features/profile/|apps/web/components/homepage/|apps/web/components/marketing/|apps/web/scripts/run-public-lighthouse\.ts|apps/web/scripts/public-route-qa\.ts|apps/web/scripts/public-surface-qa\.ts|apps/web/scripts/performance-route-manifest\.ts|apps/web/tests/e2e/public-exhaustive\.spec\.ts|apps/web/tests/e2e/profile-mobile-viewport-stability\.spec\.ts|apps/web/tests/e2e/axe-audit\.spec\.ts|apps/web/tests/e2e/utils/(public-surface-(manifest|helpers)|smoke-test-utils|mobile-profile-viewports)\.ts|packages/|apps/web/package.*\.json|package.*\.json|apps/web/tailwind\.config\.|apps/web/postcss\.config\.)'
          PUBLIC_LIGHTHOUSE_FILES=$(echo "$CHANGED_FILES" | grep -v -E '^apps/web/public/product-screenshots/' || true)
          if echo "$PUBLIC_LIGHTHOUSE_FILES" | grep -q -E "$PUBLIC_LIGHTHOUSE_PATTERN"; then
            echo "run_public_lighthouse=true" >> "$GITHUB_OUTPUT"
          else
            echo "run_public_lighthouse=false" >> "$GITHUB_OUTPUT"
          fi

          DASHBOARD_LIGHTHOUSE_PATTERN='^(\.github/workflows/ci\.yml|apps/web/\.lighthouserc\.dashboard\.pr\.json|apps/web/scripts/lighthouse-dashboard-auth\.cjs)$'
          if [[ "$RUN_BUILD" == "true" || "$RUN_TEST" == "true" ]] || echo "$CHANGED_FILES" | grep -q -E "$DASHBOARD_LIGHTHOUSE_PATTERN"; then
            echo "run_dashboard_lighthouse=true" >> "$GITHUB_OUTPUT"
          else
            echo "run_dashboard_lighthouse=false" >> "$GITHUB_OUTPUT"
          fi

          ONBOARDING_LIGHTHOUSE_PATTERN='^(\.github/workflows/ci\.yml|apps/web/\.lighthouserc\.onboarding\.pr\.json|apps/web/scripts/lighthouse-dashboard-auth\.cjs|apps/web/app/onboarding/|apps/web/components/features/dashboard/organisms/onboarding|apps/web/components/features/dashboard/organisms/apple-style-onboarding|apps/web/components/features/onboarding/|apps/web/lib/onboarding/)'
          if [[ "$RUN_BUILD" == "true" || "$RUN_TEST" == "true" ]] || echo "$CHANGED_FILES" | grep -q -E "$ONBOARDING_LIGHTHOUSE_PATTERN"; then
            echo "run_onboarding_lighthouse=true" >> $GITHUB_OUTPUT
          else
            echo "run_onboarding_lighthouse=false" >> $GITHUB_OUTPUT
          fi

          ADMIN_LIGHTHOUSE_PATTERN='^(\.github/workflows/ci\.yml|apps/web/\.lighthouserc\.admin\.pr\.json|apps/web/scripts/lighthouse-dashboard-auth\.cjs|apps/web/app/app/\(shell\)/admin/|apps/web/components/features/admin/)'
          if [[ "$RUN_BUILD" == "true" || "$RUN_TEST" == "true" ]] || echo "$CHANGED_FILES" | grep -q -E "$ADMIN_LIGHTHOUSE_PATTERN"; then
            echo "run_admin_lighthouse=true" >> "$GITHUB_OUTPUT"
          else
            echo "run_admin_lighthouse=false" >> "$GITHUB_OUTPUT"
          fi

          CHAT_LIGHTHOUSE_PATTERN='^(\.github/workflows/ci\.yml|apps/web/\.lighthouserc\.chat\.pr\.json|apps/web/scripts/lighthouse-dashboard-auth\.cjs|apps/web/app/app/\(shell\)/chat/|apps/web/components/jovie/|apps/web/components/chat/)'
          if [[ "$RUN_BUILD" == "true" || "$RUN_TEST" == "true" ]] || echo "$CHANGED_FILES" | grep -q -E "$CHAT_LIGHTHOUSE_PATTERN"; then
            echo "run_chat_lighthouse=true" >> "$GITHUB_OUTPUT"
          else
            echo "run_chat_lighthouse=false" >> "$GITHUB_OUTPUT"
          fi

          PROMPTFOO_EVAL_PATTERN='^(\.github/workflows/ci\.yml|apps/web/tests/eval/promptfoo/|apps/web/tests/eval/golden/|apps/web/vitest\.config\.golden-eval\.mts|apps/web/scripts/run-golden-eval-set-ci\.sh|apps/web/lib/chat/|apps/web/app/api/chat/|apps/web/package.*\.json|package.*\.json|pnpm-lock\.yaml)'
          if echo "$CHANGED_FILES" | grep -q -E "$PROMPTFOO_EVAL_PATTERN"; then
            echo "run_promptfoo_evals=true" >> "$GITHUB_OUTPUT"
            echo "run_golden_eval_set=true" >> "$GITHUB_OUTPUT"
          else
            echo "run_promptfoo_evals=false" >> "$GITHUB_OUTPUT"
            echo "run_golden_eval_set=false" >> "$GITHUB_OUTPUT"
          fi

          # E2E paths
          E2E_PATTERN='^(apps/web/app/|apps/web/components/|apps/web/tests/e2e/|apps/web/tests/smoke/|apps/web/playwright\.config\.|apps/web/package.*\.json|package.*\.json)'
          if echo "$CHANGED_FILES" | grep -q -E "$E2E_PATTERN"; then
            echo "run_e2e=true" >> "$GITHUB_OUTPUT"
          else
            echo "run_e2e=false" >> "$GITHUB_OUTPUT"
          fi

          # Launch-gate golden path (JOV-3758). Mirrors docs/launch/LAUNCH_GATES.md's
          # path list: onboarding routes/components/libs, auth signup/signin entrypoints,
          # dashboard shell/releases flows, billing/checkout API routes used by the golden
          # path, golden-path Playwright specs + helpers, and launch-gate CI/Lighthouse/perf
          # config. Docs-only changes never reach here (handled by the docs-only guard above).
          LAUNCH_GATE_PATTERN='^(apps/web/app/onboarding/|apps/web/components/features/onboarding/|apps/web/components/features/dashboard/organisms/onboarding|apps/web/components/features/dashboard/organisms/apple-style-onboarding|apps/web/lib/onboarding/|apps/web/app/\(auth\)/|apps/web/app/signin/|apps/web/app/signup/|apps/web/app/sign-in/|apps/web/app/sign-up/|apps/web/app/app/\(shell\)/dashboard/|apps/web/app/app/\(shell\)/layout\.tsx|apps/web/components/features/dashboard/|apps/web/lib/releases/|apps/web/app/api/stripe/|apps/web/app/api/billing/|apps/web/app/api/checkout/|apps/web/lib/stripe/|apps/web/tests/e2e/golden-path\.spec\.ts|apps/web/tests/helpers/clerk-auth\.ts|apps/web/tests/global-setup\.ts|apps/web/playwright\.config\.ts|\.github/workflows/ci\.yml|apps/web/\.lighthouserc\.dashboard\.pr\.json|apps/web/\.lighthouserc\.onboarding\.pr\.json|docs/launch/LAUNCH_GATES\.md|apps/web/package.*\.json|package.*\.json)'
          if echo "$CHANGED_FILES" | grep -q -E "$LAUNCH_GATE_PATTERN"; then
            echo "run_golden_path=true" >> "$GITHUB_OUTPUT"
          else
            echo "run_golden_path=false" >> "$GITHUB_OUTPUT"
          fi

          # Drizzle paths
          DRIZZLE_PATTERN='^(apps/web/drizzle/|apps/web/lib/db/|apps/web/drizzle\.config\.ts|drizzle\.kit\.(m?js|ts))'
          if echo "$CHANGED_FILES" | grep -q -E "$DRIZZLE_PATTERN"; then
            echo "run_drizzle=true" >> "$GITHUB_OUTPUT"
          else
            echo "run_drizzle=false" >> "$GITHUB_OUTPUT"
          fi

          # Neon ephemeral-branch gate: only spin up Neon when DB-relevant code changed.
          # Covers drizzle, lib/db, lib/queries, schema, cron routes, SQL files, and
          # lockfile changes that could affect drizzle/DB deps. Version-only package
          # metadata changes should not spin up ephemeral Neon branches.
          # Jobs that create ephemeral Neon branches must check this flag before doing so.
          NEON_PATTERN='^(apps/web/drizzle/|apps/web/lib/db/|apps/web/lib/queries/|apps/web/drizzle\.config\.ts|drizzle\.kit\.(m?js|ts)|.*\.sql$|apps/web/app/api/cron/|pnpm-lock\.yaml)'
          if echo "$CHANGED_FILES" | grep -q -E "$NEON_PATTERN"; then
            echo "run_neon=true" >> "$GITHUB_OUTPUT"
          else
            echo "run_neon=false" >> "$GITHUB_OUTPUT"
          fi

          # Fork guard: disable E2E for forks (no secrets)
          if [[ "$IS_FORK" == "true" ]]; then
            echo "Fork PR: disabling e2e and build"
            echo "run_e2e=false" >> "$GITHUB_OUTPUT"
            echo "run_golden_path=false" >> "$GITHUB_OUTPUT"
            echo "run_build=false" >> "$GITHUB_OUTPUT"
            echo "run_public_lighthouse=false" >> "$GITHUB_OUTPUT"
            echo "run_dashboard_lighthouse=false" >> "$GITHUB_OUTPUT"
            echo "run_admin_lighthouse=false" >> "$GITHUB_OUTPUT"
            echo "run_chat_lighthouse=false" >> "$GITHUB_OUTPUT"
            echo "run_test=false" >> "$GITHUB_OUTPUT"
          fi

  ci-risk-classifier:
    # Trusted PRs need classification before their build, smoke, and preview
    # dependencies evaluate. Forks intentionally stay on the secret-free path.
    if: ${{ needs.ci-path-changes.outputs.skip == 'false' && (github.event_name == 'workflow_dispatch' || (github.event_name == 'push' && github.ref == 'refs/heads/main') || (github.event_name == 'pull_request' && github.event.pull_request.head.repo.fork == false)) }}
    name: CI Risk Classifier
    needs: [ci-path-changes]
    runs-on: ${{ vars.CI_GATE_RUNNER || 'ubuntu-latest' }}
    timeout-minutes: 3
    outputs:
      risk_level: ${{ steps.classify.outputs.risk_level }}
      requires_smoke: ${{ steps.classify.outputs.requires_smoke }}
      requires_preview: ${{ steps.classify.outputs.requires_preview }}
      blocks_unattended_auto_merge: ${{ steps.classify.outputs.blocks_unattended_auto_merge }}
      recommended_labels: ${{ steps.classify.outputs.recommended_labels }}
      matched_rule_ids: ${{ steps.classify.outputs.matched_rule_ids }}
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 0
      - uses: ./.github/actions/setup-node-pnpm
      - name: Collect changed files
        id: changed-files
        shell: bash
        env:
          BASE_REF: ${{ github.base_ref || 'main' }}
          EVENT_NAME: ${{ github.event_name }}
        run: |
          set -euo pipefail

          CHANGED_FILES="$RUNNER_TEMP/ci-risk-changed-files.txt"
          : > "$CHANGED_FILES"
          DIFF_BASE=""

          if [[ "$EVENT_NAME" == "pull_request" ]]; then
            git fetch origin "$BASE_REF" --depth=1
            DIFF_BASE="origin/$BASE_REF"
            git diff --name-only "$DIFF_BASE" HEAD > "$CHANGED_FILES"
          elif [[ "$EVENT_NAME" == "push" ]]; then
            DIFF_BASE="HEAD~1"
            git diff --name-only "$DIFF_BASE" HEAD > "$CHANGED_FILES" 2>/dev/null || git show --pretty="" --name-only HEAD > "$CHANGED_FILES"
          elif [[ "$EVENT_NAME" == "workflow_dispatch" ]]; then
            git ls-files > "$CHANGED_FILES"
          fi

          echo "Changed files for CI risk classification:"
          sed 's/^/  /' "$CHANGED_FILES" || true
          echo "path=$CHANGED_FILES" >> "$GITHUB_OUTPUT"
          echo "diff_base=$DIFF_BASE" >> "$GITHUB_OUTPUT"
      - name: Classify CI risk
        id: classify
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p "$RUNNER_TEMP/ci-harness"
          RISK_JSON="$RUNNER_TEMP/ci-harness/ci-risk-classification.json"

          node scripts/ci-harness.mjs classify-risk \
            --files "${{ steps.changed-files.outputs.path }}" \
            --diff-base "${{ steps.changed-files.outputs.diff_base }}" \
            --github-output "$GITHUB_OUTPUT" | tee "$RISK_JSON"

          RISK_LEVEL=$(jq -r '.riskLevel' "$RISK_JSON")
          REQUIRES_SMOKE=$(jq -r '.requiresSmoke' "$RISK_JSON")
          REQUIRES_PREVIEW=$(jq -r '.requiresPreview' "$RISK_JSON")
          BLOCKS_UNATTENDED=$(jq -r '.blocksUnattendedAutoMerge' "$RISK_JSON")
          MATCHED_RULES=$(jq -r '[.matchedRules[].id] | join(",") // ""' "$RISK_JSON")
          if [[ -z "$MATCHED_RULES" ]]; then
            MATCHED_RULES="none"
          fi

          {
            echo "### CI risk classification"
            echo
            echo "- Risk level: $RISK_LEVEL"
            echo "- Requires smoke: $REQUIRES_SMOKE"
            echo "- Requires preview: $REQUIRES_PREVIEW"
            echo "- Blocks unattended auto-merge: $BLOCKS_UNATTENDED"
            echo "- Matched rules: $MATCHED_RULES"
            echo
            echo "Next local command:"
            echo
            echo '```bash'
            echo 'pnpm ci:harness:check'
            echo '```'
          } >> "$GITHUB_STEP_SUMMARY"
      - name: Upload CI risk artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: ci-risk-classification-${{ github.run_id }}-${{ github.run_attempt }}
          path: ${{ runner.temp }}/ci-harness/ci-risk-classification.json
          retention-days: 7

  ci-env-example-guard:
    needs: [ci-path-changes]
    if: ${{ needs.ci-path-changes.outputs.skip == 'false' && (github.event_name == 'workflow_dispatch' || (github.event_name == 'push' && github.ref == 'refs/heads/main')) }}
    name: Env Example Guard
    # Always run; fast, prevents accidental secret commits.
    runs-on: ${{ vars.CI_GATE_RUNNER || 'ubuntu-latest' }}
    timeout-minutes: 3
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with: { fetch-depth: 1 }
      - name: Validate .env.example contains placeholders only
        run: |
          set -euo pipefail

          if [ ! -f .env.example ]; then
            echo "No .env.example found; skipping."
            exit 0
          fi

          # Fail on common secret/token prefixes.
          if grep -nE '(sk_live_|sk_test_|rk_live_|rk_test_|whsec_|ghp_|github_pat_|xoxb-|BEGIN PRIVATE KEY|-----BEGIN)' .env.example; then
            echo "❌ .env.example appears to contain a real secret/token. Replace with placeholders."
            exit 1
          fi

          # Fail on real-looking Stripe price IDs (these should be placeholders).
          if grep -nE '^STRIPE_PRICE_.*=price_[A-Za-z0-9]+' .env.example; then
            echo "❌ .env.example contains Stripe price IDs. Replace with placeholders."
            exit 1
          fi

          echo "✅ .env.example looks safe (no obvious secrets)."

  ci-secret-scan:
    needs: [ci-path-changes]
    if: ${{ needs.ci-path-changes.outputs.skip == 'false' && (github.event_name == 'workflow_dispatch' || (github.event_name == 'push' && github.ref == 'refs/heads/main')) }}
    name: Secret Scan (gitleaks + trufflehog)
    # Always run; fast, catches secrets in PR diffs including *.backup files (JOV-3215).
    runs-on: ${{ vars.CI_GATE_RUNNER || 'ubuntu-latest' }}
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 0
      - name: Run secret scan
        shell: bash
        run: |
          set -euo pipefail
          if [[ "${{ github.event_name }}" == "pull_request" ]]; then
            git fetch origin "${{ github.base_ref }}"
            ./scripts/security/scan-secrets.sh ci-pr "origin/${{ github.base_ref }}"
          elif [[ "${{ github.event_name }}" == "push" ]]; then
            if [[ "${{ github.event.before }}" == "0000000000000000000000000000000000000000" ]]; then
              git fetch origin main --depth=1
              ./scripts/security/scan-secrets.sh ci-pr "origin/main"
            else
              ./scripts/security/scan-secrets.sh ci-pr "${{ github.event.before }}"
            fi
          else
            git fetch origin main --depth=1
            ./scripts/security/scan-secrets.sh ci-pr "origin/main"
          fi
      - name: Verify backup-file fixture coverage
        run: ./scripts/security/verify-gitleaks-coverage.sh

  ci-fast:
    name: ci-fast
    # JOV-3464: fold cheap cluster into one job (checkout+install once).
    # Lanes: biome, eslint server-boundaries, typecheck --force, guardrails,
    # structural (path-gated). Per-lane PASS/FAIL via scripts/ci-fast-lanes.mjs
    # (--continue semantics; never set -e out on first failure). Heavy lanes
    # (build / Lighthouse / e2e / preview) stay separate.
    needs: [ci-path-changes]
    runs-on: ubuntu-latest
    # Setup/DB work (~2m) + accepted source deploy (up to 10m) + readiness
    # verification (up to 9m) must fit without the job canceling a healthy build.
    timeout-minutes: 25
    if: ${{ needs.ci-path-changes.outputs.skip == 'false' && (github.event_name != 'pull_request' || needs.ci-path-changes.outputs.has_code_changes == 'true') }}
    env:
      # Match prior Structural Contract job so actionlint/shellcheck is hermetic.
      SHELLCHECK_OPTS: >-
        --exclude=SC2001,SC2016,SC2028,SC2034,SC2086,SC2126,SC2129,SC2329
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 0
      - uses: ./.github/actions/setup-node-pnpm
      - name: Cache Turbo
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: .turbo
          key: ${{ runner.os }}-turbo-${{ hashFiles('turbo.json', '**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-turbo-
      - name: Decide structural lane
        id: structural
        shell: bash
        run: |
          set -euo pipefail
          if [[ "${{ github.event_name }}" != "pull_request" ]]; then
            echo "skip=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          # Full (non-shallow) base fetch: guardrails needs git merge-base with
          # origin/<base>; a --depth=1 fetch leaves the base tip shallow and fails
          # desktop-release-guard / version-fanout-guard on PRs.
          git fetch origin "${{ github.base_ref }}" --no-tags
          if git diff --name-only "origin/${{ github.base_ref }}" HEAD | grep -qE '^(\.github/|\.claude/|AGENTS\.md|CODEX\.md|scripts/lib/(ci-|merge-queue)|scripts/ci-|scripts/tests/test_vercel_prebuilt_deploy\.py)'; then
            echo "skip=false" >> "$GITHUB_OUTPUT"
          else
            echo "skip=true" >> "$GITHUB_OUTPUT"
          fi
      - name: Install actionlint (structural)
        if: steps.structural.outputs.skip != 'true'
        uses: rhysd/actionlint@914e7df21a07ef503a81201c76d2b11c789d3fca # v1.7.12
      - name: Set up Python (structural regression tests)
        if: steps.structural.outputs.skip != 'true'
        uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6.3.0
        with:
          python-version: '3.12'
      - name: Install pytest (structural regression tests)
        if: steps.structural.outputs.skip != 'true'
        run: pip install pytest --quiet
      - name: Run ci-fast lanes
        id: lanes
        env:
          TURBO_SCM_BASE: ${{ (github.base_ref && format('origin/{0}', github.base_ref)) || '' }}
          CI_FAST_SKIP_STRUCTURAL: ${{ steps.structural.outputs.skip }}
          CI_FAST_LANES_OUT: ${{ runner.temp }}/ci-fast-lanes.json
        run: node scripts/ci-fast-lanes.mjs
      - name: Upload ci-fast lane results
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: ci-fast-lanes-${{ github.run_id }}-${{ github.run_attempt }}
          path: ${{ runner.temp }}/ci-fast-lanes.json
          retention-days: 7
          if-no-files-found: warn

  ci-promptfoo-evals:
    name: Promptfoo Evals (deterministic)
    needs: [ci-path-changes]
    # Deterministic gate: keep on always-available GitHub runners (see ci-biome).
    runs-on: ${{ vars.CI_FAST_RUNNER || 'ubuntu-latest' }}
    timeout-minutes: 10
    if: ${{ needs.ci-path-changes.outputs.skip == 'false' && (github.event_name == 'workflow_dispatch' || (github.event_name == 'push' && github.ref == 'refs/heads/main' && needs.ci-path-changes.outputs.run_promptfoo_evals == 'true')) }}
    env:
      JOVIE_RUN_LIVE_EVALS: '0'
      JOVIE_RUN_LIVE_HTTP_EVALS: '0'
      JOVIE_RUN_LIVE_HTTP_RATE_LIMIT_EVALS: '0'
      JOVIE_RUN_LIVE_HTTP_MODEL_ERROR_EVALS: '0'
      PROMPTFOO_DISABLE_TELEMETRY: '1'
      PROMPTFOO_DISABLE_UPDATE: '1'
      PROMPTFOO_NO_TESTCASE_ASSERT_WARNING: '1'
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with: { fetch-depth: 1 }
      - uses: ./.github/actions/setup-node-pnpm
      - name: Validate Promptfoo config
        run: pnpm --filter @jovie/web run evals:validate
      - name: Run deterministic Promptfoo evals
        run: pnpm run evals

  ci-golden-eval-set:
    name: Golden Eval Set (deterministic)
    needs: [ci-path-changes]
    # Deterministic gate: keep on always-available GitHub runners (see ci-biome).
    runs-on: ${{ vars.CI_FAST_RUNNER || 'ubuntu-latest' }}
    timeout-minutes: 5
    if: ${{ needs.ci-path-changes.outputs.skip == 'false' && (github.event_name == 'workflow_dispatch' || (github.event_name == 'push' && github.ref == 'refs/heads/main' && needs.ci-path-changes.outputs.run_golden_eval_set == 'true')) }}
    env:
      JOVIE_RUN_LIVE_EVALS: '0'
      JOVIE_RUN_LIVE_HTTP_EVALS: '0'
      JOVIE_RUN_LIVE_HTTP_RATE_LIMIT_EVALS: '0'
      JOVIE_RUN_LIVE_HTTP_MODEL_ERROR_EVALS: '0'
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with: { fetch-depth: 1 }
      - uses: ./.github/actions/setup-node-pnpm
      - name: Run golden eval-set CI gate
        run: pnpm run evals:golden

  ci-knip:
    name: Knip (dead code)
    # Detect unused files, dependencies, and exports
    needs: [ci-path-changes]
    if: ${{ needs.ci-path-changes.outputs.skip == 'false' && (github.event_name == 'workflow_dispatch' || (github.event_name == 'push' && github.ref == 'refs/heads/main' && needs.ci-path-changes.outputs.has_code_changes == 'true')) }}
    runs-on: ${{ vars.CI_FAST_RUNNER || 'ubuntu-latest' }}
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with: { fetch-depth: 1 }
      - uses: ./.github/actions/setup-node-pnpm
      - name: Run Knip (unused files, deps & exports)
        run: pnpm knip

  neon-db:
    name: Neon DB
    needs: [ci-path-changes]
    # Run for push to main, testing-labeled full CI, or PRs touching DB-relevant paths.
    # run_neon covers drizzle, lib/db, lib/queries, schema, cron routes, SQL files, and
    # package.json changes. PRs touching only UI/perf/docs skip this job entirely.
    if: ${{ needs.ci-path-changes.outputs.skip == 'false' && (github.event_name == 'push' || (github.event_name == 'pull_request' && github.event.pull_request.head.repo.fork == false && github.event.pull_request.user.login && github.event.pull_request.user.login != 'dependabot[bot]' && (needs.ci-path-changes.outputs.run_neon == 'true' || needs.ci-path-changes.outputs.run_public_lighthouse == 'true'))) }}
    runs-on: ${{ vars.CI_FAST_RUNNER || 'ubuntu-latest' }}
    timeout-minutes: 5
    concurrency:
      group: neon-endpoint-pool-${{ github.job }}-${{ (endsWith(format('{0}', github.event.pull_request.number || github.run_number), '0') || endsWith(format('{0}', github.event.pull_request.number || github.run_number), '4') || endsWith(format('{0}', github.event.pull_request.number || github.run_number), '8')) && '0' || (endsWith(format('{0}', github.event.pull_request.number || github.run_number), '1') || endsWith(format('{0}', github.event.pull_request.number || github.run_number), '5') || endsWith(format('{0}', github.event.pull_request.number || github.run_number), '9')) && '1' || (endsWith(format('{0}', github.event.pull_request.number || github.run_number), '2') || endsWith(format('{0}', github.event.pull_request.number || github.run_number), '6')) && '2' || '3' }}
      cancel-in-progress: false
    outputs:
      branch_name: ${{ steps.sanitize-branch.outputs.name }}
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 1

      - name: Check for backend changes
        id: check-backend
        if: ${{ github.event_name == 'pull_request' }}
        run: |
          set -euo pipefail

          git fetch origin "${{ github.base_ref }}" --depth=1
          CHANGED_FILES=$(git diff --name-only "origin/${{ github.base_ref }}" HEAD)

          # Check if any backend-related files changed (migrations, lib/db, API routes, etc.)
          if echo "$CHANGED_FILES" | grep -E '^(drizzle/migrations/|apps/web/lib/db/|apps/web/app/api/)' > /dev/null; then
            echo "Backend changes detected, Neon DB required"
            echo "needs_db=true" >> "$GITHUB_OUTPUT"
          else
            echo "No backend changes detected, skipping Neon DB creation"
            echo "needs_db=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi

      - name: Cleanup old Neon branches (prevent limit errors)
        if: ${{ github.event_name != 'pull_request' || needs.ci-path-changes.outputs.run_neon == 'true' || needs.ci-path-changes.outputs.run_public_lighthouse == 'true' || steps.check-backend.outputs.needs_db == 'true' }}
        uses: ./.github/actions/neon-branch-cleanup
        with:
          neon_api_key: ${{ secrets.NEON_API_KEY }}
          neon_project_id: ${{ vars.NEON_PROJECT_ID }}
          branch_limit: '10'
          protected_branches: 'main,development,preview,br-main,br-production'
          cleanup_threshold: '7'

      - name: Determine sanitized branch name
        id: sanitize-branch
        if: ${{ github.event_name != 'pull_request' || needs.ci-path-changes.outputs.run_neon == 'true' || needs.ci-path-changes.outputs.run_public_lighthouse == 'true' || steps.check-backend.outputs.needs_db == 'true' }}
        env:
          RAW_REF: ${{ github.head_ref || github.ref_name }}
          EVENT_NAME: ${{ github.event_name }}
          REF_NAME: ${{ github.ref_name }}
          RUN_ID: ${{ github.run_id }}
          RUN_ATTEMPT: ${{ github.run_attempt }}
        run: |
          RAW="$RAW_REF"

          # Avoid collisions with protected long-lived branches in Neon.
          # For push to main we reuse the long-lived Neon branch.
          if [[ "$EVENT_NAME" == "push" && "$REF_NAME" == "main" ]]; then
            RAW="$REF_NAME"
          fi

          # sanitize: lowercase, replace slashes with dashes, remove invalid chars, collapse repeats
          SANITIZED=$(printf '%s' "$RAW" | tr '[:upper:]' '[:lower:]' | sed -E 's|/|-|g; s|[^a-z0-9._-]|-|g; s/-{2,}/-/g; s/^-+//; s/-+$//')
          BASE=$(printf '%s' "$SANITIZED" | cut -c1-40)
          SUFFIX="${RUN_ID}-${RUN_ATTEMPT}"
          FINAL="${BASE}-${SUFFIX}"
          echo "Using sanitized branch name: $FINAL"
          echo "name=$FINAL" >> "$GITHUB_OUTPUT"
      - name: Create or reuse Neon branch (with retry)
        id: create-branch
        if: ${{ github.event_name != 'pull_request' || needs.ci-path-changes.outputs.run_neon == 'true' || needs.ci-path-changes.outputs.run_public_lighthouse == 'true' || steps.check-backend.outputs.needs_db == 'true' }}
        uses: ./.github/actions/neon-create-branch-with-retry
        with:
          project_id: ${{ vars.NEON_PROJECT_ID }}
          branch_name: ${{ steps.sanitize-branch.outputs.name }}
          role: neondb_owner
          api_key: ${{ secrets.NEON_API_KEY }}
          expires_in_hours: '2'
      - name: Finalize Neon DB URLs
        id: finalize-urls
        if: ${{ github.event_name != 'pull_request' || needs.ci-path-changes.outputs.run_neon == 'true' || needs.ci-path-changes.outputs.run_public_lighthouse == 'true' || steps.check-backend.outputs.needs_db == 'true' }}
        env:
          CONNECTION_FILE: ${{ steps.create-branch.outputs.connection_file }}
        run: |
          set -euo pipefail
          CONNECTION_FILE="${CONNECTION_FILE:-${RUNNER_TEMP:-/tmp}/neon-connection/connection.json}"
          if [ ! -f "$CONNECTION_FILE" ]; then
            echo "Neon connection file missing after branch create: $CONNECTION_FILE"
            echo "This indicates NEON_PROJECT_ID/NEON_API_KEY or the create-branch action is failing."
            exit 1
          fi
          if ! jq -e '
            (.db_url // "" | length > 0) and
            (.db_url_pooled // "" | length > 0)
          ' "$CONNECTION_FILE" >/dev/null; then
            echo "Neon connection file is missing db_url/db_url_pooled."
            exit 1
          fi
          # Intentionally do NOT expose DB_URL via job outputs.
          # GitHub will drop outputs that look like secrets, which breaks downstream jobs.
          echo "Neon DB URL resolved for this job (file-backed)."

      - name: Persist Neon DB connection artifact
        if: ${{ github.event_name != 'pull_request' || needs.ci-path-changes.outputs.run_neon == 'true' || needs.ci-path-changes.outputs.run_public_lighthouse == 'true' || steps.check-backend.outputs.needs_db == 'true' }}
        env:
          BRANCH_NAME: ${{ steps.sanitize-branch.outputs.name }}
          CONNECTION_FILE: ${{ steps.create-branch.outputs.connection_file }}
        run: |
          set -euo pipefail
          mkdir -p /tmp/neon-db-artifact
          # Prefer the file written by neon-create-branch-with-retry (avoids
          # re-interpolating DB passwords through GitHub expression expansion).
          if [ -n "${CONNECTION_FILE:-}" ] && [ -f "$CONNECTION_FILE" ]; then
            cp "$CONNECTION_FILE" /tmp/neon-db-artifact/connection.json
          elif [ -f "${RUNNER_TEMP:-/tmp}/neon-connection/connection.json" ]; then
            cp "${RUNNER_TEMP:-/tmp}/neon-connection/connection.json" /tmp/neon-db-artifact/connection.json
          else
            echo "Neon connection file missing; cannot persist artifact."
            exit 1
          fi
          # Ensure branch_name is present for downstream diagnostics.
          if [ -n "${BRANCH_NAME:-}" ]; then
            jq --arg branch_name "$BRANCH_NAME" '.branch_name = $branch_name' \
              /tmp/neon-db-artifact/connection.json > /tmp/neon-db-artifact/connection.json.tmp
            mv /tmp/neon-db-artifact/connection.json.tmp /tmp/neon-db-artifact/connection.json
          fi
          # Fail closed if password is missing from the artifact URL.
          if ! jq -e '
            (.db_url // "" | test("^postgres(ql)?://[^:]+:[^@]+@")) and
            (.db_url_pooled // "" | test("^postgres(ql)?://[^:]+:[^@]+@"))
          ' /tmp/neon-db-artifact/connection.json >/dev/null; then
            echo "Neon connection artifact is missing username/password credentials."
            exit 1
          fi

      - name: Upload Neon DB connection artifact
        if: ${{ github.event_name != 'pull_request' || needs.ci-path-changes.outputs.run_neon == 'true' || needs.ci-path-changes.outputs.run_public_lighthouse == 'true' || steps.check-backend.outputs.needs_db == 'true' }}
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: neon-db-connection-${{ github.run_id }}
          path: /tmp/neon-db-artifact/connection.json
          retention-days: 1

  drizzle-migration-guard:
    name: Migration Guard
    needs: [ci-path-changes]
    # Skip if 'skip-migration-guard' label is present (for schema consolidation)
    if: ${{ needs.ci-path-changes.outputs.skip == 'false' && (github.event_name == 'push' || (github.event_name == 'pull_request' && !contains(github.event.pull_request.labels.*.name, 'skip-migration-guard'))) }}
    runs-on: ${{ vars.CI_GATE_RUNNER || 'ubuntu-latest' }}
    timeout-minutes: 3
    outputs:
      run_full_ci: ${{ steps.check_changes.outputs.run_full_ci }}
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 0
      - name: Check for relevant changes
        id: check_changes
        shell: bash
        run: echo "run_full_ci=${{ needs.ci-path-changes.outputs.run_drizzle }}" >> "$GITHUB_OUTPUT"

      # Skip notification (updated condition)
      - name: Skip notification
        if: steps.check_changes.outputs.run_full_ci != 'true'
        run: |
          echo "Skipping migration guard as no DB/schema paths were changed."
          exit 0
      - name: Run Migration Guard
        if: steps.check_changes.outputs.run_full_ci == 'true'
        run: |
          # Make script executable
          chmod +x apps/web/scripts/check-migrations.sh
          chmod +x apps/web/scripts/validate-migrations.sh

          # Run migration guard and validation from apps/web directory
          cd apps/web

          ./scripts/check-migrations.sh
          ./scripts/validate-migrations.sh
        env:
          GITHUB_EVENT_PATH: ${{ github.event_path }}

  ci-drizzle-check:
    name: Drizzle Check
    needs: [neon-db, ci-path-changes]
    # Drizzle check runs on push to main or PRs with testing label
    if: ${{ needs.ci-path-changes.outputs.skip == 'false' && (github.event_name == 'push' || (github.event_name == 'pull_request' && contains(github.event.pull_request.labels.*.name, 'testing'))) }}
    runs-on: ${{ vars.CI_FAST_RUNNER || 'ubuntu-latest' }}
    timeout-minutes: 10
    outputs:
      run_full_ci: ${{ steps.check_changes.outputs.run_full_ci }}
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 1

      - name: Check for relevant changes
        id: check_changes
        shell: bash
        run: echo "run_full_ci=${{ needs.ci-path-changes.outputs.run_drizzle }}" >> "$GITHUB_OUTPUT"

      - name: Skip notification
        if: steps.check_changes.outputs.run_full_ci != 'true'
        run: |
          echo "Skipping Drizzle check as no DB/schema paths were changed."
          echo "To force a full CI run, add the 'testing' label to the PR."
          exit 0

      - name: Download Neon DB connection artifact
        if: steps.check_changes.outputs.run_full_ci == 'true'
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: neon-db-connection-${{ github.run_id }}
          path: /tmp/neon-db-connection

      - name: Resolve DATABASE_URL from Neon artifact
        id: resolve-drizzle-neon-db-url
        if: steps.check_changes.outputs.run_full_ci == 'true'
        uses: ./.github/actions/resolve-neon-database-url
        with:
          connection_file: /tmp/neon-db-connection/connection.json
          candidate_json_key: db_url
          fallback_json_key: db_url_pooled
          credential_source_url: ${{ secrets.DATABASE_URL_MAIN }}
          credential_fallback_url: ${{ secrets.DATABASE_URL }}

      - name: Export DATABASE_URL
        if: steps.check_changes.outputs.run_full_ci == 'true'
        run: echo "DATABASE_URL=${{ steps.resolve-drizzle-neon-db-url.outputs.database_url }}" >> "$GITHUB_ENV"

      - name: Fail if Neon DB URL is missing
        if: steps.check_changes.outputs.run_full_ci == 'true'
        run: |
          if [[ -z "$DATABASE_URL" ]]; then
            echo "No Neon ephemeral DATABASE_URL available. Refusing to run Drizzle check against staging/production DBs."
            exit 1
          fi

      - name: Setup Node.js and pnpm
        uses: ./.github/actions/setup-node-pnpm

      - name: Run Drizzle Check
        if: steps.check_changes.outputs.run_full_ci == 'true'
        run: pnpm --filter=@jovie/web run drizzle:check

  ci-integration-ready:
    name: Integration Fast
    # Lightweight gate for integration/* PRs (agent batch lanes). Full CI runs on train PRs to main.
    needs: [ci-path-changes, ci-risk-classifier, ci-build-public]
    if: ${{ needs.ci-path-changes.outputs.skip == 'false' && (always() && github.event_name == 'pull_request' && startsWith(github.event.pull_request.base.ref, 'integration/')) }}
    runs-on: ${{ vars.CI_FAST_RUNNER || 'ubuntu-latest' }}
    timeout-minutes: 2
    steps:
      - name: Evaluate integration fast checks
        run: |
          BUILD_RESULT="${{ needs.ci-build-public.result }}"
          RISK_RESULT="${{ needs.ci-risk-classifier.result }}"
          echo "ci-build-public: $BUILD_RESULT"
          echo "ci-risk-classifier: $RISK_RESULT"
          if [[ "$RISK_RESULT" != "success" ]]; then
            echo "Integration fast lane failed: risk classifier"
            exit 1
          fi
          if [[ "$BUILD_RESULT" != "success" && "$BUILD_RESULT" != "skipped" ]]; then
            echo "Integration fast lane failed: build"
            exit 1
          fi
          echo "Integration fast lane passed"

  ci-build-public:
    name: Build (public routes)
    needs: [ci-path-changes, ci-risk-classifier]
    # Shared Next.js build for a11y + layout-guard — build once, test twice.
    # On PRs to main: always runs (non-fork) for build validation. Part of the merge gate.
    # Merge queue intentionally skips build (already validated on the PR).
    # On main push: always runs for full coverage + JOV-4087 vercel deploy artifact.
    # Fork PRs are excluded (no secrets) — skipped via fork-pr-gate.
    if: ${{ needs.ci-path-changes.outputs.skip == 'false' && ((github.event_name == 'push' && github.ref == 'refs/heads/main') || (github.event_name == 'pull_request' && github.event.pull_request.base.ref == 'main' && github.event.pull_request.head.repo.fork == false) || github.event_name == 'workflow_dispatch') }}
    # Production deploy artifacts must not wait behind the saturated fixed PR
    # runner. Keep PR builds on jovie-fixed; route main to hosted capacity.
    runs-on: ${{ (github.event_name == 'push' && github.ref == 'refs/heads/main') && 'ubuntu-latest' || vars.CI_FAST_RUNNER || 'ubuntu-latest' }}
    timeout-minutes: 20
    outputs:
      has_artifact: ${{ steps.check_changes.outputs.run_full_ci }}
    steps:
      - name: Reset workspace git state (self-hosted)
        if: runner.environment != 'github-hosted'
        shell: bash
        run: |
          set -euo pipefail
          if [[ ! -d .git ]]; then exit 0; fi
          git sparse-checkout disable 2>/dev/null || true
          git config --local --unset-all core.sparseCheckout 2>/dev/null || true
          git config --local --unset-all core.sparseCheckoutCone 2>/dev/null || true
          rm -f .git/info/sparse-checkout
          git config --local --unset-all extensions.partialClone 2>/dev/null || true
          git config --local --unset-all remote.origin.promisorRemote 2>/dev/null || true

      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 2
          clean: true

      - name: Materialize full tree (self-hosted)
        if: runner.environment != 'github-hosted'
        shell: bash
        run: |
          set -euo pipefail
          git sparse-checkout disable 2>/dev/null || true
          git reset --hard HEAD
          git checkout-index -a -f

      - name: Verify checkout sentinel
        shell: bash
        run: |
          set -euo pipefail
          sentinel=".github/actions/setup-node-pnpm/action.yml"
          if [[ ! -f "$sentinel" ]]; then
            echo "::error::Incomplete checkout — missing ${sentinel}"
            exit 1
          fi

      - name: Check for relevant changes
        id: check_changes
        shell: bash
        run: |
          # Run when UI/component paths, public QA inputs, or risk-triggered smoke changed.
          RUN_BUILD="${{ needs.ci-path-changes.outputs.run_build }}"
          RUN_TEST="${{ needs.ci-path-changes.outputs.run_test }}"
          RUN_PUBLIC_LIGHTHOUSE="${{ needs.ci-path-changes.outputs.run_public_lighthouse }}"
          REQUIRES_SMOKE="${{ needs.ci-risk-classifier.outputs.requires_smoke }}"
          if [[ "$RUN_BUILD" == "true" || "$RUN_TEST" == "true" || "$RUN_PUBLIC_LIGHTHOUSE" == "true" || "$REQUIRES_SMOKE" == "true" ]]; then
            echo "run_full_ci=true" >> "$GITHUB_OUTPUT"
          else
            echo "run_full_ci=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Skip notification
        if: steps.check_changes.outputs.run_full_ci != 'true'
        run: echo "Skipping build — no UI-relevant paths changed."

      - uses: ./.github/actions/setup-node-pnpm
        if: steps.check_changes.outputs.run_full_ci == 'true'

      - name: Build app (public routes only — no secrets needed)
        if: steps.check_changes.outputs.run_full_ci == 'true'
        run: pnpm turbo build --filter=@jovie/web
        env:
          NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY: ${{ secrets.NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY }}
          # This shared artifact feeds bypass-based smoke/mobile evidence. The
          # real-Clerk golden path builds its own artifact below so these public
          # compile-time flags cannot leak into that lane.
          NEXT_PUBLIC_CLERK_MOCK: '1'
          NEXT_PUBLIC_CLERK_PROXY_DISABLED: '1'
          NEXT_IGNORE_ESLINT: '1'
          NEXT_IGNORE_TYPECHECK: '1'
          NEXT_PRIVATE_SKIP_SIZE_CHECK: 'true'

      - name: Verify build output exists
        if: steps.check_changes.outputs.run_full_ci == 'true'
        run: |
          if [ ! -d "apps/web/.next" ]; then
            echo "::error::Build output directory apps/web/.next does not exist — build may have used remote cache without producing local output"
            exit 1
          fi
          echo "Build output verified: $(du -sh apps/web/.next | cut -f1)"

      - name: Package build artifact
        if: steps.check_changes.outputs.run_full_ci == 'true'
        run: tar -cf /tmp/nextjs-build.tar -C apps/web .next

      - name: Upload build artifact
        if: steps.check_changes.outputs.run_full_ci == 'true'
        # ponytail: artifact only feeds informational downstream jobs (layout-guard,
        # a11y), so a transient FinalizeArtifact timeout must not fail this merge gate.
        # Build correctness is already asserted by the build + "Verify build output"
        # steps above. Re-enable hard failure if a downstream gate ever depends on it.
        continue-on-error: true
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: nextjs-build-${{ github.run_id }}
          path: /tmp/nextjs-build.tar
          retention-days: 1
          if-no-files-found: error

      # JOV-4087: produce .vercel/output once on main so deploy-staging can skip
      # its own vercel build (~3–5 min). Keep turbo `.next` artifact untouched for
      # downstream test jobs. Soft-fail so a Vercel CLI blip cannot red the merge gate.
      - name: Pull Vercel env (preview) for deploy artifact
        id: vercel_pull
        if: ${{ steps.check_changes.outputs.run_full_ci == 'true' && github.event_name == 'push' && github.ref == 'refs/heads/main' }}
        continue-on-error: true
        timeout-minutes: 10
        run: |
          set -euo pipefail
          scope_args=()
          if [ -n "${VERCEL_ORG_ID:-}" ]; then
            scope_args=(--scope "$VERCEL_ORG_ID")
          fi
          for i in {1..3}; do
            if ./node_modules/.bin/vercel pull --yes --environment=preview --token "$VERCEL_TOKEN" "${scope_args[@]}"; then
              break
            fi
            echo "Vercel pull attempt $i failed, retrying in 10s..."
            sleep 10
            if [ $i -eq 3 ]; then exit 1; fi
          done
          for env_file in .vercel/.env.preview.local .vercel/.env.local; do
            if [ -f "$env_file" ]; then
              grep -v '^TURBO_REMOTE_ONLY=' "$env_file" > "${env_file}.tmp" || true
              mv "${env_file}.tmp" "$env_file"
            fi
          done
        env:
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}

      - name: Vercel build (deploy artifact)
        id: vercel_build
        if: ${{ steps.check_changes.outputs.run_full_ci == 'true' && github.event_name == 'push' && github.ref == 'refs/heads/main' && steps.vercel_pull.outcome == 'success' }}
        continue-on-error: true
        timeout-minutes: 10
        run: |
          set -euo pipefail
          scope_args=()
          if [ -n "${VERCEL_ORG_ID:-}" ]; then
            scope_args=(--scope "$VERCEL_ORG_ID")
          fi
          # `vercel build` replaces the standalone tree. Snapshot the traced
          # runtime first, then restore it at the repo-relative paths recorded
          # in the generated function configs.
          test -d apps/web/.next/standalone/apps/web/.next/node_modules
          tar -czf /tmp/vercel-runtime-node-modules.tar.gz \
            -C apps/web/.next/standalone/apps/web/.next node_modules
          # Do NOT inherit NEXT_PUBLIC_CLERK_MOCK from the turbo build step —
          # deploy artifact must match staging preview compile flags.
          for i in {1..3}; do
            if env -u TURBO_REMOTE_ONLY -u NEXT_PUBLIC_CLERK_MOCK -u NEXT_PUBLIC_CLERK_PROXY_DISABLED \
              ./node_modules/.bin/vercel build --token "$VERCEL_TOKEN" "${scope_args[@]}"; then
              break
            fi
            echo "Vercel build attempt $i failed, retrying in 10s..."
            sleep 10
            if [ $i -eq 3 ]; then exit 1; fi
          done
          # Generated function configs reference chunks across the complete
          # Next server tree, not only edge assets. Preserve that trace root so
          # the prebuilt upload is closed over every generated server file.
          test -d apps/web/.next/server/chunks
          rm -rf apps/web/.next/node_modules
          tar -xzf /tmp/vercel-runtime-node-modules.tar.gz -C apps/web/.next
          find apps/web/.next/node_modules -maxdepth 1 -type d \
            -name 'import-in-the-middle-*' -print -quit | grep -q .
          # Vercel's function trace references the generated robots response at
          # its public-file path even though the source route is app/robots.ts.
          # Materialize it only inside the deploy artifact; the deploy helper
          # removes generated public files before any source fallback.
          test -f apps/web/.next/server/app/robots.txt.body
          cp apps/web/.next/server/app/robots.txt.body apps/web/public/robots.txt
          printf '%s\n' 'apps/web/public/robots.txt' \
            > .vercel/jovie-generated-public-files
          tar -czf /tmp/vercel-build-output.tar.gz -C . \
            .vercel/output \
            .vercel/jovie-generated-public-files \
            apps/web/public/robots.txt \
            apps/web/.next/BUILD_ID \
            apps/web/.next/app-path-routes-manifest.json \
            apps/web/.next/build-manifest.json \
            apps/web/.next/package.json \
            apps/web/.next/prerender-manifest.json \
            apps/web/.next/required-server-files.json \
            apps/web/.next/server \
            apps/web/.next/node_modules
          tar -tzf /tmp/vercel-build-output.tar.gz \
            apps/web/.next/package.json >/dev/null
          tar -tzf /tmp/vercel-build-output.tar.gz \
            apps/web/.next/server/chunks >/dev/null
          tar -tzf /tmp/vercel-build-output.tar.gz \
            apps/web/.next/node_modules >/dev/null
          tar -tzf /tmp/vercel-build-output.tar.gz \
            apps/web/public/robots.txt >/dev/null
          echo "vercel_artifact=true" >> "$GITHUB_OUTPUT"
        env:
          COREPACK_ENABLE_DOWNLOAD_PROMPT: 0
          COREPACK_ENABLE_STRICT: 0
          NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY: ${{ secrets.CLERK_PUBLISHABLE_KEY_STAGING }}
          CLERK_SECRET_KEY: ${{ secrets.CLERK_SECRET_KEY_STAGING }}
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}

      - name: Upload vercel build artifact
        if: ${{ steps.vercel_build.outputs.vercel_artifact == 'true' }}
        continue-on-error: true
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: vercel-build-output-${{ github.run_id }}
          path: /tmp/vercel-build-output.tar.gz
          retention-days: 1
          if-no-files-found: warn

  ci-layout-guard:
    name: Layout Guard
    needs: [ci-path-changes, ci-build-public]
    # Blocking on PRs via ci-pr-ready (flipped from informational — Tim directive,
    # a11y/visual gating mission 2026-07-06 / JOV-4060). Runs on PRs to main when the
    # shared build artifact exists, plus push:main for post-merge coverage.
    if: ${{ needs.ci-path-changes.outputs.skip == 'false' && ((github.event_name == 'push' && github.ref == 'refs/heads/main') || (github.event_name == 'pull_request' && github.event.pull_request.base.ref == 'main' && github.event.pull_request.head.repo.fork == false && github.event.pull_request.user.login && github.event.pull_request.user.login != 'dependabot[bot]')) && needs.ci-build-public.outputs.has_artifact == 'true' }}
    runs-on: ${{ vars.CI_FAST_RUNNER || 'ubuntu-latest' }}
    timeout-minutes: 8
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

      - uses: ./.github/actions/setup-node-pnpm

      - name: Setup Playwright (Chromium)
        uses: ./.github/actions/setup-playwright

      - name: Download build artifact
        id: download
        continue-on-error: true
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: nextjs-build-${{ github.run_id }}
          path: /tmp/nextjs-build-download

      - name: Extract build artifact
        if: steps.download.outcome == 'success'
        run: tar -xf /tmp/nextjs-build-download/nextjs-build.tar -C apps/web

      - name: Run layout overlap/touching guard
        if: steps.download.outcome == 'success'
        run: |
          cd apps/web

          if [ ! -f tests/e2e/layout-overlap-guard.spec.ts ]; then
            echo "⚠ layout-overlap-guard.spec.ts not found — skipping"
            exit 0
          fi
          mkdir -p tests/.auth
          echo '{"cookies":[],"origins":[]}' > tests/.auth/user.json
          PORT=3100 pnpm --filter=@jovie/web exec next start &
          SERVER_PID=$!
          for i in $(seq 1 30); do
            if curl -sf http://localhost:3100 > /dev/null 2>&1; then break; fi
            sleep 1
          done
          pnpm exec playwright test tests/e2e/layout-overlap-guard.spec.ts --project=chromium --reporter=line
          EXIT_CODE=$?
          kill $SERVER_PID 2>/dev/null || true
          exit $EXIT_CODE
        env:
          BASE_URL: http://localhost:3100
          SMOKE_ONLY: '1'
          CI: true
          NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY: ${{ secrets.NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY }}

      - name: Upload Playwright Report on Failure
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: layout-guard-report-${{ github.run_id }}-${{ github.run_attempt }}
          path: apps/web/playwright-report/
          retention-days: 3

  ci-mobile-overflow:
    name: Mobile Overflow Guard (${{ matrix.width }}px)
    needs: [ci-path-changes, neon-db, ci-build-public]
    if: ${{ needs.ci-path-changes.outputs.skip == 'false' && (github.event_name == 'pull_request' && github.event.pull_request.base.ref == 'main' && github.event.pull_request.head.repo.fork == false && needs.ci-path-changes.outputs.run_public_lighthouse == 'true' && github.event.pull_request.user.login && github.event.pull_request.user.login != 'dependabot[bot]') }}
    runs-on: ${{ vars.CI_FAST_RUNNER || 'ubuntu-latest' }}
    timeout-minutes: 45
    strategy:
      fail-fast: false
      # Cap concurrent widths so 3 PRs don't open 9 overflow runners at once.
      max-parallel: 1
      matrix:
        width: [320, 390, 430]
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 1

      - uses: ./.github/actions/setup-node-pnpm

      - name: Run static mobile overflow guard
        run: pnpm --filter=@jovie/web run mobile-overflow:guard

      - name: Download Neon DB connection artifact
        # Reuse the shared ephemeral branch from neon-db instead of DATABASE_URL_MAIN.
        # Screenshot/UI-only PRs still need seeded profile fixtures for Playwright.
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: neon-db-connection-${{ github.run_id }}
          path: /tmp/neon-db-connection

      - name: Resolve DATABASE_URL from Neon artifact
        # Use only the ephemeral branch credentials from the neon-db artifact.
        # Injecting DATABASE_URL_MAIN passwords onto an ephemeral host causes
        # password-auth retry storms during migrate/seed and SSR profile reads.
        id: resolve-mobile-overflow-neon-db-url
        uses: ./.github/actions/resolve-neon-database-url
        with:
          connection_file: /tmp/neon-db-connection/connection.json
          candidate_json_key: db_url_pooled
          fallback_json_key: db_url

      - name: Export DATABASE_URL (ephemeral branch)
        run: echo "DATABASE_URL=${{ steps.resolve-mobile-overflow-neon-db-url.outputs.database_url }}" >> "$GITHUB_ENV"

      - name: Fail if Neon DB URL is missing (Mobile Overflow)
        run: |
          if [[ -z "$DATABASE_URL" ]]; then
            echo "No Neon ephemeral DATABASE_URL available. Refusing to run mobile overflow against staging/production DBs."
            exit 1
          fi

      - name: Verify Neon DB connectivity (fail-fast)
        run: pnpm --filter=@jovie/web exec tsx tests/e2e/verify-neon-db-connectivity.ts
        env:
          DATABASE_URL: ${{ env.DATABASE_URL }}

      - name: Run migrations (ephemeral Neon)
        # 320px matrix shard owns migrate/seed so 390/430 siblings reuse one Neon branch.
        if: matrix.width == 320
        run: pnpm --filter=@jovie/web run drizzle:migrate:ci
        env:
          DATABASE_URL: ${{ env.DATABASE_URL }}
          NODE_ENV: test
          GIT_BRANCH: pr-${{ github.event.pull_request.number }}
          ALLOW_PROD_MIGRATIONS: 'true'

      - name: Seed mobile overflow fixtures
        if: matrix.width == 320
        run: pnpm --filter=@jovie/web run seed:test-data
        env:
          DATABASE_URL: ${{ env.DATABASE_URL }}
          NODE_ENV: test

      - name: Wait for shared Neon seed (mobile overflow width > 320)
        if: matrix.width != 320
        run: |
          echo "Waiting for 320px shard migrate/seed on shared Neon branch..."
          for attempt in $(seq 1 60); do
            if pnpm --filter=@jovie/web exec tsx tests/wait-for-public-qa-seed.ts; then
              echo "Shared Neon seed ready"
              exit 0
            fi
            echo "Attempt ${attempt}/60: seed markers not ready yet"
            sleep 5
          done
          echo "Timed out waiting for 320px shard seed on shared Neon branch"
          exit 1
        env:
          DATABASE_URL: ${{ env.DATABASE_URL }}

      - name: Setup Playwright (Chromium)
        uses: ./.github/actions/setup-playwright

      - name: Download build artifact
        id: download-build
        continue-on-error: true
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: nextjs-build-${{ github.run_id }}
          path: /tmp/nextjs-build-download

      - name: Extract build artifact
        if: steps.download-build.outcome == 'success'
        run: tar -xf /tmp/nextjs-build-download/nextjs-build.tar -C apps/web

      - name: Build app fallback
        if: steps.download-build.outcome != 'success'
        # Do not accept a remote artifact from the shared mock build. The
        # real-Clerk lane has a different compile-time auth contract.
        run: pnpm turbo build --filter=@jovie/web --force
        env:
          DATABASE_URL: ${{ env.DATABASE_URL }}
          NODE_ENV: test
          NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY: ${{ secrets.NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY }}
          NEXT_IGNORE_ESLINT: '1'
          NEXT_IGNORE_TYPECHECK: '1'
          NEXT_PRIVATE_SKIP_SIZE_CHECK: 'true'

      - name: Run mobile overflow Playwright guard
        run: |
          cd apps/web

          if [ ! -f .next/standalone/apps/web/server.js ]; then
            echo "Standalone server not found - mobile overflow guard cannot start."
            exit 1
          fi

          mkdir -p tests/.auth
          echo '{"cookies":[],"origins":[]}' > tests/.auth/user.json

          cleanup() {
            kill "$SERVER_PID" 2>/dev/null || true
          }
          trap cleanup EXIT

          # Export before server start so the production-built standalone process
          # inherits test-auth gates (step env alone is not enough if a wrapper
          # strips process.env for the backgrounded Node child on some runners).
          # HOSTNAME=localhost: GHA sets HOSTNAME to the runner machine name;
          # Next standalone uses it for URL construction, which fails the
          # test-auth trusted-host gate unless Host header is honored.
          export VERCEL_ENV=development
          export E2E_USE_TEST_AUTH_BYPASS=1
          export E2E_TEST_AUTH_PERSONA=creator-ready
          export NEXT_PUBLIC_E2E_MODE=1
          export NEXT_PUBLIC_CLERK_MOCK=1
          export NEXT_PUBLIC_CLERK_PROXY_DISABLED=1
          export HOSTNAME=localhost

          PORT=3100 node .next/standalone/apps/web/server.js &
          SERVER_PID=$!

          SERVER_READY=false
          for attempt in $(seq 1 30); do
            if curl -sf http://localhost:3100 > /dev/null 2>&1; then
              SERVER_READY=true
              break
            fi
            echo "Waiting for standalone server ($attempt/30)..."
            sleep 1
          done

          if [ "$SERVER_READY" = "false" ]; then
            echo "Standalone server failed to start within 30s."
            exit 1
          fi

          # The lane uses the deterministic test-auth bypass, so use the
          # no-auth config and avoid its unrelated legacy Clerk setup project.
          pnpm exec playwright test tests/e2e/mobile-overflow.spec.ts --config=playwright.config.noauth.ts --project=chromium --reporter=line
        env:
          BASE_URL: http://localhost:3100
          DATABASE_URL: ${{ env.DATABASE_URL }}
          NODE_ENV: test
          # Match ci-e2e-smoke: standalone is production-built; VERCEL_ENV=development
          # unlocks isExplicitDevelopmentEnvironment + test-auth enter on loopback.
          VERCEL_ENV: development
          CI: true
          E2E_SKIP_WEB_SERVER: '1'
          E2E_USE_TEST_AUTH_BYPASS: '1'
          E2E_TEST_AUTH_PERSONA: creator-ready
          NEXT_PUBLIC_E2E_MODE: '1'
          NEXT_PUBLIC_CLERK_MOCK: '1'
          NEXT_PUBLIC_CLERK_PROXY_DISABLED: '1'
          NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY: ${{ secrets.NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY }}
          CLERK_SECRET_KEY: ${{ secrets.CLERK_SECRET_KEY }}
          FLAGS_SECRET: ${{ secrets.FLAGS_SECRET }}
          URL_ENCRYPTION_KEY: mobile-overflow-ci-url-encryption-key
          AI_GATEWAY_API_KEY: mobile-overflow-ci
          MOBILE_OVERFLOW_WIDTHS: ${{ matrix.width }}
          MOBILE_OVERFLOW_PUBLIC_WIDTHS: ${{ matrix.width }}
          MOBILE_OVERFLOW_FOCUS_LIMIT: '8'
          # Each matrix shard owns one mobile width. Keep navigation serialized
          # so user-state transitions are deterministic.
          PLAYWRIGHT_WORKERS: '1'

      - name: Upload Playwright Artifacts on Failure
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: mobile-overflow-report-${{ matrix.width }}-${{ github.run_id }}-${{ github.run_attempt }}
          path: |
            apps/web/playwright-report/
            apps/web/test-results/
          retention-days: 3

  ci-lighthouse-pr:
    name: Lighthouse (public routes PR)
    needs: [ci-build-public, ci-path-changes, neon-db]
    # Runs Lighthouse CI against the public-route build artifact on PRs.
    # Blocks PRs when triggered (path-gated to public-route changes).
    # Runs on push to main (post-merge validation), workflow_dispatch, or PRs with 'testing' label.
    if: ${{ needs.ci-path-changes.outputs.skip == 'false' && (((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'workflow_dispatch' || (github.event_name == 'pull_request' && github.event.pull_request.base.ref == 'main' && github.event.pull_request.head.repo.fork == false && github.event.pull_request.user.login && github.event.pull_request.user.login != 'dependabot[bot]' && contains(github.event.pull_request.labels.*.name, 'testing'))) && needs.ci-build-public.outputs.has_artifact == 'true' && needs.ci-path-changes.outputs.run_public_lighthouse == 'true') }}
    runs-on: ${{ vars.CI_FAST_RUNNER || 'ubuntu-latest' }}
    timeout-minutes: 30
    strategy:
      fail-fast: false
      # Serial shards: Lighthouse is CPU-heavy; 2 concurrent shards × N PRs saturates hosts.
      max-parallel: 1
      matrix:
        shard: [0, 1]
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - uses: ./.github/actions/setup-node-pnpm

      - name: Setup Playwright (Chromium)
        uses: ./.github/actions/setup-playwright

      - name: Download Neon DB connection artifact
        # Reuse the shared ephemeral branch from neon-db instead of DATABASE_URL_MAIN.
        # Screenshot/UI-only PRs still need seeded profile fixtures for shard-0 Playwright.
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: neon-db-connection-${{ github.run_id }}
          path: /tmp/neon-db-connection

      - name: Resolve DATABASE_URL from Neon artifact
        # Prefer ephemeral branch host from neon-db artifact. credential_source
        # only fills missing username/password components (Neon project roles
        # share passwords) — it never rewrites the ephemeral host. This recovers
        # when GITHUB_OUTPUT/artifact handoff strips credentials.
        id: resolve-lighthouse-neon-db-url
        uses: ./.github/actions/resolve-neon-database-url
        with:
          connection_file: /tmp/neon-db-connection/connection.json
          candidate_json_key: db_url_pooled
          fallback_json_key: db_url
          credential_source_url: ${{ secrets.DATABASE_URL_MAIN }}
          credential_fallback_url: ${{ secrets.DATABASE_URL }}

      - name: Export DATABASE_URL (ephemeral branch)
        run: echo "DATABASE_URL=${{ steps.resolve-lighthouse-neon-db-url.outputs.database_url }}" >> "$GITHUB_ENV"

      - name: Fail if Neon DB URL is missing (Lighthouse)
        run: |
          if [[ -z "$DATABASE_URL" ]]; then
            echo "No Neon ephemeral DATABASE_URL available. Refusing to run public Lighthouse against staging/production DBs."
            exit 1
          fi

      - name: Verify Neon DB connectivity (fail-fast)
        run: pnpm --filter=@jovie/web exec tsx tests/e2e/verify-neon-db-connectivity.ts
        env:
          DATABASE_URL: ${{ env.DATABASE_URL }}

      - name: Run migrations (ephemeral Neon)
        # Shard 0 owns migrate/seed so matrix siblings reuse one Neon branch.
        if: matrix.shard == 0
        run: pnpm --filter=@jovie/web run drizzle:migrate:ci
        env:
          DATABASE_URL: ${{ env.DATABASE_URL }}
          NODE_ENV: test
          GIT_BRANCH: pr-${{ github.event.pull_request.number }}
          ALLOW_PROD_MIGRATIONS: 'true'

      - name: Seed public QA fixtures
        if: matrix.shard == 0
        run: pnpm --filter=@jovie/web run seed:test-data
        env:
          DATABASE_URL: ${{ env.DATABASE_URL }}
          NODE_ENV: test

      - name: Wait for shared Neon seed (lighthouse shard > 0)
        if: matrix.shard != 0
        run: |
          echo "Waiting for shard 0 migrate/seed on shared Neon branch..."
          for attempt in $(seq 1 60); do
            if pnpm --filter=@jovie/web exec tsx tests/wait-for-public-qa-seed.ts; then
              echo "Shared Neon seed ready"
              exit 0
            fi
            echo "Attempt ${attempt}/60: seed markers not ready yet"
            sleep 5
          done
          echo "Timed out waiting for shard 0 seed on shared Neon branch"
          exit 1
        env:
          DATABASE_URL: ${{ env.DATABASE_URL }}

      - name: Download build artifact
        id: download-build
        continue-on-error: true
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: nextjs-build-${{ github.run_id }}
          path: /tmp/nextjs-build-download

      - name: Extract build artifact
        if: steps.download-build.outcome == 'success'
        run: tar -xf /tmp/nextjs-build-download/nextjs-build.tar -C apps/web

      - name: Build app fallback for public Lighthouse
        if: steps.download-build.outcome != 'success'
        run: pnpm --filter=@jovie/web run build
        env:
          NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY: ${{ secrets.NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY }}
          NEXT_IGNORE_ESLINT: '1'
          NEXT_IGNORE_TYPECHECK: '1'
          NEXT_PRIVATE_SKIP_SIZE_CHECK: 'true'
          SKIP_SKILLS_CATALOG_SYNC: '1'

      - name: Run Lighthouse CI (public launch thresholds)
        run: |
          cd apps/web
          cleanup() {
            kill $SERVER_PID 2>/dev/null || true
          }
          trap cleanup EXIT

          export NEXT_PUBLIC_CLERK_MOCK=1
          export NEXT_PUBLIC_CLERK_PROXY_DISABLED=1

          if [ ! -f .next/standalone/apps/web/server.js ]; then
            echo "Standalone server not found - public Lighthouse cannot start."
            exit 1
          fi

          PORT=3000 node .next/standalone/apps/web/server.js &
          SERVER_PID=$!

          echo "Waiting for standalone server..."
          SERVER_READY=false
          for i in {1..30}; do
            if curl -s http://localhost:3000 > /dev/null; then
              echo "Server is ready!"
              SERVER_READY=true
              break
            fi
            sleep 1
          done

          if [ "$SERVER_READY" = "false" ]; then
            echo "Server failed to start within 30s."
            exit 1
          fi

          pnpm run test:lighthouse:public:launch

          # Pin the extra mobile-viewport-stability Playwright spec to the lighter
          # LHCI shard (shard 1 collects 9 URLs vs shard 0's 10). Both shards run
          # in parallel and the merge gate waits on max(shard0, shard1); piling the
          # ~1.5m spec onto the already-heavier shard 0 made it the gate long pole
          # rebalances the only-2-way fan-out and lowers wall-clock with identical
          # coverage.
          if [ "${{ matrix.shard }}" = "1" ]; then
            mkdir -p tests/.auth
            echo '{"cookies":[],"origins":[]}' > tests/.auth/user.json
            pnpm exec playwright test tests/e2e/profile-mobile-viewport-stability.spec.ts --config=playwright.config.noauth.ts --project=chromium --reporter=line
          fi
        env:
          CI: true
          BASE_URL: http://127.0.0.1:3000
          PUBLIC_LIGHTHOUSE_SHARD_INDEX: ${{ matrix.shard }}
          PUBLIC_LIGHTHOUSE_TOTAL_SHARDS: '2'
          PROFILE_MOBILE_PERF_BUDGETS: '1'
          NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY: ${{ secrets.NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY }}
          CLERK_SECRET_KEY: ${{ secrets.CLERK_SECRET_KEY }}
          DATABASE_URL: ${{ env.DATABASE_URL }}
          PUBLIC_NOAUTH_SMOKE: '1'
          URL_ENCRYPTION_KEY: lighthouse-public-ci-url-encryption-key
          AI_GATEWAY_API_KEY: lighthouse-public-ci

  ci-lighthouse-dashboard-pr:
    name: Lighthouse (dashboard PR)
    needs: [ci-path-changes, neon-db, ci-build-public]
    if: ${{ needs.ci-path-changes.outputs.skip == 'false' && (github.event_name == 'push' || github.event_name == 'workflow_dispatch' || (github.event_name == 'pull_request' && github.event.pull_request.base.ref == 'main' && github.event.pull_request.head.repo.fork == false && contains(github.event.pull_request.labels.*.name, 'testing'))) }}
    runs-on: ${{ vars.CI_FAST_RUNNER || 'ubuntu-latest' }}
    timeout-minutes: 30
    concurrency:
      group: neon-endpoint-pool-${{ github.job }}-${{ (endsWith(format('{0}', github.event.pull_request.number || github.run_number), '0') || endsWith(format('{0}', github.event.pull_request.number || github.run_number), '4') || endsWith(format('{0}', github.event.pull_request.number || github.run_number), '8')) && '0' || (endsWith(format('{0}', github.event.pull_request.number || github.run_number), '1') || endsWith(format('{0}', github.event.pull_request.number || github.run_number), '5') || endsWith(format('{0}', github.event.pull_request.number || github.run_number), '9')) && '1' || (endsWith(format('{0}', github.event.pull_request.number || github.run_number), '2') || endsWith(format('{0}', github.event.pull_request.number || github.run_number), '6')) && '2' || '3' }}
      cancel-in-progress: false
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 1

      - name: Check for relevant changes
        id: check_changes
        shell: bash
        run: |
          RUN_BUILD="${{ needs.ci-path-changes.outputs.run_build }}"
          RUN_TEST="${{ needs.ci-path-changes.outputs.run_test }}"
          RUN_DASHBOARD_LIGHTHOUSE="${{ needs.ci-path-changes.outputs.run_dashboard_lighthouse }}"
          if [[ "$RUN_BUILD" == "true" || "$RUN_TEST" == "true" || "$RUN_DASHBOARD_LIGHTHOUSE" == "true" ]]; then
            echo "run_full_ci=true" >> "$GITHUB_OUTPUT"
          else
            echo "run_full_ci=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Skip notification
        if: steps.check_changes.outputs.run_full_ci != 'true'
        run: echo "Skipping dashboard Lighthouse — no dashboard-relevant paths changed."

      - uses: ./.github/actions/setup-node-pnpm
        if: steps.check_changes.outputs.run_full_ci == 'true'

      - name: Setup Playwright (Chromium)
        if: steps.check_changes.outputs.run_full_ci == 'true'
        uses: ./.github/actions/setup-playwright

      - name: Create ephemeral Neon database branch (with retry)
        id: neon-branch
        if: steps.check_changes.outputs.run_full_ci == 'true'
        uses: ./.github/actions/neon-create-branch-with-retry
        with:
          project_id: ${{ vars.NEON_PROJECT_ID }}
          branch_name: dashboard-lighthouse-${{ github.run_id }}-${{ github.run_attempt }}
          role: neondb_owner
          api_key: ${{ secrets.NEON_API_KEY }}
          protected_branches: main,development,preview,br-main,br-production,${{ needs.neon-db.outputs.branch_name }}

      - name: Resolve DATABASE_URL (Neon ephemeral)
        if: steps.check_changes.outputs.run_full_ci == 'true'
        id: resolve-dashboard-neon-db-url
        uses: ./.github/actions/resolve-neon-database-url
        with:
          candidate_url: ${{ steps.neon-branch.outputs.db_url_pooled }}
          fallback_candidate_url: ${{ steps.neon-branch.outputs.db_url }}
          credential_source_url: ${{ secrets.DATABASE_URL_MAIN }}
          credential_fallback_url: ${{ secrets.DATABASE_URL }}

      - name: Export DATABASE_URL
        if: steps.check_changes.outputs.run_full_ci == 'true'
        run: echo "DATABASE_URL=${{ steps.resolve-dashboard-neon-db-url.outputs.database_url }}" >> "$GITHUB_ENV"

      - name: Run migrations (ephemeral Neon)
        if: steps.check_changes.outputs.run_full_ci == 'true'
        run: pnpm --filter=@jovie/web run drizzle:migrate:ci
        env:
          DATABASE_URL: ${{ env.DATABASE_URL }}
          NODE_ENV: test
          GIT_BRANCH: pr-${{ github.event.pull_request.number }}
          ALLOW_PROD_MIGRATIONS: 'true'

      - name: Download build artifact
        if: steps.check_changes.outputs.run_full_ci == 'true'
        id: download-build
        continue-on-error: true
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: nextjs-build-${{ github.run_id }}
          path: /tmp/nextjs-build-download

      - name: Extract build artifact
        if: steps.check_changes.outputs.run_full_ci == 'true' && steps.download-build.outcome == 'success'
        run: tar -xf /tmp/nextjs-build-download/nextjs-build.tar -C apps/web

      - name: Build app fallback for dashboard Lighthouse
        if: steps.check_changes.outputs.run_full_ci == 'true' && steps.download-build.outcome != 'success'
        run: pnpm --filter=@jovie/web run build
        env:
          NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY: ${{ secrets.NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY }}
          NEXT_IGNORE_ESLINT: '1'
          NEXT_IGNORE_TYPECHECK: '1'
          NEXT_PRIVATE_SKIP_SIZE_CHECK: 'true'
          SKIP_SKILLS_CATALOG_SYNC: '1'

      - name: Run authenticated dashboard Lighthouse
        if: steps.check_changes.outputs.run_full_ci == 'true'
        run: |
          cd apps/web

          if [ -z "$E2E_CLERK_USER_ID" ]; then
            echo "E2E_CLERK_USER_ID not set — skipping dashboard Lighthouse."
            exit 0
          fi

          export E2E_USE_TEST_AUTH_BYPASS=1
          export NEXT_PUBLIC_CLERK_MOCK=1
          export NEXT_PUBLIC_CLERK_PROXY_DISABLED=1

          if [ ! -f .next/standalone/apps/web/server.js ]; then
            echo "Standalone server not found — dashboard Lighthouse cannot start."
            exit 1
          fi

          node .next/standalone/apps/web/server.js &
          SERVER_PID=$!
          trap 'kill $SERVER_PID 2>/dev/null || true' EXIT

          echo "Waiting for standalone server..."
          SERVER_READY=false
          for i in {1..30}; do
            if curl -s http://localhost:3000 > /dev/null; then
              SERVER_READY=true
              break
            fi
            sleep 1
          done

          if [ "$SERVER_READY" = "false" ]; then
            echo "Server failed to start within 30s."
            exit 1
          fi

          pnpm run test:lighthouse:dashboard:pr
        env:
          CI: true
          BASE_URL: http://localhost:3000
          NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY: ${{ secrets.NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY }}
          CLERK_SECRET_KEY: ${{ secrets.CLERK_SECRET_KEY }}
          DATABASE_URL: ${{ env.DATABASE_URL }}
          URL_ENCRYPTION_KEY: lighthouse-dashboard-ci-url-encryption-key
          AI_GATEWAY_API_KEY: lighthouse-dashboard-ci
          FLAGS_SECRET: ${{ secrets.FLAGS_SECRET }}
          E2E_CLERK_USER_ID: ${{ secrets.E2E_CLERK_USER_ID }}
          E2E_CLERK_USER_USERNAME: ${{ secrets.E2E_CLERK_USER_USERNAME }}
          E2E_CLERK_USER_PASSWORD: ${{ secrets.E2E_CLERK_USER_PASSWORD }}

      - name: Upload dashboard Lighthouse artifacts
        if: always() && steps.check_changes.outputs.run_full_ci == 'true'
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: lighthouse-dashboard-pr-${{ github.run_id }}-${{ github.run_attempt }}
          path: apps/web/.lighthouseci/dashboard-pr/
          if-no-files-found: ignore
          retention-days: 3

      - name: Cleanup ephemeral Neon branch
        if: always() && steps.check_changes.outputs.run_full_ci == 'true' && steps.neon-branch.outputs.branch_id != ''
        continue-on-error: true
        run: |
          curl -sS -X DELETE \
            -H "Authorization: Bearer ${{ secrets.NEON_API_KEY }}" \
            -H "Content-Type: application/json" \
            "https://console.neon.tech/api/v2/projects/${{ vars.NEON_PROJECT_ID }}/branches/${{ steps.neon-branch.outputs.branch_id }}"

  ci-lighthouse-onboarding-pr:
    name: Lighthouse (onboarding PR)
    needs: [ci-path-changes, neon-db, ci-build-public]
    if: ${{ needs.ci-path-changes.outputs.skip == 'false' && ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'workflow_dispatch' || (github.event_name == 'pull_request' && github.event.pull_request.base.ref == 'main' && github.event.pull_request.head.repo.fork == false && github.event.pull_request.user.login && github.event.pull_request.user.login != 'dependabot[bot]' && needs.ci-path-changes.outputs.run_onboarding_lighthouse == 'true' && contains(github.event.pull_request.labels.*.name, 'testing'))) }}
    runs-on: ${{ vars.CI_FAST_RUNNER || 'ubuntu-latest' }}
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 1

      - uses: ./.github/actions/setup-node-pnpm

      - name: Setup Playwright (Chromium)
        uses: ./.github/actions/setup-playwright

      - name: Download Neon DB connection artifact
        # Reuse the shared ephemeral branch from neon-db to avoid parallel endpoint limits.
        if: needs.ci-path-changes.outputs.run_neon == 'true'
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: neon-db-connection-${{ github.run_id }}
          path: /tmp/neon-db-connection

      - name: Resolve DATABASE_URL from Neon artifact
        if: needs.ci-path-changes.outputs.run_neon == 'true'
        id: resolve-onboarding-neon-db-url
        uses: ./.github/actions/resolve-neon-database-url
        with:
          connection_file: /tmp/neon-db-connection/connection.json
          candidate_json_key: db_url_pooled
          fallback_json_key: db_url

      - name: Export DATABASE_URL (ephemeral branch)
        if: needs.ci-path-changes.outputs.run_neon == 'true'
        run: echo "DATABASE_URL=${{ steps.resolve-onboarding-neon-db-url.outputs.database_url }}" >> "$GITHUB_ENV"

      - name: Export DATABASE_URL (main — non-DB PR)
        # No ephemeral branch was created; use the stable main DB for SSR reads.
        if: needs.ci-path-changes.outputs.run_neon != 'true'
        run: echo "DATABASE_URL=${{ secrets.DATABASE_URL_MAIN }}" >> "$GITHUB_ENV"

      - name: Run migrations (ephemeral Neon)
        if: needs.ci-path-changes.outputs.run_neon == 'true'
        run: pnpm --filter=@jovie/web run drizzle:migrate:ci
        env:
          DATABASE_URL: ${{ env.DATABASE_URL }}
          NODE_ENV: test
          GIT_BRANCH: pr-${{ github.event.pull_request.number }}
          ALLOW_PROD_MIGRATIONS: 'true'

      - name: Download build artifact
        id: download-build
        continue-on-error: true
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: nextjs-build-${{ github.run_id }}
          path: /tmp/nextjs-build-download

      - name: Extract build artifact
        if: steps.download-build.outcome == 'success'
        run: tar -xf /tmp/nextjs-build-download/nextjs-build.tar -C apps/web

      - name: Build app fallback for onboarding Lighthouse
        if: steps.download-build.outcome != 'success'
        run: pnpm --filter=@jovie/web run build
        env:
          NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY: ${{ secrets.NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY }}
          NEXT_IGNORE_ESLINT: '1'
          NEXT_IGNORE_TYPECHECK: '1'
          NEXT_PRIVATE_SKIP_SIZE_CHECK: 'true'
          SKIP_SKILLS_CATALOG_SYNC: '1'

      - name: Run authenticated onboarding Lighthouse
        run: |
          cd apps/web

          if [ -z "$E2E_CLERK_USER_ID" ]; then
            echo "E2E_CLERK_USER_ID not set — skipping onboarding Lighthouse."
            exit 0
          fi

          export E2E_USE_TEST_AUTH_BYPASS=1
          export NEXT_PUBLIC_CLERK_MOCK=1
          export NEXT_PUBLIC_CLERK_PROXY_DISABLED=1

          if [ ! -f .next/standalone/apps/web/server.js ]; then
            echo "Standalone server not found — onboarding Lighthouse cannot start."
            exit 1
          fi

          node .next/standalone/apps/web/server.js &
          SERVER_PID=$!
          trap 'kill $SERVER_PID 2>/dev/null || true' EXIT

          echo "Waiting for standalone server..."
          SERVER_READY=false
          for i in {1..30}; do
            if curl -s http://localhost:3000 > /dev/null; then
              SERVER_READY=true
              break
            fi
            sleep 1
          done

          if [ "$SERVER_READY" = "false" ]; then
            echo "Server failed to start within 30s."
            exit 1
          fi

          pnpm run test:lighthouse:onboarding:pr
        env:
          CI: true
          BASE_URL: http://localhost:3000
          NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY: ${{ secrets.NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY }}
          CLERK_SECRET_KEY: ${{ secrets.CLERK_SECRET_KEY }}
          DATABASE_URL: ${{ env.DATABASE_URL }}
          URL_ENCRYPTION_KEY: lighthouse-onboarding-ci-url-encryption-key
          AI_GATEWAY_API_KEY: lighthouse-onboarding-ci
          FLAGS_SECRET: ${{ secrets.FLAGS_SECRET }}
          E2E_CLERK_USER_ID: ${{ secrets.E2E_CLERK_USER_ID }}
          E2E_CLERK_USER_USERNAME: ${{ secrets.E2E_CLERK_USER_USERNAME }}
          E2E_CLERK_USER_PASSWORD: ${{ secrets.E2E_CLERK_USER_PASSWORD }}

      - name: Upload onboarding Lighthouse artifacts
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: lighthouse-onboarding-pr-${{ github.run_id }}-${{ github.run_attempt }}
          path: apps/web/.lighthouseci/onboarding-pr/
          if-no-files-found: ignore
          retention-days: 3

  ci-lighthouse-admin-pr:
    name: Lighthouse (admin PR)
    # Reuse the shared neon-db artifact when run_neon=true; non-DB admin PRs read DATABASE_URL_MAIN.
    needs: [ci-path-changes, neon-db, ci-build-public]
    if: ${{ needs.ci-path-changes.outputs.skip == 'false' && ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'workflow_dispatch' || (github.event_name == 'pull_request' && github.event.pull_request.base.ref == 'main' && github.event.pull_request.head.repo.fork == false && github.event.pull_request.user.login && github.event.pull_request.user.login != 'dependabot[bot]' && needs.ci-path-changes.outputs.run_admin_lighthouse == 'true' && contains(github.event.pull_request.labels.*.name, 'testing'))) && (needs.neon-db.result == 'success' || needs.neon-db.result == 'skipped') }}
    runs-on: ${{ vars.CI_FAST_RUNNER || 'ubuntu-latest' }}
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 1

      - uses: ./.github/actions/setup-node-pnpm

      - name: Setup Playwright (Chromium)
        uses: ./.github/actions/setup-playwright

      - name: Download Neon DB connection artifact
        if: needs.ci-path-changes.outputs.run_neon == 'true'
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: neon-db-connection-${{ github.run_id }}
          path: /tmp/neon-db-connection

      - name: Resolve DATABASE_URL from Neon artifact
        if: needs.ci-path-changes.outputs.run_neon == 'true'
        id: resolve-admin-neon-db-url
        uses: ./.github/actions/resolve-neon-database-url
        with:
          connection_file: /tmp/neon-db-connection/connection.json
          candidate_json_key: db_url_pooled
          fallback_json_key: db_url

      - name: Export DATABASE_URL (ephemeral branch)
        if: needs.ci-path-changes.outputs.run_neon == 'true'
        run: echo "DATABASE_URL=${{ steps.resolve-admin-neon-db-url.outputs.database_url }}" >> "$GITHUB_ENV"

      - name: Export DATABASE_URL (main — non-DB PR)
        if: needs.ci-path-changes.outputs.run_neon != 'true'
        run: echo "DATABASE_URL=${{ secrets.DATABASE_URL_MAIN }}" >> "$GITHUB_ENV"

      - name: Run migrations (ephemeral Neon)
        if: needs.ci-path-changes.outputs.run_neon == 'true'
        run: pnpm --filter=@jovie/web run drizzle:migrate:ci
        env:
          DATABASE_URL: ${{ env.DATABASE_URL }}
          NODE_ENV: test
          GIT_BRANCH: pr-${{ github.event.pull_request.number }}
          ALLOW_PROD_MIGRATIONS: 'true'

      - name: Download build artifact
        id: download-build
        continue-on-error: true
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: nextjs-build-${{ github.run_id }}
          path: /tmp/nextjs-build-download

      - name: Extract build artifact
        if: steps.download-build.outcome == 'success'
        run: tar -xf /tmp/nextjs-build-download/nextjs-build.tar -C apps/web

      - name: Build app fallback for admin Lighthouse
        if: steps.download-build.outcome != 'success'
        run: pnpm --filter=@jovie/web run build
        env:
          NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY: ${{ secrets.NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY }}
          NEXT_IGNORE_ESLINT: '1'
          NEXT_IGNORE_TYPECHECK: '1'
          NEXT_PRIVATE_SKIP_SIZE_CHECK: 'true'
          SKIP_SKILLS_CATALOG_SYNC: '1'

      - name: Run authenticated admin Lighthouse
        run: |
          cd apps/web

          if [ -z "$E2E_CLERK_USER_ID" ]; then
            echo "E2E_CLERK_USER_ID empty (no E2E_ADMIN_CLERK_USER_ID secret) — skipping admin Lighthouse."
            exit 0
          fi

          export E2E_USE_TEST_AUTH_BYPASS=1
          export NEXT_PUBLIC_CLERK_MOCK=1
          export NEXT_PUBLIC_CLERK_PROXY_DISABLED=1

          if [ ! -f .next/standalone/apps/web/server.js ]; then
            echo "Standalone server not found — admin Lighthouse cannot start."
            exit 1
          fi

          node .next/standalone/apps/web/server.js &
          SERVER_PID=$!
          trap 'kill $SERVER_PID 2>/dev/null || true' EXIT

          echo "Waiting for standalone server..."
          SERVER_READY=false
          for i in {1..30}; do
            if curl -s http://localhost:3000 > /dev/null; then
              SERVER_READY=true
              break
            fi
            sleep 1
          done

          if [ "$SERVER_READY" = "false" ]; then
            echo "Server failed to start within 30s."
            exit 1
          fi

          pnpm run test:lighthouse:admin:pr
        env:
          CI: true
          BASE_URL: http://localhost:3000
          NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY: ${{ secrets.NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY }}
          CLERK_SECRET_KEY: ${{ secrets.CLERK_SECRET_KEY }}
          DATABASE_URL: ${{ env.DATABASE_URL }}
          URL_ENCRYPTION_KEY: lighthouse-admin-ci-url-encryption-key
          AI_GATEWAY_API_KEY: lighthouse-admin-ci
          FLAGS_SECRET: ${{ secrets.FLAGS_SECRET }}
          E2E_CLERK_USER_ID: ${{ secrets.E2E_ADMIN_CLERK_USER_ID }}
          E2E_CLERK_USER_USERNAME: ${{ secrets.E2E_ADMIN_CLERK_USER_USERNAME }}
          E2E_CLERK_USER_PASSWORD: ${{ secrets.E2E_ADMIN_CLERK_USER_PASSWORD }}

      - name: Upload admin Lighthouse artifacts
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: lighthouse-admin-pr-${{ github.run_id }}-${{ github.run_attempt }}
          path: apps/web/.lighthouseci/admin-pr/
          if-no-files-found: ignore
          retention-days: 3

  ci-lighthouse-chat-pr:
    name: Lighthouse (chat PR)
    # Informational baseline run — first PR establishes the budget; once
    # baselines stabilize this can be promoted to a hard gate by removing
    # `continue-on-error` and adding the job to ci-pr-ready.needs.
    needs: [ci-path-changes, neon-db, ci-build-public]
    if: ${{ needs.ci-path-changes.outputs.skip == 'false' && ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'workflow_dispatch' || (github.event_name == 'pull_request' && github.event.pull_request.base.ref == 'main' && github.event.pull_request.head.repo.fork == false && github.event.pull_request.user.login && github.event.pull_request.user.login != 'dependabot[bot]' && needs.ci-path-changes.outputs.run_chat_lighthouse == 'true' && contains(github.event.pull_request.labels.*.name, 'testing'))) }}
    continue-on-error: true
    runs-on: ${{ vars.CI_FAST_RUNNER || 'ubuntu-latest' }}
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 1

      - uses: ./.github/actions/setup-node-pnpm

      - name: Setup Playwright (Chromium)
        uses: ./.github/actions/setup-playwright

      - name: Download Neon DB connection artifact
        if: needs.ci-path-changes.outputs.run_neon == 'true'
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: neon-db-connection-${{ github.run_id }}
          path: /tmp/neon-db-connection

      - name: Resolve DATABASE_URL from Neon artifact
        if: needs.ci-path-changes.outputs.run_neon == 'true'
        id: resolve-chat-neon-db-url
        uses: ./.github/actions/resolve-neon-database-url
        with:
          connection_file: /tmp/neon-db-connection/connection.json
          candidate_json_key: db_url_pooled
          fallback_json_key: db_url

      - name: Export DATABASE_URL (ephemeral branch)
        if: needs.ci-path-changes.outputs.run_neon == 'true'
        run: echo "DATABASE_URL=${{ steps.resolve-chat-neon-db-url.outputs.database_url }}" >> "$GITHUB_ENV"

      - name: Export DATABASE_URL (main — non-DB PR)
        if: needs.ci-path-changes.outputs.run_neon != 'true'
        run: echo "DATABASE_URL=${{ secrets.DATABASE_URL_MAIN }}" >> "$GITHUB_ENV"

      - name: Run migrations (ephemeral Neon)
        if: needs.ci-path-changes.outputs.run_neon == 'true'
        run: pnpm --filter=@jovie/web run drizzle:migrate:ci
        env:
          DATABASE_URL: ${{ env.DATABASE_URL }}
          NODE_ENV: test
          GIT_BRANCH: pr-${{ github.event.pull_request.number }}
          ALLOW_PROD_MIGRATIONS: 'true'

      - name: Download build artifact
        id: download-build
        continue-on-error: true
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: nextjs-build-${{ github.run_id }}
          path: /tmp/nextjs-build-download

      - name: Extract build artifact
        if: steps.download-build.outcome == 'success'
        run: tar -xf /tmp/nextjs-build-download/nextjs-build.tar -C apps/web

      - name: Build app fallback for chat Lighthouse
        if: steps.download-build.outcome != 'success'
        run: pnpm --filter=@jovie/web run build
        env:
          NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY: ${{ secrets.NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY }}
          NEXT_IGNORE_ESLINT: '1'
          NEXT_IGNORE_TYPECHECK: '1'
          NEXT_PRIVATE_SKIP_SIZE_CHECK: 'true'
          SKIP_SKILLS_CATALOG_SYNC: '1'

      - name: Run authenticated chat Lighthouse
        run: |
          cd apps/web

          if [ -z "$E2E_CLERK_USER_ID" ]; then
            echo "E2E_CLERK_USER_ID not set — skipping chat Lighthouse."
            exit 0
          fi

          export E2E_USE_TEST_AUTH_BYPASS=1
          export NEXT_PUBLIC_CLERK_MOCK=1
          export NEXT_PUBLIC_CLERK_PROXY_DISABLED=1

          if [ ! -f .next/standalone/apps/web/server.js ]; then
            echo "Standalone server not found — chat Lighthouse cannot start."
            exit 1
          fi

          node .next/standalone/apps/web/server.js &
          SERVER_PID=$!
          trap 'kill $SERVER_PID 2>/dev/null || true' EXIT

          echo "Waiting for standalone server..."
          SERVER_READY=false
          for i in {1..30}; do
            if curl -s http://localhost:3000 > /dev/null; then
              SERVER_READY=true
              break
            fi
            sleep 1
          done

          if [ "$SERVER_READY" = "false" ]; then
            echo "Server failed to start within 30s."
            exit 1
          fi

          pnpm run test:lighthouse:chat:pr
        env:
          CI: true
          BASE_URL: http://localhost:3000
          NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY: ${{ secrets.NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY }}
          CLERK_SECRET_KEY: ${{ secrets.CLERK_SECRET_KEY }}
          DATABASE_URL: ${{ env.DATABASE_URL }}
          URL_ENCRYPTION_KEY: lighthouse-chat-ci-url-encryption-key
          AI_GATEWAY_API_KEY: lighthouse-chat-ci
          FLAGS_SECRET: ${{ secrets.FLAGS_SECRET }}
          E2E_CLERK_USER_ID: ${{ secrets.E2E_CLERK_USER_ID }}
          E2E_CLERK_USER_USERNAME: ${{ secrets.E2E_CLERK_USER_USERNAME }}
          E2E_CLERK_USER_PASSWORD: ${{ secrets.E2E_CLERK_USER_PASSWORD }}

      - name: Upload chat Lighthouse artifacts
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: lighthouse-chat-pr-${{ github.run_id }}-${{ github.run_attempt }}
          path: apps/web/.lighthouseci/chat-pr/
          if-no-files-found: ignore
          retention-days: 3

  ci-pr-neon-migrate:
    name: DB Migrate (PR main)
    needs: [neon-db, ci-path-changes]
    # Only run when drizzle/schema paths changed — skip entirely otherwise to save runners
    if: ${{ needs.ci-path-changes.outputs.skip == 'false' && (github.event_name == 'pull_request' && github.event.pull_request.base.ref == 'main' && github.event.pull_request.head.repo.fork == false && needs.ci-path-changes.outputs.run_drizzle == 'true') }}
    runs-on: ${{ vars.CI_FAST_RUNNER || 'ubuntu-latest' }}
    timeout-minutes: 12
    outputs:
      run_full_ci: ${{ steps.check_changes.outputs.run_full_ci }}
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 1

      - name: Check for relevant changes
        id: check_changes
        shell: bash
        run: echo "run_full_ci=${{ needs.ci-path-changes.outputs.run_drizzle }}" >> "$GITHUB_OUTPUT"

      - name: Skip notification
        if: steps.check_changes.outputs.run_full_ci != 'true'
        run: |
          echo "Skipping DB migrate check: no DB/schema/migration paths were changed."
          exit 0

      - name: Download Neon DB connection artifact
        if: steps.check_changes.outputs.run_full_ci == 'true'
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: neon-db-connection-${{ github.run_id }}
          path: /tmp/neon-db-connection

      - name: Resolve DATABASE_URL from Neon artifact
        id: resolve-pr-neon-db-url
        if: steps.check_changes.outputs.run_full_ci == 'true'
        uses: ./.github/actions/resolve-neon-database-url
        with:
          connection_file: /tmp/neon-db-connection/connection.json
          candidate_json_key: db_url
          fallback_json_key: db_url_pooled

      - name: Export DATABASE_URL (ephemeral branch)
        if: steps.check_changes.outputs.run_full_ci == 'true'
        run: echo "DATABASE_URL=${{ steps.resolve-pr-neon-db-url.outputs.database_url }}" >> "$GITHUB_ENV"

      - name: Fail if Neon DB URL is missing (PR migrate)
        if: steps.check_changes.outputs.run_full_ci == 'true'
        run: |
          if [[ -z "$DATABASE_URL" ]]; then
            echo "No Neon ephemeral DATABASE_URL available. Refusing to run PR migrate against staging/production DBs."
            exit 1
          fi

      - uses: ./.github/actions/setup-node-pnpm
        if: steps.check_changes.outputs.run_full_ci == 'true'

      - name: Verify Neon DB connectivity (fail-fast)
        if: steps.check_changes.outputs.run_full_ci == 'true'
        run: pnpm --filter=@jovie/web exec tsx tests/e2e/verify-neon-db-connectivity.ts
        env:
          DATABASE_URL: ${{ env.DATABASE_URL }}

      - name: Run migrations (ephemeral Neon)
        if: steps.check_changes.outputs.run_full_ci == 'true'
        run: pnpm --filter=@jovie/web run drizzle:migrate:ci
        env:
          DATABASE_URL: ${{ env.DATABASE_URL }}
          NODE_ENV: test
          GIT_BRANCH: pr-${{ github.event.pull_request.number }}
          ALLOW_PROD_MIGRATIONS: 'true'

  # Post-deploy smoke test -- deterministic curl checks against production.
  # Replaces the previous Playwright-based test that was flaky due to
  # dynamic content assertions, hydration timing, and excessive retries.
  ci-public-profile-smoke:
    name: Post-Deploy Smoke (Production)
    needs: [promote-production]
    # Run after successful deploy to production
    if: ${{ always() && needs.promote-production.result == 'success' }}
    runs-on: ${{ vars.CI_FAST_RUNNER || 'ubuntu-latest' }}
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 1

      - uses: ./.github/actions/setup-node-pnpm

      - name: Wait for CDN propagation
        run: |
          echo "Waiting 30s for Vercel CDN propagation after deploy..."
          sleep 30

      - name: Verify production endpoints are healthy
        env:
          VERCEL_AUTOMATION_BYPASS_SECRET: ${{ secrets.VERCEL_AUTOMATION_BYPASS_SECRET }}
        run: |
          set -euo pipefail

          USER_AGENT="Mozilla/5.0 (compatible; JovieCI/1.0; +https://jov.ie)"
          COOKIE_JAR="$(mktemp)"
          MAX_RETRIES=5

          # Vercel Deployment Protection bypass
          BYPASS_SECRET="${VERCEL_AUTOMATION_BYPASS_SECRET:-}"
          BYPASS_ARGS=()
          if [ -n "$BYPASS_SECRET" ]; then
            BYPASS_ARGS+=(
              -H "x-vercel-protection-bypass: $BYPASS_SECRET"
              -H "x-vercel-set-bypass-cookie: true"
              -c "$COOKIE_JAR"
              -b "$COOKIE_JAR"
            )
          fi

          # Helper: curl with standard options, returns "body\nstatus_code"
          # Append a cache-busting query parameter to bypass Vercel CDN edge cache,
          # ensuring post-deploy smoke tests always hit the origin server.
          fetch() {
            local sep="?"
            [[ "$1" == *"?"* ]] && sep="&"
            curl -sS -L -A "$USER_AGENT" \
              -H "Cache-Control: no-cache, no-store, must-revalidate" \
              -H "Pragma: no-cache" \
              "${BYPASS_ARGS[@]}" -w "\n%{http_code}" "${1}${sep}_cb=$(date +%s%N)" || echo -e "\n000"
          }

          # Helper: retry a check function with exponential backoff (max 5 attempts)
          retry() {
            local label="$1"; shift
            local attempt=1
            while [ $attempt -le $MAX_RETRIES ]; do
              if "$@"; then
                return 0
              fi
              if [ $attempt -lt $MAX_RETRIES ]; then
                local delay=$((attempt * 3))
                echo "  Retry $attempt/$MAX_RETRIES for $label in ${delay}s..."
                sleep "$delay"
              fi
              attempt=$((attempt + 1))
            done
            echo "::error::$label failed after $MAX_RETRIES attempts"
            return 1
          }

          FAILURES=0

          # -- 1. Health API: JSON status=ok --
          check_health() {
            local response
            response=$(fetch "https://jov.ie/api/health")
            local status="${response##*$'\n'}"
            local body="${response%$'\n'*}"

            if [ "$status" != "200" ]; then
              echo "  Health API returned HTTP $status"
              return 1
            fi

            if ! echo "$body" | grep -q '"status":"ok"'; then
              echo "  Health API status is not ok: $body"
              return 1
            fi

            echo "  Health API: HTTP 200, status=ok"
            return 0
          }

          echo "=== 1/5 Health API ==="
          if ! retry "Health API" check_health; then
            FAILURES=$((FAILURES + 1))
          fi

          # -- 2. Homepage: HTTP 200, >1000 chars, no error strings --
          check_homepage() {
            local response
            response=$(fetch "https://jov.ie")
            local status="${response##*$'\n'}"
            local body="${response%$'\n'*}"

            if [ "$status" != "200" ]; then
              echo "  Homepage returned HTTP $status"
              return 1
            fi

            if [ ${#body} -lt 1000 ]; then
              echo "  Homepage body too short (${#body} chars)"
              return 1
            fi

            if echo "$body" | grep -qi "application error\|internal server error\|something went wrong"; then
              echo "  Homepage contains error message"
              return 1
            fi

            echo "  Homepage: HTTP 200, ${#body} chars, no errors"
            return 0
          }

          echo "=== 2/5 Homepage ==="
          if ! retry "Homepage" check_homepage; then
            FAILURES=$((FAILURES + 1))
          fi

          # -- 3. Public profile: HTTP 200, substantial content, no 404 markers --
          # Warm up the database connection before checking the profile page.
          # The health check (step 1) may use a lightweight query that doesn't
          # fully warm Neon, so hit the profile once and discard the result.
          fetch "https://jov.ie/tim" > /dev/null 2>&1
          sleep 5

          check_profile() {
            local response
            response=$(fetch "https://jov.ie/tim")
            local status="${response##*$'\n'}"
            local body="${response%$'\n'*}"

            if [ "$status" -ge 500 ] 2>/dev/null; then
              echo "  Profile /tim returned HTTP $status (server error)"
              return 1
            fi

            if [ ${#body} -lt 500 ]; then
              echo "  Profile /tim body too short (${#body} chars)"
              return 1
            fi

            if echo "$body" | grep -qi "application error\|internal server error\|something went wrong"; then
              echo "  Profile /tim contains error content"
              return 1
            fi

            # "Profile not found" with HTTP 200 is a valid app response (data issue),
            # not a code regression — warn but don't fail.
            if echo "$body" | grep -qi "profile not found\|page could not be found"; then
              echo "  Profile /tim: HTTP $status, profile not seeded (non-blocking warning)"
            else
              echo "  Profile /tim: HTTP $status, ${#body} chars, renders correctly"
            fi
            return 0
          }

          echo "=== 3/5 Public Profile ==="
          if ! retry "Public Profile" check_profile; then
            FAILURES=$((FAILURES + 1))
          fi

          # -- 4. Critical pages: /signup, /start, /pricing -- no 5xx --
          check_critical_pages() {
            for route in "/signup" "/start" "/pricing"; do
              local response
              response=$(fetch "https://jov.ie${route}")
              local status="${response##*$'\n'}"
              local body="${response%$'\n'*}"

              if [ "$status" -ge 500 ] 2>/dev/null; then
                echo "  Route ${route} returned HTTP $status (server error)"
                return 1
              fi
              if echo "$body" | grep -qi "turnstile is not configured\|auth unavailable\|clerk is not configured"; then
                echo "  Route ${route} contains front-door config error"
                return 1
              fi
              echo "  ${route}: HTTP $status"
            done
            return 0
          }

          echo "=== 4/5 Critical Pages ==="
          if ! retry "Critical Pages" check_critical_pages; then
            FAILURES=$((FAILURES + 1))
          fi

          # -- 5. Production SEO surfaces: robots.txt + sitemap.xml --
          echo "=== 5/5 Production SEO Surfaces ==="
          if ! retry "Production SEO Surfaces" bash -c 'BASE_URL=https://jov.ie pnpm --filter=@jovie/web exec tsx scripts/seo-ratchet-live.ts'; then
            FAILURES=$((FAILURES + 1))
          fi

          # -- Summary --
          echo ""
          if [ $FAILURES -gt 0 ]; then
            echo "::error::Post-deploy smoke failed: $FAILURES/5 checks failed"
            exit 1
          fi
          echo "Post-deploy smoke passed: 5/5 checks OK"

  # Post-deploy authenticated smoke test — lightweight Playwright test against production.
  # Verifies sign-in + dashboard load with real credentials after deploy.
  # Non-blocking initially — promote to blocking once stable.
  ci-post-deploy-auth-smoke:
    name: Post-Deploy Auth Smoke (Production)
    needs: [promote-production]
    if: ${{ always() && needs.promote-production.result == 'success' }}
    runs-on: ${{ vars.CI_FAST_RUNNER || 'ubuntu-latest' }}
    continue-on-error: true
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 1

      - name: Check for production auth credentials
        id: check-creds
        run: |
          if [ -z "${{ secrets.E2E_PROD_USER_EMAIL }}" ] && [ -z "${{ secrets.E2E_CLERK_USER_USERNAME }}" ]; then
            echo "skip=true" >> "$GITHUB_OUTPUT"
            echo "No production auth credentials configured — skipping"
          else
            echo "skip=false" >> "$GITHUB_OUTPUT"
          fi

      - uses: ./.github/actions/setup-node-pnpm
        if: steps.check-creds.outputs.skip != 'true'

      - name: Setup Playwright (Chromium)
        if: steps.check-creds.outputs.skip != 'true'
        uses: ./.github/actions/setup-playwright

      - name: Wait for CDN propagation
        if: steps.check-creds.outputs.skip != 'true'
        run: sleep 30

      - name: Run production auth smoke tests
        if: steps.check-creds.outputs.skip != 'true'
        run: |
          cd apps/web

          mkdir -p tests/.auth
          echo '{"cookies":[],"origins":[]}' > tests/.auth/user.json
          CI=true BASE_URL=https://jov.ie pnpm exec playwright test tests/e2e/smoke-prod-auth.spec.ts --project=chromium --reporter=line
        env:
          E2E_PROD_USER_EMAIL: ${{ secrets.E2E_PROD_USER_EMAIL }}
          E2E_PROD_USER_PASSWORD: ${{ secrets.E2E_PROD_USER_PASSWORD }}
          E2E_CLERK_USER_USERNAME: ${{ secrets.E2E_CLERK_USER_USERNAME }}
          E2E_CLERK_USER_PASSWORD: ${{ secrets.E2E_CLERK_USER_PASSWORD }}
          NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY: ${{ secrets.NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY }}
          VERCEL_AUTOMATION_BYPASS_SECRET: ${{ secrets.VERCEL_AUTOMATION_BYPASS_SECRET }}

      - name: Upload Playwright Artifacts on Failure
        if: failure() && steps.check-creds.outputs.skip != 'true'
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: post-deploy-auth-smoke-${{ github.run_id }}
          path: |
            apps/web/playwright-report/
            apps/web/test-results/
          retention-days: 3

  ci-unit-tests:
    name: Unit Tests (${{ matrix.shard }})
    needs: [ci-path-changes]
    # Runs on PRs to main, main pushes, and manual dispatch. Merge queue skips (PR-validated).
    # Parallel with typecheck/build for faster CI.
    # Path-gated: skips test execution when no test-relevant files changed.
    if: ${{ needs.ci-path-changes.outputs.skip == 'false' && ((github.event_name == 'pull_request' && github.event.pull_request.base.ref == 'main') || (github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'workflow_dispatch') }}
    # 5-shard unit tests; runner is var-switchable (CI_UNIT_RUNNER=jovie-runner
    # offloads all shards to the self-hosted pool when hosted concurrency is
    # the bottleneck). Defaults to GitHub-hosted. Shards upload no artifacts on
    # the PR path (coverage is nightly-only), so pool egress is not a concern.
    runs-on: ${{ vars.CI_UNIT_RUNNER || 'ubuntu-latest' }}
    timeout-minutes: 40
    strategy:
      fail-fast: false
      # Cap shard concurrency so multi-PR bursts don't open 5×N runners at once.
      max-parallel: 3
      matrix:
        shard: ['1/5', '2/5', '3/5', '4/5', '5/5']
    outputs:
      run_full_ci: ${{ steps.check_changes.outputs.run_full_ci }}
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 0 # Full history for vitest --changed (names via commit graph)
          filter: blob:none # historical blobs lazy-fetch; saves ~30s × 5 shards/run

      - name: Check for relevant changes
        id: check_changes
        shell: bash
        run: echo "run_full_ci=${{ needs.ci-path-changes.outputs.run_test }}" >> "$GITHUB_OUTPUT"

      # Skip remaining steps if no relevant changes
      - name: Skip notification
        if: steps.check_changes.outputs.run_full_ci != 'true'
        run: |
          echo "Skipping unit tests as no critical paths were changed."
          exit 0

      # Continue with CI steps only if relevant changes detected or full-ci label present
      - name: Setup Node.js and pnpm
        if: steps.check_changes.outputs.run_full_ci == 'true'
        uses: ./.github/actions/setup-node-pnpm

      - name: Load quarantined unit tests
        if: steps.check_changes.outputs.run_full_ci == 'true'
        id: quarantine
        run: node .github/scripts/quarantine-ledger.mjs emit-github-output

      - name: Run unit tests
        if: steps.check_changes.outputs.run_full_ci == 'true'
        env:
          TURBO_SCM_HEAD: ${{ github.sha }}
          TURBO_SCM_BASE: ${{ (github.base_ref && format('origin/{0}', github.base_ref)) || '' }}
        run: |
          # Use forked pool with limited workers to reduce CI flakiness from memory pressure
          export NODE_OPTIONS="--max-old-space-size=3072"
          VITEST_CI_FLAGS="--pool=forks --maxWorkers=3"
          # Retry budget comes from apps/web/tests/quarantine.json
          RETRY_FLAG="--retry=${{ steps.quarantine.outputs.unit_default_retries }}"
          # Use shard-specific output file so Codecov Test Analytics receives all 10 shards
          # (a shared filename causes only the last upload to be recorded)
          # Note: vitest runs from apps/web/, so path is relative to that directory
          SHARD_SLUG="${{ matrix.shard }}"
          SHARD_SLUG="${SHARD_SLUG//\//-}"
          OUTPUT_FLAG="--outputFile=test-report.${SHARD_SLUG}.junit.xml"

          if [ "${{ github.ref }}" = "refs/heads/main" ]; then
            echo "main: Running web unit suite through Turbo without coverage; nightly coverage audit uploads Codecov coverage"
            pnpm turbo test:fast --filter=@jovie/web -- $VITEST_CI_FLAGS $RETRY_FLAG --shard=${{ matrix.shard }} ${{ steps.quarantine.outputs.unit_excludes }} $OUTPUT_FLAG
          elif [ -n "${{ github.base_ref }}" ]; then
            echo "🔍 PR: Running full test suite"
            # Run all tests on PRs to catch regressions before merge
            pnpm turbo test:fast --affected -- $VITEST_CI_FLAGS $RETRY_FLAG --shard=${{ matrix.shard }} ${{ steps.quarantine.outputs.unit_excludes }} $OUTPUT_FLAG
          else
            echo "🔍 Feature branch push: Running critical + interaction tests only (no base_ref available)"
            # Push events on feature branches (claude/*, codegen-bot/*) don't have base_ref,
            # so we can't determine changed files. Run critical + interaction tests only to avoid OOM
            # from the full test suite. The PR event covers changed-file tests.
            # --passWithNoTests: some shards may have no files matching the filter
            pnpm turbo test:fast --affected -- $VITEST_CI_FLAGS $RETRY_FLAG --passWithNoTests --shard=${{ matrix.shard }} ${{ steps.quarantine.outputs.unit_excludes }} "critical|interaction" $OUTPUT_FLAG
          fi
      - name: Run quarantined unit tests (retries)
        if: steps.check_changes.outputs.run_full_ci == 'true' && steps.quarantine.outputs.has_unit == 'true'
        continue-on-error: true
        env:
          TURBO_SCM_HEAD: ${{ github.sha }}
          TURBO_SCM_BASE: ${{ (github.base_ref && format('origin/{0}', github.base_ref)) || '' }}
        run: pnpm turbo test:fast --affected -- --pool=forks --maxWorkers=2 --retry=${{ steps.quarantine.outputs.quarantine_unit_retries }} ${{ steps.quarantine.outputs.unit_files }}

      - name: Run packages/ui unit tests
        # This suite is not sharded; running it in every web-test shard was
        # identical duplicate work. Keep it in the existing required matrix.
        if: steps.check_changes.outputs.run_full_ci == 'true' && matrix.shard == '1/5'
        run: pnpm turbo test --filter=@jovie/ui

      - name: Upload test results to Codecov
        if: ${{ !cancelled() && steps.check_changes.outputs.run_full_ci == 'true' }}
        uses: codecov/test-results-action@0fa95f0e1eeaafde2c782583b36b28ad0d8c77d3 # v1
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: apps/web/test-report.*.junit.xml
          flags: unittests
          fail_ci_if_error: false
          verbose: true

  ci-a11y:
    name: A11y (axe)
    # Runs axe-core WCAG 2.1 AA audit on public routes — uses shared build artifact.
    # Runs on push to main, workflow_dispatch, and PRs touching public UI paths
    # (run_public_lighthouse gate — same paths that provision the shared Neon branch).
    # Blocking on PRs via ci-pr-ready (JOV-4060).
    needs: [ci-build-public, ci-path-changes, neon-db]
    if: ${{ needs.ci-path-changes.outputs.skip == 'false' && (((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'workflow_dispatch' || (github.event_name == 'pull_request' && github.event.pull_request.base.ref == 'main' && github.event.pull_request.head.repo.fork == false && github.event.pull_request.user.login && github.event.pull_request.user.login != 'dependabot[bot]' && needs.ci-path-changes.outputs.run_public_lighthouse == 'true')) && needs.ci-build-public.outputs.has_artifact == 'true') }}
    runs-on: ${{ vars.CI_FAST_RUNNER || 'ubuntu-latest' }}
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 1

      - name: Check for relevant changes
        id: check_changes
        shell: bash
        run: |
          # Align with ci-build-public so public QA config changes still run the blocking lane.
          RUN_BUILD="${{ needs.ci-path-changes.outputs.run_build }}"
          RUN_TEST="${{ needs.ci-path-changes.outputs.run_test }}"
          RUN_PUBLIC_LIGHTHOUSE="${{ needs.ci-path-changes.outputs.run_public_lighthouse }}"
          if [[ "$RUN_BUILD" == "true" || "$RUN_TEST" == "true" || "$RUN_PUBLIC_LIGHTHOUSE" == "true" ]]; then
            echo "run_full_ci=true" >> "$GITHUB_OUTPUT"
          else
            echo "run_full_ci=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Skip notification
        if: steps.check_changes.outputs.run_full_ci != 'true'
        run: |
          echo "Skipping a11y audit — no UI-relevant paths changed."
          exit 0

      - uses: ./.github/actions/setup-node-pnpm
        if: steps.check_changes.outputs.run_full_ci == 'true'

      - name: Download Neon DB connection artifact
        # Reuse the shared ephemeral branch from neon-db instead of creating a
        # second branch (avoids duplicate Neon branches and flaky third-party
        # action downloads on this job).
        if: steps.check_changes.outputs.run_full_ci == 'true'
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: neon-db-connection-${{ github.run_id }}
          path: /tmp/neon-db-connection

      - name: Resolve DATABASE_URL from Neon artifact
        if: steps.check_changes.outputs.run_full_ci == 'true'
        id: resolve-a11y-neon-db-url
        uses: ./.github/actions/resolve-neon-database-url
        with:
          connection_file: /tmp/neon-db-connection/connection.json
          candidate_json_key: db_url_pooled
          fallback_json_key: db_url
          credential_source_url: ${{ secrets.DATABASE_URL_MAIN }}
          credential_fallback_url: ${{ secrets.DATABASE_URL }}

      - name: Export DATABASE_URL (ephemeral branch)
        if: steps.check_changes.outputs.run_full_ci == 'true'
        run: echo "DATABASE_URL=${{ steps.resolve-a11y-neon-db-url.outputs.database_url }}" >> "$GITHUB_ENV"

      - name: Fail if Neon DB URL is missing (A11y)
        if: steps.check_changes.outputs.run_full_ci == 'true'
        run: |
          if [[ -z "$DATABASE_URL" ]]; then
            echo "No Neon ephemeral DATABASE_URL available. Refusing to run a11y against staging/production DBs."
            exit 1
          fi

      - name: Setup Playwright (Chromium)
        if: steps.check_changes.outputs.run_full_ci == 'true'
        uses: ./.github/actions/setup-playwright

      - name: Download build artifact
        id: download
        if: steps.check_changes.outputs.run_full_ci == 'true'
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: nextjs-build-${{ github.run_id }}
          path: /tmp/nextjs-build-download

      - name: Extract build artifact
        if: steps.check_changes.outputs.run_full_ci == 'true'
        run: tar -xf /tmp/nextjs-build-download/nextjs-build.tar -C apps/web

      - name: Run migrations (ephemeral Neon)
        if: steps.check_changes.outputs.run_full_ci == 'true'
        run: pnpm --filter=@jovie/web run drizzle:migrate:ci
        env:
          DATABASE_URL: ${{ env.DATABASE_URL }}
          NODE_ENV: test
          GIT_BRANCH: pr-${{ github.event.pull_request.number || github.run_id }}
          ALLOW_PROD_MIGRATIONS: 'true'

      - name: Seed public QA fixtures
        if: steps.check_changes.outputs.run_full_ci == 'true'
        run: pnpm --filter=@jovie/web run seed:test-data
        env:
          DATABASE_URL: ${{ env.DATABASE_URL }}
          NODE_ENV: test

      - name: Run axe a11y audit (public routes)
        if: steps.check_changes.outputs.run_full_ci == 'true'
        run: |
          cd apps/web

          # Create empty auth state so Playwright's auth-setup project doesn't crash.
          # The axe audit only tests public routes (SMOKE_ONLY=1) so no real auth is needed.
          mkdir -p tests/.auth
          echo '{"cookies":[],"origins":[]}' > tests/.auth/user.json

          cleanup() {
            kill "$SERVER_PID" 2>/dev/null || true
          }
          trap cleanup EXIT

          if [ ! -f .next/standalone/apps/web/server.js ]; then
            echo "Standalone server not found - public axe audit cannot start."
            exit 1
          fi

          # Keep absolute URLs on loopback (GHA HOSTNAME is the runner machine name).
          export HOSTNAME=localhost
          export NEXT_PUBLIC_APP_URL=http://localhost:3100

          PORT=3100 node .next/standalone/apps/web/server.js &
          SERVER_PID=$!

          SERVER_READY=false
          for i in $(seq 1 30); do
            if curl -sf http://localhost:3100 > /dev/null 2>&1; then
              SERVER_READY=true
              break
            fi
            sleep 1
          done

          if [ "$SERVER_READY" = "false" ]; then
            echo "Standalone server failed to start within 30s."
            exit 1
          fi

          pnpm exec playwright test tests/e2e/axe-audit.spec.ts --config=playwright.config.ts --project=chromium --reporter=line
        env:
          BASE_URL: http://localhost:3100
          SMOKE_ONLY: '1'
          CI: true
          DATABASE_URL: ${{ env.DATABASE_URL }}
          NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY: ${{ secrets.NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY }}
          URL_ENCRYPTION_KEY: a11y-public-ci-url-encryption-key
          AI_GATEWAY_API_KEY: a11y-public-ci

      - name: Upload Playwright Report on Failure
        if: failure() && steps.check_changes.outputs.run_full_ci == 'true'
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: a11y-axe-report-${{ github.run_id }}-${{ github.run_attempt }}
          path: apps/web/playwright-report/
          retention-days: 3

  # Authenticated a11y (JOV-4061): dashboard + chat axe suites that previously
  # lived only as dead local coverage. Path/event-gated and informational until
  # violations are burned down — does NOT fan into PR Ready.
  ci-a11y-authed:
    name: A11y (authenticated, informational)
    needs: [ci-build-public, ci-path-changes, neon-db]
    # Main + workflow_dispatch always (when artifact present). PRs only with
    # 'testing' label or dashboard/chat path changes — avoids Neon tax on every UI PR.
    if: ${{ needs.ci-path-changes.outputs.skip == 'false' && needs.ci-build-public.outputs.has_artifact == 'true' && (((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'workflow_dispatch') || (github.event_name == 'pull_request' && github.event.pull_request.base.ref == 'main' && github.event.pull_request.head.repo.fork == false && github.event.pull_request.user.login && github.event.pull_request.user.login != 'dependabot[bot]' && (contains(github.event.pull_request.labels.*.name, 'testing') || needs.ci-path-changes.outputs.run_dashboard_lighthouse == 'true' || needs.ci-path-changes.outputs.run_chat_lighthouse == 'true'))) }}
    continue-on-error: true
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 1

      - uses: ./.github/actions/setup-node-pnpm

      - name: Download Neon DB connection artifact
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: neon-db-connection-${{ github.run_id }}
          path: /tmp/neon-db-connection
        continue-on-error: true

      - name: Resolve DATABASE_URL from Neon artifact
        id: resolve-a11y-authed-neon-db-url
        uses: ./.github/actions/resolve-neon-database-url
        with:
          connection_file: /tmp/neon-db-connection/connection.json
          candidate_json_key: db_url_pooled
          fallback_json_key: db_url
          credential_source_url: ${{ secrets.DATABASE_URL_MAIN }}
          credential_fallback_url: ${{ secrets.DATABASE_URL }}
        continue-on-error: true

      - name: Export DATABASE_URL
        if: steps.resolve-a11y-authed-neon-db-url.outputs.database_url != ''
        run: echo "DATABASE_URL=${{ steps.resolve-a11y-authed-neon-db-url.outputs.database_url }}" >> "$GITHUB_ENV"

      - name: Skip when Neon URL missing
        if: env.DATABASE_URL == ''
        run: |
          echo "No Neon DATABASE_URL — skipping authenticated a11y (informational)."
          exit 0

      - name: Setup Playwright (Chromium)
        if: env.DATABASE_URL != ''
        uses: ./.github/actions/setup-playwright

      - name: Download build artifact
        id: download
        if: env.DATABASE_URL != ''
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: nextjs-build-${{ github.run_id }}
          path: /tmp/nextjs-build-download
        continue-on-error: true

      - name: Extract or rebuild
        if: env.DATABASE_URL != ''
        run: |
          if [ -f /tmp/nextjs-build-download/nextjs-build.tar ]; then
            tar -xf /tmp/nextjs-build-download/nextjs-build.tar -C apps/web
          else
            pnpm turbo build --filter=@jovie/web
          fi
        env:
          NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY: ${{ secrets.NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY }}
          NEXT_IGNORE_ESLINT: '1'
          NEXT_IGNORE_TYPECHECK: '1'
          NEXT_PRIVATE_SKIP_SIZE_CHECK: 'true'

      - name: Run migrations + seed
        if: env.DATABASE_URL != ''
        run: |
          pnpm --filter=@jovie/web run drizzle:migrate:ci
          pnpm --filter=@jovie/web run seed:test-data
        env:
          DATABASE_URL: ${{ env.DATABASE_URL }}
          NODE_ENV: test
          GIT_BRANCH: a11y-authed-${{ github.run_id }}
          ALLOW_PROD_MIGRATIONS: 'true'

      - name: Run authenticated a11y specs
        id: a11y-authed
        if: env.DATABASE_URL != ''
        run: |
          cd apps/web
          mkdir -p tests/.auth
          echo '{"cookies":[],"origins":[]}' > tests/.auth/user.json

          cleanup() { kill "$SERVER_PID" 2>/dev/null || true; }
          trap cleanup EXIT

          if [ ! -f .next/standalone/apps/web/server.js ]; then
            echo "::error::Standalone server not found for authenticated a11y."
            exit 1
          fi

          # Same server-side env export as mobile-overflow / e2e-smoke so
          # test-auth enter is available on the production-built standalone.
          export VERCEL_ENV=development
          export E2E_USE_TEST_AUTH_BYPASS=1
          export E2E_TEST_AUTH_PERSONA=creator-ready
          export NEXT_PUBLIC_E2E_MODE=1
          export NEXT_PUBLIC_CLERK_MOCK=1
          export NEXT_PUBLIC_CLERK_PROXY_DISABLED=1
          export HOSTNAME=localhost

          PORT=3100 node .next/standalone/apps/web/server.js &
          SERVER_PID=$!
          for i in $(seq 1 30); do
            if curl -sf http://localhost:3100 > /dev/null 2>&1; then break; fi
            sleep 1
          done

          pnpm exec playwright test \
            tests/e2e/a11y.spec.ts \
            tests/e2e/chat-axe.spec.ts \
            --config=playwright.config.ts --project=chromium --reporter=line
        env:
          BASE_URL: http://localhost:3100
          CI: true
          DATABASE_URL: ${{ env.DATABASE_URL }}
          NODE_ENV: test
          # Match ci-e2e-smoke standalone auth env (see mobile-overflow).
          VERCEL_ENV: development
          E2E_SKIP_WEB_SERVER: '1'
          E2E_USE_TEST_AUTH_BYPASS: '1'
          E2E_TEST_AUTH_PERSONA: creator-ready
          NEXT_PUBLIC_E2E_MODE: '1'
          NEXT_PUBLIC_CLERK_MOCK: '1'
          NEXT_PUBLIC_CLERK_PROXY_DISABLED: '1'
          NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY: ${{ secrets.NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY }}
          URL_ENCRYPTION_KEY: a11y-authed-ci-url-encryption-key
          AI_GATEWAY_API_KEY: a11y-authed-ci

      - name: Upload Playwright Report on Failure
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: a11y-authed-report-${{ github.run_id }}-${{ github.run_attempt }}
          path: apps/web/playwright-report/
          retention-days: 7

  ci-e2e-smoke:
    name: E2E Smoke (PR Fast Feedback)
    environment: Preview – jovie
    # Gated: runs with 'testing' label or deterministic high-risk classification.
    # Full E2E runs on merge to main anyway.
    # Dependabot PRs cannot access NEON_API_KEY or other Preview-environment secrets.
    needs: [ci-path-changes, ci-risk-classifier, ci-build-public, neon-db]
    if: ${{ needs.ci-path-changes.outputs.skip == 'false' && ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'workflow_dispatch' || (github.event_name == 'pull_request' && github.event.pull_request.base.ref == 'main' && github.event.pull_request.head.repo.fork == false && github.event.pull_request.user.login && github.event.pull_request.user.login != 'dependabot[bot]' && needs.ci-build-public.outputs.has_artifact == 'true' && (contains(github.event.pull_request.labels.*.name, 'testing') || needs.ci-risk-classifier.outputs.requires_smoke == 'true') && (needs.neon-db.result == 'success' || needs.neon-db.result == 'skipped'))) }}
    runs-on: ${{ vars.CI_FAST_RUNNER || 'ubuntu-latest' }}
    timeout-minutes: 30
    concurrency:
      group: neon-endpoint-pool-${{ github.job }}-${{ (endsWith(format('{0}', github.event.pull_request.number || github.run_number), '0') || endsWith(format('{0}', github.event.pull_request.number || github.run_number), '4') || endsWith(format('{0}', github.event.pull_request.number || github.run_number), '8')) && '0' || (endsWith(format('{0}', github.event.pull_request.number || github.run_number), '1') || endsWith(format('{0}', github.event.pull_request.number || github.run_number), '5') || endsWith(format('{0}', github.event.pull_request.number || github.run_number), '9')) && '1' || (endsWith(format('{0}', github.event.pull_request.number || github.run_number), '2') || endsWith(format('{0}', github.event.pull_request.number || github.run_number), '6')) && '2' || '3' }}
      cancel-in-progress: false
    outputs:
      run_full_ci: ${{ steps.check_changes.outputs.run_full_ci }}
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 1

      - name: Check for relevant changes
        id: check_changes
        shell: bash
        run: echo "run_full_ci=${{ needs.ci-path-changes.outputs.run_e2e == 'true' || needs.ci-risk-classifier.outputs.requires_smoke == 'true' }}" >> "$GITHUB_OUTPUT"

      - name: Skip notification
        if: steps.check_changes.outputs.run_full_ci != 'true'
        run: |
          echo "Skipping E2E smoke; no E2E-relevant paths changed."
          exit 0

      - uses: ./.github/actions/setup-node-pnpm
        if: steps.check_changes.outputs.run_full_ci == 'true'

      - name: Download Neon DB connection artifact
        if: steps.check_changes.outputs.run_full_ci == 'true' && needs.neon-db.result == 'success'
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: neon-db-connection-${{ github.run_id }}
          path: /tmp/neon-db-connection

      - name: Resolve DATABASE_URL from Neon artifact
        if: steps.check_changes.outputs.run_full_ci == 'true' && needs.neon-db.result == 'success'
        id: resolve-e2e-smoke-neon-db-url
        uses: ./.github/actions/resolve-neon-database-url
        with:
          connection_file: /tmp/neon-db-connection/connection.json
          candidate_json_key: db_url
          fallback_json_key: db_url_pooled

      - name: Export DATABASE_URL (shared ephemeral branch)
        if: steps.check_changes.outputs.run_full_ci == 'true' && needs.neon-db.result == 'success'
        run: echo "DATABASE_URL=${{ steps.resolve-e2e-smoke-neon-db-url.outputs.database_url }}" >> "$GITHUB_ENV"

      - name: Create ephemeral Neon database branch (with retry)
        id: neon-branch
        if: steps.check_changes.outputs.run_full_ci == 'true' && needs.neon-db.result != 'success'
        uses: ./.github/actions/neon-create-branch-with-retry
        with:
          project_id: ${{ vars.NEON_PROJECT_ID }}
          branch_name: e2e-smoke-${{ github.run_id }}-${{ github.run_attempt }}
          role: neondb_owner
          api_key: ${{ secrets.NEON_API_KEY }}
          protected_branches: main,development,preview,br-main,br-production

      - name: Resolve DATABASE_URL (fallback Neon ephemeral)
        if: steps.check_changes.outputs.run_full_ci == 'true' && needs.neon-db.result != 'success'
        id: resolve-e2e-smoke-fallback-neon-db-url
        uses: ./.github/actions/resolve-neon-database-url
        with:
          candidate_url: ${{ steps.neon-branch.outputs.db_url }}
          fallback_candidate_url: ${{ steps.neon-branch.outputs.db_url_pooled }}
          credential_source_url: ${{ secrets.DATABASE_URL_MAIN }}
          credential_fallback_url: ${{ secrets.DATABASE_URL }}

      - name: Export DATABASE_URL (fallback ephemeral branch)
        if: steps.check_changes.outputs.run_full_ci == 'true' && needs.neon-db.result != 'success'
        run: echo "DATABASE_URL=${{ steps.resolve-e2e-smoke-fallback-neon-db-url.outputs.database_url }}" >> "$GITHUB_ENV"

      - name: Run migrations (ephemeral Neon)
        if: steps.check_changes.outputs.run_full_ci == 'true'
        run: pnpm --filter=@jovie/web run drizzle:migrate:ci
        env:
          DATABASE_URL: ${{ env.DATABASE_URL }}
          NODE_ENV: test
          GIT_BRANCH: pr-${{ github.event.pull_request.number }}
          ALLOW_PROD_MIGRATIONS: 'true'

      - name: Seed public QA fixtures
        if: steps.check_changes.outputs.run_full_ci == 'true'
        run: pnpm --filter=@jovie/web run seed:test-data
        env:
          DATABASE_URL: ${{ env.DATABASE_URL }}
          NODE_ENV: test

      - name: Setup Playwright (Chromium)
        if: steps.check_changes.outputs.run_full_ci == 'true'
        uses: ./.github/actions/setup-playwright

      - name: Download build artifact
        if: steps.check_changes.outputs.run_full_ci == 'true'
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: nextjs-build-${{ github.run_id }}
          path: /tmp/nextjs-build-download

      - name: Extract build artifact
        if: steps.check_changes.outputs.run_full_ci == 'true'
        run: tar -xf /tmp/nextjs-build-download/nextjs-build.tar -C apps/web

      - name: Materialize standalone assets (PR smoke)
        if: steps.check_changes.outputs.run_full_ci == 'true'
        run: |
          # Tarred .next artifacts can omit sibling paths the standalone server needs at runtime.
          cp -r apps/web/.next/static apps/web/.next/standalone/apps/web/.next/static
          cp -r apps/web/public apps/web/.next/standalone/apps/web/public

      - name: Run E2E Smoke (Chromium)
        if: steps.check_changes.outputs.run_full_ci == 'true'
        run: |
          cd apps/web

          if [ ! -f .next/standalone/apps/web/server.js ]; then
            echo "::error::Standalone server not found — ci-build-public artifact is required for PR smoke"
            exit 1
          fi

          # Standalone production server (not next dev --webpack) to avoid CI OOM/timeouts.
          # Keep env aligned with the post-merge smoke lane (ci-e2e-tests) so manifest
          # specs behave the same against the shared ci-build-public artifact.
          export DATABASE_URL="${{ env.DATABASE_URL }}"
          export NODE_ENV=test
          export CI=true
          export SMOKE_ONLY=1
          export SENTRY_E2E_REPORTING=1
          export SENTRY_DSN="${{ secrets.SENTRY_DSN }}"
          export SENTRY_ENVIRONMENT=ci-pr-e2e
          export SENTRY_RELEASE="${{ github.sha }}"
          export E2E_USE_TEST_AUTH_BYPASS=1
          export VERCEL_ENV=development
          export E2E_TEST_AUTH_PERSONA=creator-ready
          export NEXT_PUBLIC_CLERK_MOCK=1
          export NEXT_PUBLIC_CLERK_PROXY_DISABLED=1
          export NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY="${{ secrets.NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY }}"
          export NEXT_PUBLIC_TURNSTILE_SITE_KEY="1x00000000000000000000AA"
          export TURNSTILE_SECRET_KEY="1x0000000000000000000000000000000AA"
          export CLERK_SECRET_KEY="${{ secrets.CLERK_SECRET_KEY }}"
          export NEXT_PUBLIC_E2E_MODE=1
          export NEXT_DISABLE_TOOLBAR=1
          export CHAT_LLM_FAILURE_INJECTION=1
          export E2E_CLERK_USER_ID="${{ secrets.E2E_CLERK_USER_ID }}"
          export E2E_CLERK_USER_USERNAME="${{ secrets.E2E_CLERK_USER_USERNAME }}"
          export E2E_CLERK_USER_PASSWORD="${{ secrets.E2E_CLERK_USER_PASSWORD }}"
          export FLAGS_SECRET="${{ secrets.FLAGS_SECRET }}"
          export URL_ENCRYPTION_KEY="smoke-preview-url-encryption-key"
          export AI_GATEWAY_API_KEY="smoke-preview-ai-gateway-key"
          export E2E_SKIP_WARMUP=1
          export SESSION_SECRET="${{ secrets.SESSION_SECRET }}"
          # Arms the onboarding LLM-failure fallback spec; matches playwright.config.smoke.ts
          export CHAT_LLM_FAILURE_INJECTION=1
          # GHA sets HOSTNAME to the runner machine name; Next standalone uses it for
          # absolute URL construction and can rewrite redirects to 0.0.0.0 / non-loopback
          # hosts, which breaks claim flows and test-auth trusted-host checks.
          export HOSTNAME=localhost
          export NEXT_PUBLIC_APP_URL=http://localhost:3100

          PORT=3100 node .next/standalone/apps/web/server.js &
          SERVER_PID=$!
          trap 'kill $SERVER_PID 2>/dev/null || true' EXIT

          SERVER_READY=false
          for i in $(seq 1 30); do
            if curl -sf http://localhost:3100 > /dev/null 2>&1; then
              SERVER_READY=true
              break
            fi
            sleep 1
          done

          if [ "$SERVER_READY" = "false" ]; then
            echo "::error::Standalone server failed to start within 30s"
            exit 1
          fi

          cd "$GITHUB_WORKSPACE"
          CI=true BASE_URL=http://localhost:3100 pnpm --filter=@jovie/web exec playwright test --config=playwright.config.smoke.ts --project=chromium --reporter=line
        env:
          DATABASE_URL: ${{ env.DATABASE_URL }}
          NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY: ${{ secrets.NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY }}
          CLERK_SECRET_KEY: ${{ secrets.CLERK_SECRET_KEY }}
          FLAGS_SECRET: ${{ secrets.FLAGS_SECRET }}

      - name: Upload Playwright Artifacts on Failure
        if: failure() && steps.check_changes.outputs.run_full_ci == 'true'
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: e2e-smoke-report-${{ github.run_id }}-${{ github.run_attempt }}
          path: |
            apps/web/playwright-report/
            apps/web/test-results/
          retention-days: 3

      - name: Cleanup ephemeral Neon branch
        if: always() && steps.check_changes.outputs.run_full_ci == 'true' && needs.neon-db.result != 'success' && steps.neon-branch.outputs.branch_id != ''
        continue-on-error: true
        run: |
          curl -sS -X DELETE \
            -H "Authorization: Bearer ${{ secrets.NEON_API_KEY }}" \
            -H "Content-Type: application/json" \
            "https://console.neon.tech/api/v2/projects/${{ vars.NEON_PROJECT_ID }}/branches/${{ steps.neon-branch.outputs.branch_id }}"

  ci-golden-path:
    name: Golden Path (PR)
    environment: Preview – jovie
    # Launch-gate job (JOV-3758): full signup -> onboarding -> music import -> live
    # public profile -> Stripe checkout journey against REAL Clerk + an ephemeral
    # Neon branch. Required for launch-candidate PRs per docs/launch/LAUNCH_GATES.md.
    # Unlike ci-e2e-smoke this must NOT set NEXT_PUBLIC_CLERK_MOCK or
    # E2E_USE_TEST_AUTH_BYPASS — those make golden-path.spec.ts self-skip.
    # Path-triggered (not always-on): the spec has a 600s timeout, so always-on would
    # blow the CI budget and churn the Clerk dev-instance 100-user cap.
    needs: [ci-path-changes, ci-risk-classifier, ci-build-public, neon-db]
    if: ${{ needs.ci-path-changes.outputs.skip == 'false' && (github.event_name == 'workflow_dispatch' || (github.event_name == 'pull_request' && github.event.pull_request.base.ref == 'main' && github.event.pull_request.head.repo.fork == false && github.event.pull_request.user.login && github.event.pull_request.user.login != 'dependabot[bot]' && needs.ci-build-public.outputs.has_artifact == 'true' && (needs.ci-path-changes.outputs.run_golden_path == 'true' || contains(github.event.pull_request.labels.*.name, 'launch-candidate') || contains(github.event.pull_request.labels.*.name, 'testing')) && (needs.neon-db.result == 'success' || needs.neon-db.result == 'skipped'))) }}
    runs-on: ${{ vars.CI_FAST_RUNNER || 'ubuntu-latest' }}
    timeout-minutes: 25
    # Constant group so at most ONE golden-path job runs repo-wide. This serializes
    # both the Neon endpoint pool AND the Clerk dev instance: the spec's
    # purgeStaleClerkTestUsers deletes ALL gp-* users, so two concurrent runs would
    # delete each other's sessions and flake. Do not parameterize this group.
    concurrency:
      group: neon-endpoint-pool-ci-golden-path
      cancel-in-progress: false
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 1

      - uses: ./.github/actions/setup-node-pnpm

      - name: Download Neon DB connection artifact
        if: needs.neon-db.result == 'success'
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: neon-db-connection-${{ github.run_id }}
          path: /tmp/neon-db-connection

      - name: Resolve DATABASE_URL from Neon artifact
        if: needs.neon-db.result == 'success'
        id: resolve-golden-path-neon-db-url
        uses: ./.github/actions/resolve-neon-database-url
        with:
          connection_file: /tmp/neon-db-connection/connection.json
          candidate_json_key: db_url
          fallback_json_key: db_url_pooled

      - name: Export DATABASE_URL (shared ephemeral branch)
        if: needs.neon-db.result == 'success'
        run: echo "DATABASE_URL=${{ steps.resolve-golden-path-neon-db-url.outputs.database_url }}" >> "$GITHUB_ENV"

      - name: Create ephemeral Neon database branch (with retry)
        id: neon-branch
        if: needs.neon-db.result != 'success'
        uses: ./.github/actions/neon-create-branch-with-retry
        with:
          project_id: ${{ vars.NEON_PROJECT_ID }}
          branch_name: golden-path-${{ github.run_id }}-${{ github.run_attempt }}
          role: neondb_owner
          api_key: ${{ secrets.NEON_API_KEY }}
          protected_branches: main,development,preview,br-main,br-production

      - name: Resolve DATABASE_URL (fallback Neon ephemeral)
        if: needs.neon-db.result != 'success'
        id: resolve-golden-path-fallback-neon-db-url
        uses: ./.github/actions/resolve-neon-database-url
        with:
          candidate_url: ${{ steps.neon-branch.outputs.db_url }}
          fallback_candidate_url: ${{ steps.neon-branch.outputs.db_url_pooled }}
          credential_source_url: ${{ secrets.DATABASE_URL_MAIN }}
          credential_fallback_url: ${{ secrets.DATABASE_URL }}

      - name: Export DATABASE_URL (fallback ephemeral branch)
        if: needs.neon-db.result != 'success'
        run: echo "DATABASE_URL=${{ steps.resolve-golden-path-fallback-neon-db-url.outputs.database_url }}" >> "$GITHUB_ENV"

      - name: Run migrations (ephemeral Neon)
        run: pnpm --filter=@jovie/web run drizzle:migrate:ci
        env:
          DATABASE_URL: ${{ env.DATABASE_URL }}
          NODE_ENV: test
          GIT_BRANCH: pr-${{ github.event.pull_request.number }}
          ALLOW_PROD_MIGRATIONS: 'true'

      - name: Setup Playwright (Chromium)
        uses: ./.github/actions/setup-playwright

      - name: Build real-Clerk golden-path artifact
        # NEXT_PUBLIC_* values are compile-time inputs. Do not reuse the
        # bypass artifact consumed by smoke/mobile jobs.
        run: pnpm turbo build --filter=@jovie/web --force
        env:
          NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY: ${{ secrets.NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY }}
          NEXT_IGNORE_ESLINT: '1'
          NEXT_IGNORE_TYPECHECK: '1'
          NEXT_PRIVATE_SKIP_SIZE_CHECK: 'true'

      - name: Materialize standalone assets (golden path)
        run: |
          cp -r apps/web/.next/static apps/web/.next/standalone/apps/web/.next/static
          cp -r apps/web/public apps/web/.next/standalone/apps/web/public

      - name: Run Golden Path (Chromium, Better Auth)
        run: |
          cd apps/web

          if [ ! -f .next/standalone/apps/web/server.js ]; then
            echo "::error::Standalone server not found — ci-build-public artifact is required for the golden path"
            exit 1
          fi

          # Standalone production server (not next dev) to avoid CI OOM/timeouts.
          # Real Better Auth signup: no test-auth bypass is permitted here.
          export DATABASE_URL="${{ env.DATABASE_URL }}"
          export NODE_ENV=test
          export CI=true
          export VERCEL_ENV=development
          # Enables Better Auth's deterministic OTP only for the golden path's
          # canonical +e2e test address; production hard-blocks this switch.
          export E2E_TEST_MODE=1
          export NEXT_PUBLIC_APP_URL=http://localhost:3100
          # GHA HOSTNAME is the runner machine name; pin loopback so BA redirects
          # and form origin checks stay on localhost:3100.
          export HOSTNAME=localhost
          export NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY="${{ secrets.NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY }}"
          export CLERK_SECRET_KEY="${{ secrets.CLERK_SECRET_KEY }}"
          export FLAGS_SECRET="${{ secrets.FLAGS_SECRET }}"
          export SPOTIFY_CLIENT_ID="${{ secrets.SPOTIFY_CLIENT_ID }}"
          export SPOTIFY_CLIENT_SECRET="${{ secrets.SPOTIFY_CLIENT_SECRET }}"
          export MUSICFETCH_API_TOKEN="${{ secrets.MUSICFETCH_API_TOKEN }}"
          export UPSTASH_REDIS_REST_URL="${{ secrets.UPSTASH_REDIS_REST_URL }}"
          export UPSTASH_REDIS_REST_TOKEN="${{ secrets.UPSTASH_REDIS_REST_TOKEN }}"

          PORT=3100 node .next/standalone/apps/web/server.js &
          SERVER_PID=$!
          trap 'kill $SERVER_PID 2>/dev/null || true' EXIT

          SERVER_READY=false
          for i in $(seq 1 30); do
            if curl -sf http://localhost:3100 > /dev/null 2>&1; then
              SERVER_READY=true
              break
            fi
            sleep 1
          done

          if [ "$SERVER_READY" = "false" ]; then
            echo "::error::Standalone server failed to start within 30s"
            exit 1
          fi

          cd "$GITHUB_WORKSPACE"
          BASE_URL=http://localhost:3100 E2E_SKIP_WEB_SERVER=1 pnpm --filter @jovie/web run test:e2e:golden-path:ci
        env:
          # The runner also needs the database and deterministic E2E OTP flag.
          # UPSTASH_* is optional; the spec degrades to the in-memory limiter.
          DATABASE_URL: ${{ env.DATABASE_URL }}
          E2E_TEST_MODE: '1'
          NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY: ${{ secrets.NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY }}
          CLERK_SECRET_KEY: ${{ secrets.CLERK_SECRET_KEY }}
          FLAGS_SECRET: ${{ secrets.FLAGS_SECRET }}
          SPOTIFY_CLIENT_ID: ${{ secrets.SPOTIFY_CLIENT_ID }}
          SPOTIFY_CLIENT_SECRET: ${{ secrets.SPOTIFY_CLIENT_SECRET }}
          MUSICFETCH_API_TOKEN: ${{ secrets.MUSICFETCH_API_TOKEN }}
          UPSTASH_REDIS_REST_URL: ${{ secrets.UPSTASH_REDIS_REST_URL }}
          UPSTASH_REDIS_REST_TOKEN: ${{ secrets.UPSTASH_REDIS_REST_TOKEN }}

      - name: Upload Playwright Artifacts on Failure
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: golden-path-report-${{ github.run_id }}-${{ github.run_attempt }}
          path: |
            apps/web/playwright-report/
            apps/web/test-results/
          retention-days: 3

      - name: Cleanup ephemeral Neon branch
        if: always() && needs.neon-db.result != 'success' && steps.neon-branch.outputs.branch_id != ''
        continue-on-error: true
        run: |
          curl -sS -X DELETE \
            -H "Authorization: Bearer ${{ secrets.NEON_API_KEY }}" \
            -H "Content-Type: application/json" \
            "https://console.neon.tech/api/v2/projects/${{ vars.NEON_PROJECT_ID }}/branches/${{ steps.neon-branch.outputs.branch_id }}"

  ci-admin-smoke:
    name: Admin E2E Smoke
    environment: Preview – jovie
    # Gated: only runs with 'testing' label (admin tests need auth credentials).
    # Removed push-to-main trigger — merge queue validates PRs before merge.
    needs: [ci-path-changes, neon-db, ci-build-public]
    if: ${{ needs.ci-path-changes.outputs.skip == 'false' && (github.event_name == 'pull_request' && github.event.pull_request.head.repo.fork == false && contains(github.event.pull_request.labels.*.name, 'testing')) }}
    runs-on: ${{ vars.CI_FAST_RUNNER || 'ubuntu-latest' }}
    timeout-minutes: 10
    concurrency:
      group: neon-endpoint-pool-${{ github.job }}-${{ (endsWith(format('{0}', github.event.pull_request.number || github.run_number), '0') || endsWith(format('{0}', github.event.pull_request.number || github.run_number), '4') || endsWith(format('{0}', github.event.pull_request.number || github.run_number), '8')) && '0' || (endsWith(format('{0}', github.event.pull_request.number || github.run_number), '1') || endsWith(format('{0}', github.event.pull_request.number || github.run_number), '5') || endsWith(format('{0}', github.event.pull_request.number || github.run_number), '9')) && '1' || (endsWith(format('{0}', github.event.pull_request.number || github.run_number), '2') || endsWith(format('{0}', github.event.pull_request.number || github.run_number), '6')) && '2' || '3' }}
      cancel-in-progress: false
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 1

      - name: Check for relevant changes
        id: check_changes
        shell: bash
        run: echo "run_full_ci=${{ needs.ci-path-changes.outputs.run_e2e }}" >> "$GITHUB_OUTPUT"

      - name: Skip notification
        if: steps.check_changes.outputs.run_full_ci != 'true'
        run: |
          echo "Skipping admin smoke; no E2E-relevant paths changed."
          exit 0

      - uses: ./.github/actions/setup-node-pnpm
        if: steps.check_changes.outputs.run_full_ci == 'true'

      - name: Create ephemeral Neon database branch (with retry)
        id: neon-branch
        if: steps.check_changes.outputs.run_full_ci == 'true'
        uses: ./.github/actions/neon-create-branch-with-retry
        with:
          project_id: ${{ vars.NEON_PROJECT_ID }}
          branch_name: admin-smoke-${{ github.run_id }}-${{ github.run_attempt }}
          role: neondb_owner
          api_key: ${{ secrets.NEON_API_KEY }}
          protected_branches: main,development,preview,br-main,br-production,${{ needs.neon-db.outputs.branch_name }}

      - name: Resolve DATABASE_URL (Neon ephemeral)
        if: steps.check_changes.outputs.run_full_ci == 'true'
        id: resolve-admin-neon-db-url
        uses: ./.github/actions/resolve-neon-database-url
        with:
          candidate_url: ${{ steps.neon-branch.outputs.db_url_pooled }}
          fallback_candidate_url: ${{ steps.neon-branch.outputs.db_url }}
          credential_source_url: ${{ secrets.DATABASE_URL_MAIN }}
          credential_fallback_url: ${{ secrets.DATABASE_URL }}

      - name: Export DATABASE_URL
        if: steps.check_changes.outputs.run_full_ci == 'true'
        run: echo "DATABASE_URL=${{ steps.resolve-admin-neon-db-url.outputs.database_url }}" >> "$GITHUB_ENV"

      - name: Run migrations (ephemeral Neon)
        if: steps.check_changes.outputs.run_full_ci == 'true'
        run: pnpm --filter=@jovie/web run drizzle:migrate:ci
        env:
          DATABASE_URL: ${{ env.DATABASE_URL }}
          NODE_ENV: test
          GIT_BRANCH: ${{ github.event_name == 'pull_request' && format('pr-{0}', github.event.pull_request.number) || github.ref_name }}
          ALLOW_PROD_MIGRATIONS: 'true'

      - name: Setup Playwright (Chromium)
        if: steps.check_changes.outputs.run_full_ci == 'true'
        uses: ./.github/actions/setup-playwright

      - name: Load quarantined E2E specs
        id: quarantine
        if: steps.check_changes.outputs.run_full_ci == 'true'
        run: |
          node .github/scripts/quarantine-ledger.mjs validate
          FILE="apps/web/tests/quarantine.json"
          QUARANTINED=()

          if [ -f "$FILE" ]; then
            mapfile -t QUARANTINED < <(jq -r '.entries[]? | select(.kind == "e2e") | .path' "$FILE")
          fi

          ADMIN_SPEC="apps/web/tests/e2e/dashboard-pages-health.spec.ts"

          for quarantined in "${QUARANTINED[@]}"; do
            if [[ "$quarantined" == "$ADMIN_SPEC" ]]; then
              echo "run_admin_smoke=false" >> "$GITHUB_OUTPUT"
              echo "Admin smoke spec is quarantined: $ADMIN_SPEC"
              exit 0
            fi
          done

          echo "run_admin_smoke=true" >> "$GITHUB_OUTPUT"

      - name: Run Admin Smoke Tests (Chromium)
        if: steps.check_changes.outputs.run_full_ci == 'true' && steps.quarantine.outputs.run_admin_smoke == 'true'
        env:
          DATABASE_URL: ${{ env.DATABASE_URL }}
          NODE_ENV: test
          CI: true
          NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY: ${{ secrets.NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY }}
          CLERK_SECRET_KEY: ${{ secrets.CLERK_SECRET_KEY }}
          E2E_CLERK_ADMIN_USERNAME: ${{ secrets.E2E_CLERK_ADMIN_USERNAME }}
          E2E_CLERK_ADMIN_PASSWORD: ${{ secrets.E2E_CLERK_ADMIN_PASSWORD }}
          E2E_CLERK_USER_USERNAME: ${{ secrets.E2E_CLERK_USER_USERNAME }}
          E2E_CLERK_USER_PASSWORD: ${{ secrets.E2E_CLERK_USER_PASSWORD }}
          FLAGS_SECRET: ${{ secrets.FLAGS_SECRET }}
        # dashboard-pages-health is temporarily quarantined in apps/web/tests/quarantine.json due to JOV-782 flakiness.
        run: pnpm --filter=@jovie/web exec playwright test tests/e2e/dashboard-pages-health.spec.ts --grep "Admin Pages Health Check" --project=chromium

      - name: Skip notification (quarantined)
        if: steps.check_changes.outputs.run_full_ci == 'true' && steps.quarantine.outputs.run_admin_smoke != 'true'
        run: echo "Skipping admin smoke; admin spec is quarantined."

      - name: Upload Playwright Artifacts on Failure
        if: failure() && steps.check_changes.outputs.run_full_ci == 'true'
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: admin-smoke-report-${{ github.run_id }}-${{ github.run_attempt }}
          path: |
            apps/web/playwright-report/
            apps/web/test-results/
          retention-days: 3

      - name: Cleanup ephemeral Neon branch
        if: always() && steps.check_changes.outputs.run_full_ci == 'true' && steps.neon-branch.outputs.branch_id != ''
        continue-on-error: true
        run: |
          curl -sS -X DELETE \
            -H "Authorization: Bearer ${{ secrets.NEON_API_KEY }}" \
            -H "Content-Type: application/json" \
            "https://console.neon.tech/api/v2/projects/${{ vars.NEON_PROJECT_ID }}/branches/${{ steps.neon-branch.outputs.branch_id }}"

  ci-e2e-migrate:
    name: E2E DB Migrate
    environment: Preview – jovie
    needs: [ci-path-changes, ci-build-public, neon-db]
    if: ${{ needs.ci-path-changes.outputs.skip == 'false' && (github.event_name == 'push' || (github.event_name == 'pull_request' && contains(github.event.pull_request.labels.*.name, 'testing'))) }}
    runs-on: ${{ vars.CI_FAST_RUNNER || 'ubuntu-latest' }}
    timeout-minutes: 15
    outputs:
      run_full_ci: ${{ steps.check_changes.outputs.run_full_ci }}
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 1

      - name: Check for relevant changes
        id: check_changes
        shell: bash
        run: echo "run_full_ci=${{ needs.ci-path-changes.outputs.run_e2e }}" >> "$GITHUB_OUTPUT"

      - name: Skip notification
        if: steps.check_changes.outputs.run_full_ci != 'true'
        run: |
          echo "Skipping E2E DB migrate as no critical paths were changed."
          echo "To force a full CI run, add the 'testing' label to the PR."
          exit 0

      - name: Setup Node.js and pnpm
        if: steps.check_changes.outputs.run_full_ci == 'true'
        uses: ./.github/actions/setup-node-pnpm

      - name: Download Neon DB connection artifact
        if: steps.check_changes.outputs.run_full_ci == 'true'
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: neon-db-connection-${{ github.run_id }}
          path: /tmp/neon-db-connection

      - name: Resolve DATABASE_URL from Neon artifact
        id: resolve-e2e-migrate-neon-db-url
        if: steps.check_changes.outputs.run_full_ci == 'true'
        uses: ./.github/actions/resolve-neon-database-url
        with:
          connection_file: /tmp/neon-db-connection/connection.json
          candidate_json_key: db_url
          fallback_json_key: db_url_pooled
          credential_source_url: ${{ secrets.DATABASE_URL_MAIN }}
          credential_fallback_url: ${{ secrets.DATABASE_URL }}

      - name: Export DATABASE_URL
        if: steps.check_changes.outputs.run_full_ci == 'true'
        run: echo "DATABASE_URL=${{ steps.resolve-e2e-migrate-neon-db-url.outputs.database_url }}" >> "$GITHUB_ENV"

      - name: Fail if Neon DB URL is missing (E2E)
        if: steps.check_changes.outputs.run_full_ci == 'true'
        run: |
          if [[ -z "$DATABASE_URL" ]]; then
            echo "No Neon ephemeral DATABASE_URL available. Refusing to run E2E against staging/production DBs."
            exit 1
          fi

      - name: Run migrations for E2E DB
        if: steps.check_changes.outputs.run_full_ci == 'true'
        run: pnpm --filter=@jovie/web run drizzle:migrate:ci
        env:
          DATABASE_URL: ${{ env.DATABASE_URL }}
          NODE_ENV: test
          GIT_BRANCH: ${{ github.ref_name }}
          ALLOW_PROD_MIGRATIONS: 'true'

      - name: Seed E2E DB
        if: steps.check_changes.outputs.run_full_ci == 'true'
        run: pnpm --filter=@jovie/web run seed:test-data
        env:
          DATABASE_URL: ${{ env.DATABASE_URL }}
          NODE_ENV: test
          CLERK_SECRET_KEY: ${{ secrets.CLERK_SECRET_KEY }}
          E2E_CLERK_USER_ID: ${{ secrets.E2E_CLERK_USER_ID }}
          E2E_CLERK_USER_USERNAME: ${{ secrets.E2E_CLERK_USER_USERNAME }}

  ci-e2e-tests:
    name: Full E2E (Preview)
    environment: Preview – jovie
    # Opt-in validation lane for risky PRs. Keep it off the main deploy critical path.
    needs: [ci-path-changes, ci-build-public, neon-db, ci-e2e-migrate]
    if: ${{ needs.ci-path-changes.outputs.skip == 'false' && (needs.ci-e2e-migrate.outputs.run_full_ci == 'true' && (github.event_name == 'workflow_dispatch' || (github.event_name == 'pull_request' && contains(github.event.pull_request.labels.*.name, 'testing')))) }}
    runs-on: ${{ vars.CI_FAST_RUNNER || 'ubuntu-latest' }}
    timeout-minutes: 30
    strategy:
      fail-fast: true
      # Cap E2E shard concurrency (preview env + Playwright is heavy).
      max-parallel: 2
      matrix:
        shard: [1, 2, 3, 4]
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 2

      - name: Set shard env
        run: |
          echo "SHARD_TOTAL=4" >> "$GITHUB_ENV"
          echo "SHARD_INDEX=${{ matrix.shard }}" >> "$GITHUB_ENV"

      # Continue with CI steps only if relevant changes detected or full-ci label present
      - name: Setup Node.js and pnpm
        uses: ./.github/actions/setup-node-pnpm

      - name: Download Neon DB connection artifact
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: neon-db-connection-${{ github.run_id }}
          path: /tmp/neon-db-connection

      - name: Resolve DATABASE_URL from Neon artifact
        id: resolve-e2e-neon-db-url
        uses: ./.github/actions/resolve-neon-database-url
        with:
          connection_file: /tmp/neon-db-connection/connection.json
          candidate_json_key: db_url
          fallback_json_key: db_url_pooled
          credential_source_url: ${{ secrets.DATABASE_URL_MAIN }}
          credential_fallback_url: ${{ secrets.DATABASE_URL }}

      - name: Export DATABASE_URL
        run: |
          echo "DATABASE_URL=${{ steps.resolve-e2e-neon-db-url.outputs.database_url }}" >> "$GITHUB_ENV"
          echo "RESOLVED_DB_SOURCE=neon-artifact" >> "$GITHUB_ENV"

      - name: Fail if Neon DB URL is missing (E2E)
        run: |
          if [[ -z "$DATABASE_URL" ]]; then
            echo "No Neon ephemeral DATABASE_URL available. Refusing to run E2E against staging/production DBs."
            exit 1
          fi

      - name: Setup Playwright (Chromium)
        if: ${{ (hashFiles('apps/web/tests/e2e/**') != '' || hashFiles('apps/web/tests/smoke/**') != '') }}
        uses: ./.github/actions/setup-playwright

      - name: Load quarantined E2E specs
        if: ${{ (hashFiles('apps/web/tests/e2e/**') != '' || hashFiles('apps/web/tests/smoke/**') != '') }}
        id: quarantine
        run: |
          node .github/scripts/quarantine-ledger.mjs validate
          FILE="apps/web/tests/quarantine.json"
          QUARANTINED=()

          if [ -f "$FILE" ]; then
            mapfile -t QUARANTINED < <(jq -r '.entries[]? | select(.kind == "e2e") | .path' "$FILE")
          fi

          mapfile -t ALL_SPECS < <(git ls-files "apps/web/tests/e2e/**/*.spec.ts" "apps/web/tests/smoke/**/*.spec.ts")

          ALLOWED_SPECS=()
          for spec in "${ALL_SPECS[@]}"; do
            skip=false
            for quarantined in "${QUARANTINED[@]}"; do
              if [[ "$spec" == "$quarantined" ]]; then
                skip=true
                break
              fi
            done
            if [[ "$skip" == "false" ]]; then
              ALLOWED_SPECS+=("$spec")
            fi
          done

          echo "allowed_specs=${ALLOWED_SPECS[*]}" >> "$GITHUB_OUTPUT"
          echo "quarantined_specs=${QUARANTINED[*]}" >> "$GITHUB_OUTPUT"

          if [ ${#QUARANTINED[@]} -gt 0 ]; then
            echo "has_quarantine=true" >> "$GITHUB_OUTPUT"
          else
            echo "has_quarantine=false" >> "$GITHUB_OUTPUT"
          fi

      - name: E2E Smoke (PR to Preview)
        if: ${{ github.event_name == 'pull_request' && github.ref != 'refs/heads/production' && (hashFiles('apps/web/tests/e2e/**') != '' || hashFiles('apps/web/tests/smoke/**') != '') }}
        env:
          DATABASE_URL: ${{ env.DATABASE_URL }}
          NODE_ENV: test
          E2E_SKIP_SEED: '1'
          SMOKE_ONLY: '1'
          E2E_USE_TEST_AUTH_BYPASS: '1'
          VERCEL_ENV: development
          SENTRY_E2E_REPORTING: '1'
          SENTRY_DSN: ${{ secrets.SENTRY_DSN }}
          SENTRY_ENVIRONMENT: ci-pr-e2e
          SENTRY_RELEASE: ${{ github.sha }}
          NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY: ${{ secrets.NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY }}
          CLERK_SECRET_KEY: ${{ secrets.CLERK_SECRET_KEY }}
          E2E_CLERK_USER_ID: ${{ secrets.E2E_CLERK_USER_ID }}
          E2E_CLERK_USER_USERNAME: ${{ secrets.E2E_CLERK_USER_USERNAME }}
          E2E_CLERK_USER_PASSWORD: ${{ secrets.E2E_CLERK_USER_PASSWORD }}
          FLAGS_SECRET: ${{ secrets.FLAGS_SECRET }}
        run: |
          if [ -z "${{ steps.quarantine.outputs.allowed_specs }}" ]; then
            echo "No non-quarantined E2E specs to run."
            exit 0
          fi
          # Chrome-only for PRs - faster feedback, Firefox runs on main only.
          # `e2e:smoke:tagged` runs every `-g @smoke` test across the suite,
          # sharded and quarantine-filtered. The focused PR-fast lane lives in
          # the separate `ci-e2e-smoke` job (playwright.config.smoke.ts).
          cd apps/web && pnpm e2e:smoke:tagged --trace=retain-on-failure --project=chromium --shard=${SHARD_INDEX}/${SHARD_TOTAL} ${{ steps.quarantine.outputs.allowed_specs }}

      - name: E2E Auth Smoke (PR Gate)
        if: ${{ matrix.shard == 1 && github.event_name == 'pull_request' && github.ref != 'refs/heads/production' && hashFiles('apps/web/tests/e2e/smoke-auth.spec.ts') != '' }}
        env:
          DATABASE_URL: ${{ env.DATABASE_URL }}
          NODE_ENV: test
          E2E_SKIP_SEED: '1'
          SMOKE_ONLY: '1'
          E2E_USE_TEST_AUTH_BYPASS: '1'
          E2E_TEST_AUTH_PERSONA: creator-ready
          NEXT_PUBLIC_CLERK_MOCK: '1'
          NEXT_PUBLIC_CLERK_PROXY_DISABLED: '1'
          VERCEL_ENV: development
          SENTRY_E2E_REPORTING: '1'
          SENTRY_DSN: ${{ secrets.SENTRY_DSN }}
          SENTRY_ENVIRONMENT: ci-pr-e2e-auth
          SENTRY_RELEASE: ${{ github.sha }}
          # Auth smoke requires real Clerk keys in CI
          NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY: ${{ secrets.NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY }}
          CLERK_SECRET_KEY: ${{ secrets.CLERK_SECRET_KEY }}
          E2E_CLERK_USER_ID: ${{ secrets.E2E_CLERK_USER_ID }}
          # Auth smoke signs in a configured E2E user
          E2E_CLERK_USER_USERNAME: ${{ secrets.E2E_CLERK_USER_USERNAME }}
          E2E_CLERK_USER_PASSWORD: ${{ secrets.E2E_CLERK_USER_PASSWORD }}
          # Optional: enable deterministic spotify-only UI check
          FLAGS_SECRET: ${{ secrets.FLAGS_SECRET }}
        run: |
          cd apps/web && pnpm --filter=@jovie/web run e2e:auth-smoke --project=chromium

      - name: E2E Full (Main Branch)
        if: ${{ github.ref == 'refs/heads/main' && (hashFiles('apps/web/tests/e2e/**') != '' || hashFiles('apps/web/tests/smoke/**') != '') }}
        env:
          DATABASE_URL: ${{ env.DATABASE_URL }}
          NODE_ENV: test
          E2E_SKIP_SEED: '1'
          SENTRY_E2E_REPORTING: '1'
          SENTRY_DSN: ${{ secrets.SENTRY_DSN }}
          SENTRY_ENVIRONMENT: ci-main-e2e
          SENTRY_RELEASE: ${{ github.sha }}
          NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY: ${{ secrets.NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY }}
          CLERK_PUBLISHABLE_KEY: ${{ secrets.NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY }}
          CLERK_SECRET_KEY: ${{ secrets.CLERK_SECRET_KEY }}
          E2E_USE_TEST_AUTH_BYPASS: '1'
          E2E_CLERK_USER_ID: ${{ secrets.E2E_CLERK_USER_ID }}
          E2E_CLERK_USER_USERNAME: ${{ secrets.E2E_CLERK_USER_USERNAME }}
          E2E_CLERK_USER_PASSWORD: ${{ secrets.E2E_CLERK_USER_PASSWORD }}
          FLAGS_SECRET: ${{ secrets.FLAGS_SECRET }}
        run: |
          if [ -z "${{ steps.quarantine.outputs.allowed_specs }}" ]; then
            echo "No non-quarantined E2E specs to run."
            exit 0
          fi
          cd apps/web && pnpm playwright test --shard=${SHARD_INDEX}/${SHARD_TOTAL} ${{ steps.quarantine.outputs.allowed_specs }}

      - name: E2E Quarantine (retries)
        if: ${{ matrix.shard == 1 && steps.quarantine.outputs.has_quarantine == 'true' }}
        continue-on-error: true
        env:
          DATABASE_URL: ${{ env.DATABASE_URL }}
          NODE_ENV: test
          E2E_SKIP_SEED: '1'
          NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY: ${{ secrets.NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY }}
          CLERK_PUBLISHABLE_KEY: ${{ secrets.NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY }}
          CLERK_SECRET_KEY: ${{ secrets.CLERK_SECRET_KEY }}
          E2E_CLERK_USER_USERNAME: ${{ secrets.E2E_CLERK_USER_USERNAME }}
          E2E_CLERK_USER_PASSWORD: ${{ secrets.E2E_CLERK_USER_PASSWORD }}
          FLAGS_SECRET: ${{ secrets.FLAGS_SECRET }}
          SENTRY_E2E_REPORTING: '1'
          SENTRY_DSN: ${{ secrets.SENTRY_DSN }}
          SENTRY_ENVIRONMENT: ci-main-e2e-quarantine
          SENTRY_RELEASE: ${{ github.sha }}
        run: |
          RETRIES=$(jq -r '.retryBudget.quarantineE2eRetries // 2' apps/web/tests/quarantine.json)
          cd apps/web && pnpm playwright test --retries="$RETRIES" ${{ steps.quarantine.outputs.quarantined_specs }}

      - name: Upload Playwright Report on Failure
        if: ${{ failure() && (hashFiles('apps/web/tests/e2e/**') != '' || hashFiles('apps/web/tests/smoke/**') != '') }}
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: ${{ github.job }}-shard-${{ matrix.shard }}-playwright-report-${{ github.run_id }}-${{ github.run_attempt }}
          path: apps/web/playwright-report/
          retention-days: 7

      - name: Upload Playwright Test Results on Failure
        if: ${{ failure() && (hashFiles('apps/web/tests/e2e/**') != '' || hashFiles('apps/web/tests/smoke/**') != '') }}
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: ${{ github.job }}-shard-${{ matrix.shard }}-test-results-${{ github.run_id }}-${{ github.run_attempt }}
          path: apps/web/test-results/
          retention-days: 7

  ci-test-performance:
    name: Test Performance Budgets
    needs: [ci-path-changes]
    # Post-merge only: takes ~6.5 min and is not a merge gate.
    # Running on PRs adds wall time without blocking merge.
    if: ${{ github.event_name == 'push' && github.ref == 'refs/heads/main' }}
    runs-on: ${{ vars.CI_FAST_RUNNER || 'ubuntu-latest' }}
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 1

      - name: Check for relevant changes
        id: check_changes
        shell: bash
        run: echo "run_full_ci=${{ needs.ci-path-changes.outputs.run_test }}" >> "$GITHUB_OUTPUT"

      - name: Skip notification
        if: steps.check_changes.outputs.run_full_ci != 'true'
        run: |
          echo "Skipping test performance budgets; no relevant paths changed."
          exit 0

      - uses: ./.github/actions/setup-node-pnpm
        if: steps.check_changes.outputs.run_full_ci == 'true'

      - name: Generate test performance profile
        if: steps.check_changes.outputs.run_full_ci == 'true'
        run: pnpm test:profile

      - name: Enforce test performance budgets
        if: steps.check_changes.outputs.run_full_ci == 'true'
        run: |
          TEST_PERFORMANCE_SUITE="full" TEST_PERFORMANCE_USE_BASELINE="true" pnpm test:guard -- --suite="full" --use-baseline
  ci-pr-vercel-preview:
    name: Preview Deploy (PR)
    # Preview deploys are the default QA surface for internal PRs.
    # Keep this independent from the main deploy gate so review happens before merge.
    needs: [ci-path-changes, ci-risk-classifier]
    if: ${{ needs.ci-path-changes.outputs.skip == 'false' && (github.event_name == 'pull_request' && github.event.pull_request.base.ref == 'main' && github.event.pull_request.head.repo.fork == false && github.event.pull_request.user.login && github.event.pull_request.user.login != 'dependabot[bot]' && (needs.ci-risk-classifier.outputs.requires_preview == 'true' || contains(github.event.pull_request.labels.*.name, 'testing') || contains(github.event.pull_request.labels.*.name, 'deploy-preview'))) }}
    runs-on: ${{ vars.CI_FAST_RUNNER || 'ubuntu-latest' }}
    # Build + pull env consume ~5m; Vercel prebuilt upload often needs >20m alone.
    timeout-minutes: 40
    concurrency:
      # Scope preview concurrency to a single PR/ref so unrelated PRs do not
      # evict each other's pending preview deployments from the queue.
      group: preview-deploy-${{ github.repository }}-${{ github.event.pull_request.number || github.ref_name }}
      cancel-in-progress: false
    outputs:
      deployment_url: ${{ steps.pr_deploy.outputs.deployment_url }}
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - uses: ./.github/actions/setup-node-pnpm
      - name: Pull env (preview)
        timeout-minutes: 10
        run: |
          scope_args=()
          if [ -n "${VERCEL_ORG_ID:-}" ]; then
            scope_args=(--scope "$VERCEL_ORG_ID")
          fi
          for i in {1..3}; do
            if ./node_modules/.bin/vercel pull --yes --environment=preview --token "$VERCEL_TOKEN" "${scope_args[@]}"; then
              break
            fi
            echo "Vercel pull attempt $i failed, retrying in 10s..."
            sleep 10
            if [ $i -eq 3 ]; then exit 1; fi
          done
        env:
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
      - name: Strip deprecated Turbo env overrides (preview)
        run: |
          for env_file in .vercel/.env.preview.local .vercel/.env.local; do
            if [ -f "$env_file" ]; then
              grep -v '^TURBO_REMOTE_ONLY=' "$env_file" > "${env_file}.tmp" || true
              mv "${env_file}.tmp" "$env_file"
            fi
          done
      - name: Build (PR preview)
        timeout-minutes: 10
        run: |
          scope_args=()
          if [ -n "${VERCEL_ORG_ID:-}" ]; then
            scope_args=(--scope "$VERCEL_ORG_ID")
          fi
          for i in {1..3}; do
            if env -u TURBO_REMOTE_ONLY ./node_modules/.bin/vercel build --token "$VERCEL_TOKEN" "${scope_args[@]}"; then
              break
            fi
            echo "Vercel build attempt $i failed, retrying in 10s..."
            sleep 10
            if [ $i -eq 3 ]; then exit 1; fi
          done
        env:
          COREPACK_ENABLE_DOWNLOAD_PROMPT: 0
          COREPACK_ENABLE_STRICT: 0
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
      - name: Deploy (PR preview, fast deployment for UI-only changes)
        id: pr_deploy
        timeout-minutes: 30
        run: |
          # For fast preview PRs: use main secrets (no Neon dependency)
          # Fallback hierarchy for maximum compatibility
          POOLED_URL=""
          if [ -n "$DATABASE_URL_MAIN" ]; then
            POOLED_URL="$DATABASE_URL_MAIN"
            echo "Using main database URL"
          elif [ -n "$DATABASE_URL" ]; then
            POOLED_URL="$DATABASE_URL"
            echo "Using fallback database URL"
          else
            # Provide cheap stub for UI-only deployments that might not need DB
            POOLED_URL="postgresql://stub:***@localhost:5432/main_stub"
            echo "Using stub database URL for UI-only deployment"
          fi

          for i in {1..3}; do
            if bash .github/scripts/vercel-prebuilt-deploy.sh deployment_url \
              --env DATABASE_URL="$POOLED_URL" \
              --env GIT_BRANCH="${GIT_BRANCH}"; then
              break
            fi
            echo "Vercel deploy attempt $i failed, retrying in 10s..."
            sleep 10
            if [ $i -eq 3 ]; then exit 1; fi
          done
        env:
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
          VERCEL_ALLOW_SOURCE_FALLBACK: '1'
          VERCEL_ENABLE_PLAIN_PREBUILT_FALLBACK: 'true'
          # Use main secrets - no Neon dependency for fast deployments
          DATABASE_URL_MAIN: ${{ secrets.DATABASE_URL_MAIN }}
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          GIT_BRANCH: ${{ github.head_ref || github.ref_name }}

      - name: Wait for PR preview readiness
        timeout-minutes: 9
        run: |
          ./node_modules/.bin/vercel inspect "${{ steps.pr_deploy.outputs.deployment_url }}" \
            --wait \
            --timeout 8m \
            --token "$VERCEL_TOKEN" \
            --scope "$VERCEL_ORG_ID"
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}

  # PR Lighthouse runs as a path-gated preview-evidence lane with stricter thresholds (0.75 perf, 0.85 a11y).
  # Original removal was due to 0.5 thresholds being too relaxed. Updated .lighthouserc.pr.json fixes this.
  # Production Lighthouse (in ci-build job) has even stricter thresholds (0.8 perf, 0.95 a11y).

  ci-pr-ready:
    name: PR Ready
    # THE single aggregate merge gate (docs/PR_FLOW.md, merge-queue-guard.mjs
    # REQUIRED_MERGE_STATUSES). Branch protection and every enqueue automation
    # (drain-pr-queue.sh, agent-pipeline, agent-landing-sweep, agent-tick,
    # pr-conflict-handler) key on this one check-run name. Individual leaves
    # still report separately for at-a-glance failure visibility; this job only
    # fans them in so the required-check set stays one stable context instead
    # of N shard-named contexts that drift (removing it in #13610 left main
    # requiring only Migration Guard + Fork PR Gate + PR Size Guard and let
    # red-unit-test PRs merge — do not remove without updating the ruleset AND
    # all five automation consumers in the same PR).
    needs:
      [
        ci-path-changes,
        ci-risk-classifier,
        ci-fast,
        ci-unit-tests,
        ci-build-public,
        ci-pr-vercel-preview,
        ci-a11y,
        ci-layout-guard,
      ]
    # always() so we still evaluate when a leaf fails. !cancelled() avoids a
    # second red PR Ready when the whole run is concurrency-cancelled mid-DAG
    # (Path Changes cancel → leaves SKIPPED → risk "skipped" noise).
    if: ${{ always() && !cancelled() && needs.ci-path-changes.outputs.skip == 'false' && github.event_name == 'pull_request' && github.event.pull_request.base.ref == 'main' }}
    runs-on: ${{ vars.CI_GATE_RUNNER || 'ubuntu-latest' }}
    timeout-minutes: 2
    steps:
      - name: Evaluate required PR checks
        run: |
          PATH_RESULT="${{ needs.ci-path-changes.result }}"
          FAST_RESULT="${{ needs.ci-fast.result }}"
          UNIT_RESULT="${{ needs.ci-unit-tests.result }}"
          BUILD_RESULT="${{ needs.ci-build-public.result }}"
          A11Y_RESULT="${{ needs.ci-a11y.result }}"
          LAYOUT_GUARD_RESULT="${{ needs.ci-layout-guard.result }}"
          RISK_RESULT="${{ needs.ci-risk-classifier.result }}"
          RISK_LEVEL="${{ needs.ci-risk-classifier.outputs.risk_level }}"
          RISK_REQUIRES_PREVIEW="${{ needs.ci-risk-classifier.outputs.requires_preview }}"
          RISK_BLOCKS_UNATTENDED="${{ needs.ci-risk-classifier.outputs.blocks_unattended_auto_merge }}"
          RISK_RULES="${{ needs.ci-risk-classifier.outputs.matched_rule_ids }}"
          PREVIEW_RESULT="${{ needs.ci-pr-vercel-preview.result }}"
          IS_DEPENDABOT="${{ github.event.pull_request.user.login == 'dependabot[bot]' }}"
          HAS_CODE_CHANGES="${{ needs.ci-path-changes.outputs.has_code_changes }}"
          echo "ci-path-changes: $PATH_RESULT (has_code_changes=$HAS_CODE_CHANGES)"
          echo "ci-fast: $FAST_RESULT"
          echo "ci-unit-tests: $UNIT_RESULT"
          echo "ci-build-public: $BUILD_RESULT"
          echo "ci-a11y: $A11Y_RESULT"
          echo "ci-layout-guard: $LAYOUT_GUARD_RESULT"
          echo "ci-risk-classifier: $RISK_RESULT ($RISK_LEVEL; rules: ${RISK_RULES:-none})"
          echo "ci-pr-vercel-preview: $PREVIEW_RESULT"

          {
            echo "### PR Ready"
            echo
            echo "| Gate | Result | Next local command |"
            echo "| --- | --- | --- |"
            echo "| Path Changes | $PATH_RESULT | re-run workflow if cancelled/failed |"
            echo "| ci-fast (typecheck/biome/eslint/guardrails/structural) | $FAST_RESULT | \`pnpm run typecheck && pnpm run biome:check\` |"
            echo "| Unit Tests | $UNIT_RESULT | \`pnpm --filter=@jovie/web run test:fast\` |"
            echo "| Build (public routes) | $BUILD_RESULT | \`pnpm run build:web\` |"
            echo "| A11y (axe) | $A11Y_RESULT | \`pnpm exec playwright test tests/e2e/axe-audit.spec.ts --project=chromium\` |"
            echo "| Layout Guard | $LAYOUT_GUARD_RESULT | \`pnpm exec playwright test tests/e2e/layout-overlap-guard.spec.ts --project=chromium\` |"
            echo "| Risk classifier | $RISK_RESULT | \`pnpm ci:harness:check\` |"
            echo "| Preview Deploy | $PREVIEW_RESULT | \`pnpm run build:web\` |"
            echo
            echo "Risk level: ${RISK_LEVEL:-low}; matched rules: ${RISK_RULES:-none}"
          } >> "$GITHUB_STEP_SUMMARY"

          # Fail closed first when the path DAG root never completed — leaves
          # (including risk classifier) are SKIPPED in that case, which must not
          # look like a docs-only green path.
          if [[ "$PATH_RESULT" != "success" ]]; then
            echo "❌ Path Changes did not succeed (result: $PATH_RESULT)"
            echo "Leaf gates (CI Risk Classifier, ci-fast, Unit Tests, …) were skipped because the path DAG root did not finish."
            echo "Likely fix: re-run the failed jobs / workflow. Path Changes is hosted ubuntu-latest to avoid self-hosted cancel cascades."
            exit 1
          fi

          if [[ "$RISK_RESULT" != "success" ]]; then
            echo "❌ CI risk classifier did not pass (result: $RISK_RESULT)"
            echo "Likely fix: run pnpm ci:harness:check and inspect .github/ci-harness/manifest.json."
            exit 1
          fi

          # Code PRs must run ci-fast (not skip). Docs-only may skip when
          # has_code_changes=false from a successful Path Changes detect.
          if [[ "$HAS_CODE_CHANGES" == "true" ]]; then
            if [[ "$FAST_RESULT" != "success" ]]; then
              echo "❌ ci-fast did not pass (result: $FAST_RESULT) on a code-changing PR"
              echo "Likely fix: pnpm run typecheck && pnpm run biome:check (see ci-fast step summary for the failing lane)."
              exit 1
            fi
          elif [[ "$FAST_RESULT" != "success" && "$FAST_RESULT" != "skipped" ]]; then
            echo "❌ ci-fast did not pass (result: $FAST_RESULT)"
            echo "Likely fix: pnpm run typecheck && pnpm run biome:check (see ci-fast step summary for the failing lane)."
            exit 1
          fi

          # Unit tests must succeed (or be skipped when path-gated with no test files changed)
          if [[ "$UNIT_RESULT" != "success" && "$UNIT_RESULT" != "skipped" ]]; then
            echo "❌ Unit tests did not pass (result: $UNIT_RESULT)"
            echo "Likely fix: run the failing Vitest file locally or pnpm --filter=@jovie/web run test:fast."
            exit 1
          fi

          # A11y (axe) must succeed when it runs (skipped when no public UI paths changed).
          if [[ "$A11Y_RESULT" != "success" && "$A11Y_RESULT" != "skipped" ]]; then
            echo "❌ A11y (axe) audit did not pass (result: $A11Y_RESULT)"
            echo "Likely fix: download the a11y-axe-report artifact from this run and fix the WCAG violations it lists."
            exit 1
          fi

          # Layout Guard must succeed when it runs (skipped when the shared build artifact is absent).
          if [[ "$LAYOUT_GUARD_RESULT" != "success" && "$LAYOUT_GUARD_RESULT" != "skipped" ]]; then
            echo "❌ Layout Guard did not pass (result: $LAYOUT_GUARD_RESULT)"
            echo "Likely fix: run pnpm exec playwright test tests/e2e/layout-overlap-guard.spec.ts locally and fix overlapping/touching elements."
            exit 1
          fi

          # Build is non-blocking (throughput philosophy): corrections are cheap, waiting is expensive.
          # Build correctness is also validated by Vercel preview deploy + post-merge push CI.
          if [[ "$BUILD_RESULT" != "success" && "$BUILD_RESULT" != "skipped" ]]; then
            echo "::warning::ci-build-public did not pass (result: $BUILD_RESULT) — non-blocking per throughput policy"
            echo "Build failure will be corrected post-merge or in a follow-up PR."
          fi

          if [[ "$RISK_REQUIRES_PREVIEW" == "true" && "$PREVIEW_RESULT" != "success" && "$IS_DEPENDABOT" != "true" ]]; then
            echo "❌ Risk classifier requires preview evidence, but Preview Deploy result was $PREVIEW_RESULT"
            echo "Likely fix: inspect the Preview Deploy logs, then run pnpm run build:web locally."
            exit 1
          fi
          if [[ "$IS_DEPENDABOT" == "true" && "$PREVIEW_RESULT" == "skipped" ]]; then
            echo "Dependabot PR: preview deploy intentionally skipped — treating as waived."
          fi

          if [[ "$RISK_BLOCKS_UNATTENDED" == "true" ]]; then
            echo "Risk classifier blocks unattended auto-merge for this PR. Human review may still merge after required evidence is present."
          fi

          echo "All required PR checks passed: lint, typecheck, guardrails, unit tests, a11y, layout guard, build"

  ci-summary:
    name: PR Summary
    # Informational only (posts a PR comment).
    # where the comment target is a synthetic batch PR, not the source PR a human reviews.
    if: ${{ needs.ci-path-changes.outputs.skip == 'false' && (always() && github.event_name == 'pull_request') }}
    permissions:
      actions: read
      pull-requests: write
      contents: read
    needs:
      - ci-path-changes
      - ci-env-example-guard
      - ci-secret-scan
      - ci-fast
      - ci-pr-ready
      - ci-risk-classifier
      - neon-db
      - drizzle-migration-guard
      - ci-drizzle-check
      - ci-e2e-smoke
      - ci-pr-neon-migrate
      - ci-unit-tests
      - ci-test-performance
      - ci-e2e-migrate
      - ci-e2e-tests
      - ci-pr-vercel-preview
      - ci-build-public
      - ci-layout-guard
      - ci-lighthouse-pr
      - ci-lighthouse-dashboard-pr
      - ci-lighthouse-onboarding-pr
      - ci-lighthouse-admin-pr
      - ci-a11y
    runs-on: ${{ vars.CI_FAST_RUNNER }}
    timeout-minutes: 5
    steps:
      - name: Reset workspace git state (self-hosted)
        if: runner.environment != 'github-hosted'
        shell: bash
        run: |
          set -euo pipefail
          if [[ ! -d .git ]]; then exit 0; fi
          git sparse-checkout disable 2>/dev/null || true
          git config --local --unset-all core.sparseCheckout 2>/dev/null || true
          git config --local --unset-all core.sparseCheckoutCone 2>/dev/null || true
          rm -f .git/info/sparse-checkout
          git config --local --unset-all extensions.partialClone 2>/dev/null || true
          git config --local --unset-all remote.origin.promisorRemote 2>/dev/null || true

      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 1
          clean: true

      - name: Materialize full tree (self-hosted)
        if: runner.environment != 'github-hosted'
        shell: bash
        run: |
          set -euo pipefail
          git sparse-checkout disable 2>/dev/null || true
          git reset --hard HEAD
          git checkout-index -a -f

      - name: Verify checkout sentinel
        shell: bash
        run: |
          set -euo pipefail
          sentinel=".github/actions/setup-node-pnpm/action.yml"
          if [[ ! -f "$sentinel" ]]; then
            echo "::error::Incomplete checkout — missing ${sentinel}"
            exit 1
          fi

      - uses: ./.github/actions/setup-node-pnpm
      - name: Build CI harness artifact
        env:
          PR_NUMBER: ${{ github.event.pull_request.number }}
          PR_HEAD_SHA: ${{ github.event.pull_request.head.sha }}
          PR_PREVIEW_URL: ${{ needs.ci-pr-vercel-preview.outputs.deployment_url }}
          RISK_LEVEL: ${{ needs.ci-risk-classifier.outputs.risk_level }}
          RISK_REQUIRES_SMOKE: ${{ needs.ci-risk-classifier.outputs.requires_smoke }}
          RISK_REQUIRES_PREVIEW: ${{ needs.ci-risk-classifier.outputs.requires_preview }}
          RISK_BLOCKS_UNATTENDED: ${{ needs.ci-risk-classifier.outputs.blocks_unattended_auto_merge }}
          RISK_RULES: ${{ needs.ci-risk-classifier.outputs.matched_rule_ids }}
          CI_HARNESS_JOB_CI_RISK_CLASSIFIER: ${{ needs.ci-risk-classifier.result }}
          CI_HARNESS_JOB_CI_FAST: ${{ needs.ci-fast.result }}
          CI_HARNESS_JOB_CI_STRUCTURAL_CONTRACT: ${{ needs.ci-fast.result }}
          CI_HARNESS_JOB_CI_UNIT_TESTS: ${{ needs.ci-unit-tests.result }}
          CI_HARNESS_JOB_CI_BUILD_PUBLIC: ${{ needs.ci-build-public.result }}
          CI_HARNESS_JOB_CI_LIGHTHOUSE_PR: ${{ needs.ci-lighthouse-pr.result }}
          CI_HARNESS_JOB_CI_LIGHTHOUSE_DASHBOARD_PR: ${{ needs.ci-lighthouse-dashboard-pr.result }}
          CI_HARNESS_JOB_CI_LIGHTHOUSE_ONBOARDING_PR: ${{ needs.ci-lighthouse-onboarding-pr.result }}
          CI_HARNESS_JOB_CI_LIGHTHOUSE_ADMIN_PR: ${{ needs.ci-lighthouse-admin-pr.result }}
          CI_HARNESS_JOB_CI_E2E_SMOKE: ${{ needs.ci-e2e-smoke.result }}
          CI_HARNESS_JOB_CI_PR_VERCEL_PREVIEW: ${{ needs.ci-pr-vercel-preview.result }}
        run: |
          set -euo pipefail

          mkdir -p "$RUNNER_TEMP/ci-harness"
          RISK_JSON="$RUNNER_TEMP/ci-harness/risk.json"
          ARTIFACT_JSON="$RUNNER_TEMP/ci-harness/ci-harness.v1.json"

          risk_level="${RISK_LEVEL:-low}"
          requires_smoke="${RISK_REQUIRES_SMOKE:-false}"
          requires_preview="${RISK_REQUIRES_PREVIEW:-false}"
          blocks_unattended="${RISK_BLOCKS_UNATTENDED:-false}"
          risk_rules="${RISK_RULES:-}"

          jq -n \
            --arg riskLevel "$risk_level" \
            --arg rules "$risk_rules" \
            --argjson requiresSmoke "$requires_smoke" \
            --argjson requiresPreview "$requires_preview" \
            --argjson blocksUnattended "$blocks_unattended" \
            '{
              schemaVersion: 1,
              riskLevel: $riskLevel,
              requiresSmoke: $requiresSmoke,
              requiresPreview: $requiresPreview,
              blocksUnattendedAutoMerge: $blocksUnattended,
              matchedRules: ($rules | split(",") | map(select(length > 0) | {id: ., title: ., level: $riskLevel, files: []})),
              recommendedLabels: ((if $requiresSmoke then ["testing"] else [] end) + (if $blocksUnattended then ["needs-human"] else [] end))
            }' > "$RISK_JSON"

          node scripts/ci-harness.mjs emit-artifact \
            --risk "$RISK_JSON" \
            --out "$ARTIFACT_JSON"

          {
            echo "### CI harness artifact"
            echo
            echo "- Artifact: \`ci-harness.v1.json\`"
            echo "- Risk level: $risk_level"
            echo "- Preview URL: ${PR_PREVIEW_URL:-none}"
            echo
            echo "Next local command:"
            echo
            echo '```bash'
            echo "pnpm ci:harness:check"
            echo '```'
          } >> "$GITHUB_STEP_SUMMARY"
      - name: Upload CI harness artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: ci-harness-${{ github.run_id }}-${{ github.run_attempt }}
          path: ${{ runner.temp }}/ci-harness/ci-harness.v1.json
          retention-days: 14
      - name: Post CI summary to PR
        uses: actions/github-script@3a2844b7e9c422d3c10d287c895573f7108da1b3 # v9.0.0
        env:
          BUILD_RUN_FULL_CI: ${{ needs.ci-path-changes.outputs.run_build }}
          DRIZZLE_RUN_FULL_CI: ${{ needs.ci-drizzle-check.outputs.run_full_ci }}
          E2E_RUN_FULL_CI: ${{ needs.ci-e2e-migrate.outputs.run_full_ci }}
          E2E_SMOKE_RUN_FULL_CI: ${{ needs.ci-e2e-smoke.outputs.run_full_ci }}
          MIGRATION_GUARD_RUN_FULL_CI: ${{ needs.drizzle-migration-guard.outputs.run_full_ci }}
          PR_MIGRATE_RUN_FULL_CI: ${{ needs.ci-pr-neon-migrate.outputs.run_full_ci }}
          UNIT_RUN_FULL_CI: ${{ needs.ci-unit-tests.outputs.run_full_ci }}
          PR_PREVIEW_URL: ${{ needs.ci-pr-vercel-preview.outputs.deployment_url }}
          RISK_LEVEL: ${{ needs.ci-risk-classifier.outputs.risk_level }}
          RISK_REQUIRES_SMOKE: ${{ needs.ci-risk-classifier.outputs.requires_smoke }}
          RISK_REQUIRES_PREVIEW: ${{ needs.ci-risk-classifier.outputs.requires_preview }}
          RISK_BLOCKS_UNATTENDED: ${{ needs.ci-risk-classifier.outputs.blocks_unattended_auto_merge }}
          RISK_RULES: ${{ needs.ci-risk-classifier.outputs.matched_rule_ids }}
        with:
          script: |
            const { owner, repo } = context.repo;
            const prNumber = context.payload.pull_request.number;
            const runId = context.runId;

            const jobsResponse = await github.rest.actions.listJobsForWorkflowRun({
              owner,
              repo,
              run_id: runId,
              per_page: 100
            });

            const jobByName = new Map(
              jobsResponse.data.jobs.map(job => [job.name, job])
            );

            const formatDuration = (job) => {
              if (!job?.started_at || !job?.completed_at) return '—';
              const start = new Date(job.started_at);
              const end = new Date(job.completed_at);
              const ms = Math.max(0, end - start);
              const minutes = Math.floor(ms / 60000);
              const seconds = Math.floor((ms % 60000) / 1000);
              return minutes > 0 ? `${minutes}m ${seconds}s` : `${seconds}s`;
            };

            const resolveOutcome = (jobName, fallbackResult) => {
              const job = jobByName.get(jobName);
              if (!job) return { status: fallbackResult ?? 'not_run', duration: '—' };
              return {
                status: job.conclusion || job.status || fallbackResult || 'unknown',
                duration: formatDuration(job)
              };
            };

            const summarizeStatus = (status) => {
              switch (status) {
                case 'success':
                  return '✅ Passed';
                case 'failure':
                  return '❌ Failed';
                case 'cancelled':
                  return '⚠️ Cancelled';
                case 'skipped':
                  return '⚠️ Skipped';
                case 'timed_out':
                  return '❌ Timed out';
                case 'not_run':
                  return '⚪️ Not run';
                default:
                  return `⚪️ ${status}`;
              }
            };

            const deriveSkipReason = (jobKey, runFullCiFlag) => {
              if (runFullCiFlag === 'false') {
                return 'Skipped (path guard: no relevant changes)';
              }
              if (runFullCiFlag === '') {
                return 'Skipped (job not applicable)';
              }
              return jobKey === 'neon-db'
                ? 'Skipped (not required for this PR)'
                : 'Skipped (conditional job)';
            };

            const summaryRows = [
              { label: '**PR Ready (merge gate)**', jobName: 'PR Ready' },
              { label: 'CI Risk Classifier', jobName: 'CI Risk Classifier' },
              { label: 'Structural Contract', jobName: 'Structural Contract' },
              { label: 'Env Example Guard', jobName: 'Env Example Guard' },
              { label: 'Guardrails', jobName: 'Guardrails (proxy)' },
              { label: 'Biome', jobName: 'Biome (lint + format)' },
              {
                label: 'Migration Guard',
                jobName: 'Migration Guard',
                runFullCi: process.env.MIGRATION_GUARD_RUN_FULL_CI
              },
              { label: 'Neon DB', jobName: 'Neon DB' },
              {
                label: 'Drizzle Check',
                jobName: 'Drizzle Check',
                runFullCi: process.env.DRIZZLE_RUN_FULL_CI
              },
              {
                label: 'Build',
                jobName: 'Build',
                runFullCi: process.env.BUILD_RUN_FULL_CI
              },
              { label: 'Build (public routes)', jobName: 'Build (public routes)' },
              { label: 'Layout Guard', jobName: 'Layout Guard' },
              { label: 'Lighthouse (public routes PR)', jobName: 'Lighthouse (public routes PR)' },
              { label: 'Lighthouse (dashboard PR)', jobName: 'Lighthouse (dashboard PR)' },
              { label: 'Lighthouse (onboarding PR)', jobName: 'Lighthouse (onboarding PR)' },
              { label: 'Lighthouse (admin PR)', jobName: 'Lighthouse (admin PR)' },
              { label: 'A11y (axe)', jobName: 'A11y (axe)' },
              {
                label: 'Unit Tests',
                jobName: 'Unit Tests',
                runFullCi: process.env.UNIT_RUN_FULL_CI
              },
              {
                label: 'E2E Smoke',
                jobName: 'E2E Smoke (PR Fast Feedback)',
                runFullCi: process.env.E2E_SMOKE_RUN_FULL_CI
              },
              {
                label: 'Test Performance Budgets',
                jobName: 'Test Performance Budgets'
              },
              {
                label: 'E2E Tests',
                jobName: 'E2E Tests',
                runFullCi: process.env.E2E_RUN_FULL_CI
              },
              {
                label: 'DB Migrate (PR main)',
                jobName: 'DB Migrate (PR main)',
                runFullCi: process.env.PR_MIGRATE_RUN_FULL_CI
              },
              { label: 'Preview Deploy', jobName: 'Preview Deploy (PR)' }
            ];

            const rows = summaryRows.map((row) => {
              const outcome = resolveOutcome(row.jobName);
              const statusLabel = summarizeStatus(outcome.status);
              const timing = outcome.duration;
              let context = 'Executed';
              if (outcome.status === 'skipped' || outcome.status === 'not_run') {
                context = deriveSkipReason(row.label, row.runFullCi ?? '');
              }
              return `| ${row.label} | ${statusLabel} | ${context} | ${timing} |`;
            });

            // Add preview URL section if available
            let previewUrlSection = '';
            if (process.env.PR_PREVIEW_URL) {
              previewUrlSection = `\n🔗 **Preview Deployment**: ${process.env.PR_PREVIEW_URL}\n`;
            }

            const summaryBody = [
              '## CI Summary',
              '',
              'A crisp snapshot of this PR run — executed vs. skipped, with timing for every key lane.',
              '',
              `**Risk:** ${process.env.RISK_LEVEL || 'low'} (smoke: ${process.env.RISK_REQUIRES_SMOKE || 'false'}, preview: ${process.env.RISK_REQUIRES_PREVIEW || 'false'}, blocks unattended auto-merge: ${process.env.RISK_BLOCKS_UNATTENDED || 'false'})`,
              `**Matched risk rules:** ${process.env.RISK_RULES || 'none'}`,
              '**Machine artifact:** `ci-harness.v1.json` uploaded to this workflow run.',
              previewUrlSection,
              '',
              '| Job | Status | Context | Time |',
              '| --- | ------ | ------- | ---- |',
              ...rows,
              '',
              `**Run:** [${runId}](${context.serverUrl}/${owner}/${repo}/actions/runs/${runId})`,
              ''
            ].join('\n');

            const existingComments = await github.rest.issues.listComments({
              owner,
              repo,
              issue_number: prNumber
            });

            const botComment = existingComments.data.find(comment =>
              comment.user?.type === 'Bot' && comment.body?.startsWith('## CI Summary')
            );

            if (botComment) {
              await github.rest.issues.updateComment({
                owner,
                repo,
                comment_id: botComment.id,
                body: summaryBody
              });
            } else {
              await github.rest.issues.createComment({
                owner,
                repo,
                issue_number: prNumber,
                body: summaryBody
              });
            }
  # Extended smoke validation for risky changes.
  # This remains available for opt-in PR validation but does not block production deploys.
  ci-smoke-required:
    name: Extended Smoke (Preview)
    needs: [ci-path-changes, ci-build-public, neon-db, ci-e2e-tests]
    if: ${{ needs.ci-path-changes.outputs.skip == 'false' && (always() && ((github.event_name == 'workflow_dispatch' && needs.neon-db.result == 'success') || (github.event_name == 'pull_request' && github.event.pull_request.head.repo.fork == false && github.event.pull_request.user.login && github.event.pull_request.user.login != 'dependabot[bot]' && contains(github.event.pull_request.labels.*.name, 'testing') && needs.ci-path-changes.outputs.run_e2e == 'true'))) }}
    runs-on: ${{ vars.CI_FAST_RUNNER || 'ubuntu-latest' }}
    environment: Preview – jovie
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 1

      - uses: ./.github/actions/setup-node-pnpm

      - name: Download Neon DB connection artifact
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: neon-db-connection-${{ github.run_id }}
          path: /tmp/neon-db-connection

      - name: Resolve DATABASE_URL from Neon artifact
        id: resolve-smoke-neon-db-url
        uses: ./.github/actions/resolve-neon-database-url
        with:
          connection_file: /tmp/neon-db-connection/connection.json
          candidate_json_key: db_url_pooled
          fallback_json_key: db_url
          credential_source_url: ${{ secrets.DATABASE_URL_MAIN }}
          credential_fallback_url: ${{ secrets.DATABASE_URL }}

      - name: Export DATABASE_URL
        run: echo "DATABASE_URL=${{ steps.resolve-smoke-neon-db-url.outputs.database_url }}" >> "$GITHUB_ENV"

      - name: Run migrations for Smoke DB
        run: pnpm --filter=@jovie/web run drizzle:migrate:ci
        env:
          DATABASE_URL: ${{ env.DATABASE_URL }}
          NODE_ENV: test
          GIT_BRANCH: ${{ github.ref_name }}
          ALLOW_PROD_MIGRATIONS: 'true'

      - name: Setup Playwright (Chromium)
        uses: ./.github/actions/setup-playwright

      - name: Download build artifact from ci-build
        id: download-build
        if: ${{ needs.ci-path-changes.outputs.run_build == 'true' }}
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: nextjs-build-${{ github.run_id }}
          path: /tmp/nextjs-build-download
        continue-on-error: true

      - name: Extract or rebuild for smoke tests
        run: |
          if [ -f /tmp/nextjs-build-download/nextjs-build.tar ]; then
            echo "Reusing build artifact (saves ~3-4 min)"
            tar -xf /tmp/nextjs-build-download/nextjs-build.tar -C apps/web
          else
            echo "Build artifact not available, building from scratch"
            pnpm turbo build --filter=@jovie/web
          fi
          # Standalone output needs static assets alongside server.js
          cp -r apps/web/.next/static apps/web/.next/standalone/apps/web/.next/static
          cp -r apps/web/public apps/web/.next/standalone/apps/web/public
        env:
          DATABASE_URL: ${{ env.DATABASE_URL }}
          NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY: ${{ secrets.NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY }}
          # Keep rebuild fallback equivalent to the shared ci-build artifact.
          NEXT_PUBLIC_TURNSTILE_SITE_KEY: 1x00000000000000000000AA
          # NEXT_PUBLIC_* must be present at build time for standalone smoke runs.
          NEXT_PUBLIC_E2E_MODE: '1'
          NEXT_PUBLIC_CLERK_MOCK: '1'
          NEXT_PUBLIC_CLERK_PROXY_DISABLED: '1'
          NEXT_IGNORE_ESLINT: '1'
          NEXT_IGNORE_TYPECHECK: '1'
          NEXT_PRIVATE_SKIP_SIZE_CHECK: 'true'

      - name: Run Required Smoke Tests
        run: |
          cd apps/web

          # Start standalone production server (not Turbopack dev) to avoid SWC OOM on CI runners
          # IMPORTANT: Pass runtime env vars explicitly — the step's env: block only applies to
          # the shell process, but backgrounded processes inherit from the shell's exported vars.
          export DATABASE_URL="${{ env.DATABASE_URL }}"
          export NODE_ENV=test
          export VERCEL_ENV=development
          export NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY="${{ secrets.NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY }}"
          export NEXT_PUBLIC_TURNSTILE_SITE_KEY="1x00000000000000000000AA"
          export TURNSTILE_SECRET_KEY="1x0000000000000000000000000000000AA"
          export CLERK_SECRET_KEY="${{ secrets.CLERK_SECRET_KEY }}"
          export NEXT_PUBLIC_E2E_MODE=1
          export NEXT_DISABLE_TOOLBAR=1
          export NEXT_PUBLIC_CLERK_MOCK=1
          export NEXT_PUBLIC_CLERK_PROXY_DISABLED=1
          export E2E_USE_TEST_AUTH_BYPASS=1
          export E2E_TEST_AUTH_PERSONA=creator-ready
          export E2E_CLERK_USER_ID="${{ secrets.E2E_CLERK_USER_ID }}"
          export FLAGS_SECRET="${{ secrets.FLAGS_SECRET }}"
          export URL_ENCRYPTION_KEY="smoke-preview-url-encryption-key"
          export AI_GATEWAY_API_KEY="smoke-preview-ai-gateway-key"
          PORT=3100 node .next/standalone/apps/web/server.js &
          SERVER_PID=$!
          trap 'kill $SERVER_PID 2>/dev/null || true' EXIT

          # Wait for server readiness
          SERVER_READY=false
          for i in $(seq 1 30); do
            if curl -sf http://localhost:3100 > /dev/null 2>&1; then
              SERVER_READY=true
              break
            fi
            sleep 1
          done

          if [ "$SERVER_READY" = "false" ]; then
            echo "::error::Standalone server failed to start within 30s"
            exit 1
          fi

          # Run smoke tests with BASE_URL set (skips Playwright webServer config)
          cd "$GITHUB_WORKSPACE"
          set +e
          CI=true BASE_URL=http://localhost:3100 pnpm --filter=@jovie/web exec playwright test --config=playwright.config.smoke.ts --reporter=line
          EXIT_CODE=$?
          set -e
          exit $EXIT_CODE
        env:
          DATABASE_URL: ${{ env.DATABASE_URL }}
          NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY: ${{ secrets.NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY }}
          NEXT_PUBLIC_TURNSTILE_SITE_KEY: 1x00000000000000000000AA
          TURNSTILE_SECRET_KEY: 1x0000000000000000000000000000000AA
          CLERK_SECRET_KEY: ${{ secrets.CLERK_SECRET_KEY }}
          NEXT_PUBLIC_E2E_MODE: '1'
          NEXT_DISABLE_TOOLBAR: '1'
          NEXT_PUBLIC_CLERK_MOCK: '1'
          NEXT_PUBLIC_CLERK_PROXY_DISABLED: '1'
          E2E_USE_TEST_AUTH_BYPASS: '1'
          E2E_TEST_AUTH_PERSONA: creator-ready
          E2E_CLERK_USER_ID: ${{ secrets.E2E_CLERK_USER_ID }}
          E2E_CLERK_USER_USERNAME: ${{ secrets.E2E_CLERK_USER_USERNAME }}
          E2E_CLERK_USER_PASSWORD: ${{ secrets.E2E_CLERK_USER_PASSWORD }}
          FLAGS_SECRET: ${{ secrets.FLAGS_SECRET }}

      - name: Upload Playwright Artifacts on Failure
        if: ${{ failure() }}
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: smoke-required-report-${{ github.run_id }}
          path: |
            apps/web/playwright-report/
            apps/web/test-results/
          retention-days: 3

  # Homepage smoke test checks CURRENT production state (informational only).
  # NOT a deploy blocker — if prod is degraded we still need to be able to ship fixes.
  # Post-deploy verification is handled by canary-health-gate.
  ci-homepage-smoke:
    name: Homepage Smoke (Informational)
    needs: [ci-build-public]
    if: ${{ github.event_name == 'push' && github.ref == 'refs/heads/main' }}
    runs-on: ${{ vars.CI_FAST_RUNNER || 'ubuntu-latest' }}
    continue-on-error: true
    timeout-minutes: 5
    steps:
      - name: Verify homepage renders correctly
        env:
          VERCEL_AUTOMATION_BYPASS_SECRET: ${{ secrets.VERCEL_AUTOMATION_BYPASS_SECRET }}
        run: |
          echo "Running homepage smoke test against production..."

          USER_AGENT="Mozilla/5.0 (compatible; JovieCI/1.0; +https://jov.ie)"
          COOKIE_JAR="$(mktemp)"

          # Vercel Deployment Protection bypass for automation
          BYPASS_SECRET="${VERCEL_AUTOMATION_BYPASS_SECRET:-}"
          BYPASS_ARGS=()
          if [ -n "$BYPASS_SECRET" ]; then
            BYPASS_ARGS+=(
              -H "x-vercel-protection-bypass: $BYPASS_SECRET"
              -H "x-vercel-set-bypass-cookie: true"
              -c "$COOKIE_JAR"
              -b "$COOKIE_JAR"
            )
          fi

          # Check homepage
          response=$(curl -sS -L -A "$USER_AGENT" -H "Cache-Control: no-cache" "${BYPASS_ARGS[@]}" -w "\n%{http_code}" "https://jov.ie" || echo "\n000")
          status="${response##*$'\n'}"
          body="${response%$'\n'*}"

          if [ "$status" != "200" ]; then
            echo "❌ Homepage returned HTTP $status"
            exit 1
          fi

          if [ ${#body} -lt 1000 ]; then
            echo "❌ Homepage body too short (${#body} chars) - likely broken"
            exit 1
          fi

          if echo "$body" | grep -qi "application error\|internal server error\|something went wrong"; then
            echo "❌ Homepage contains error message"
            exit 1
          fi

          # Check critical pages (/sign-up, /pricing)
          for route in "/sign-up" "/pricing"; do
            route_response=$(curl -sS -L -A "$USER_AGENT" -H "Cache-Control: no-cache" "${BYPASS_ARGS[@]}" -w "\n%{http_code}" "https://jov.ie${route}" || echo "\n000")
            route_status="${route_response##*$'\n'}"

            if [ "$route_status" -ge 500 ]; then
              echo "❌ Route ${route} returned HTTP $route_status (server error)"
              exit 1
            fi
          done

          echo "✅ Homepage smoke passed"

  # Intermediate commits on main do not need to ship on their own. During
  # merge-queue bursts, N commits can land in quick succession and each one
  # would otherwise consume a full ~15 min deploy-chain slot against the
  # shared production-deploy concurrency group, starving the tip. This gate
  # runs first: only the commit that is still origin/main tip at gate time
  # proceeds to deploy-staging. Older intermediates short-circuit cleanly.
  deploy-gate:
    name: Deploy Gate (HEAD-only)
    needs: [ci-path-changes, ci-build-public]
    if: ${{ always() && github.event_name == 'push' && github.ref == 'refs/heads/main' && !failure() && !cancelled() }}
    runs-on: ubuntu-latest
    timeout-minutes: 2
    outputs:
      is_head: ${{ steps.check.outputs.is_head }}
    steps:
      - name: Check if SHA is still main HEAD
        id: check
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          tip=$(gh api "repos/${{ github.repository }}/commits/main" --jq '.sha // ""' 2>/dev/null || true)
          if [ -z "$tip" ]; then
            echo "::warning::Could not resolve origin/main tip; defaulting to proceed"
            echo "is_head=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          if [ "$tip" = "${{ github.sha }}" ]; then
            echo "::notice::SHA ${{ github.sha }} is main HEAD — proceeding with deploy"
            echo "is_head=true" >> "$GITHUB_OUTPUT"
          else
            echo "::notice::Skipping deploy — ${{ github.sha }} is behind main HEAD ($tip). The newer commit will promote."
            echo "is_head=false" >> "$GITHUB_OUTPUT"
          fi

  deploy-staging:
    # deploy-gate already needs ci-build-public (JOV-4087 vercel artifact producer).
    needs: [deploy-gate]
    # Fast main path: merge on deterministic checks, deploy a production build to
    # a preview URL, verify it on staging, then promote only if it passes.
    # Intermediate commits (not main HEAD at gate time) skip so they don't
    # saturate the production-deploy concurrency slot.
    if: ${{ always() && github.event_name == 'push' && github.ref == 'refs/heads/main' && !failure() && !cancelled() && needs.deploy-gate.outputs.is_head == 'true' }}
    runs-on: ubuntu-latest
    timeout-minutes: 40
    # Deploy concurrency: serialize main deploys instead of cancelling them.
    # Cancelling in-progress main deploys during merge bursts can starve
    # production promotion and leave main without a completed deploy run.
    concurrency:
      group: production-deploy
      cancel-in-progress: false
    permissions:
      contents: read
      actions: read
      # Required for SLSA build-provenance attestation
      attestations: write
      id-token: write
    outputs:
      migration_status: ${{ steps.migrate.outcome }}
      preflight_status: ${{ steps.preflight.outcome }}
      # Note: if deploy is cancelled before verify completes, outcome is '' (not 'failure'),
      # so no drift warning fires. This is acceptable — the cancelled deploy didn't ship.
      schema_drift: ${{ steps.verify.outcome == 'failure' }}
      deploy_url: ${{ steps.deploy.outputs.deploy_url }}
      deploy_url_b64: ${{ steps.encode_deploy_url.outputs.deploy_url_b64 }}
    environment:
      name: Staging – jovie
      url: https://staging.jov.ie
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - uses: ./.github/actions/setup-node-pnpm
      # Main deploys to staging with the isolated Doppler `stg` service token.
      # Doppler owns runtime config; GitHub owns the existing Vercel control-plane
      # credentials used only to pull, deploy, and alias the staging build.
      - uses: ./.github/actions/setup-doppler
        with:
          doppler-token: ${{ secrets.DOPPLER_TOKEN_STG }}
      - name: Configure staging deployment credentials
        shell: bash
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
        run: |
          # Write after setup-doppler so a runtime config cannot override the
          # repository-scoped deployment identity for later Vercel CLI steps.
          {
            printf 'VERCEL_TOKEN=%s\n' "$VERCEL_TOKEN"
            printf 'VERCEL_ORG_ID=%s\n' "$VERCEL_ORG_ID"
            printf 'VERCEL_PROJECT_ID=%s\n' "$VERCEL_PROJECT_ID"
          } >> "$GITHUB_ENV"
      - name: Verify staging deploy control-plane credentials
        shell: bash
        run: |
          missing=()
          for key in VERCEL_TOKEN VERCEL_ORG_ID VERCEL_PROJECT_ID; do
            if [ -z "${!key:-}" ]; then
              missing+=("$key")
            fi
          done
          if [ "${#missing[@]}" -gt 0 ]; then
            echo "::error::Missing required staging deployment credentials: ${missing[*]}"
            exit 1
          fi
      - name: Cancel stale Vercel preview deployments
        run: node .github/scripts/cancel-stale-vercel-previews.mjs
      - name: DB safety check (staging preflight)
        id: preflight
        run: pnpm --filter=@jovie/web exec tsx scripts/drizzle-migrate-preflight.ts
        env:
          GIT_BRANCH: main
          ALLOW_PROD_MIGRATIONS: 'true'
      - name: DB migrate (staging - Drizzle)
        id: migrate
        run: pnpm --filter=@jovie/web run drizzle:migrate:ci
        env:
          GIT_BRANCH: main
          ALLOW_PROD_MIGRATIONS: 'true'
        continue-on-error: false
      - name: DB verify (post-migration schema check)
        id: verify
        run: pnpm --filter=@jovie/web run drizzle:verify:ci
        # Blocks deploy by default — missing columns = broken staging.
        # To bypass in emergencies, include [skip-schema-verify] in the
        # merge commit message. The Slack drift notification in deploy-notify
        # fires when verify fails but deploy proceeds (bypass alert).
        continue-on-error: ${{ contains(github.event.head_commit.message, '[skip-schema-verify]') }}
      # JOV-4087: reuse vercel build from ci-build-public when available.
      - name: Download vercel build artifact
        id: download_vercel_build
        continue-on-error: true
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: vercel-build-output-${{ github.run_id }}
          path: /tmp

      - name: Restore vercel build output from artifact
        id: restore_vercel_build
        if: steps.download_vercel_build.outcome == 'success'
        continue-on-error: true
        run: |
          set -euo pipefail
          if [ -f /tmp/vercel-build-output.tar.gz ]; then
            mkdir -p .vercel
            tar -xzf /tmp/vercel-build-output.tar.gz -C .
            if [ -d .vercel/output ]; then
              echo "restored=true" >> "$GITHUB_OUTPUT"
              echo "Restored .vercel/output from ci-build-public artifact (JOV-4087)"
            else
              echo "restored=false" >> "$GITHUB_OUTPUT"
              echo "::warning::vercel artifact missing .vercel/output after extract"
            fi
          else
            echo "restored=false" >> "$GITHUB_OUTPUT"
            echo "::warning::vercel-build-output tar not found after download"
          fi

      - name: Pull env (staging preview)
        # Always pull project metadata/env; needed even when prebuilt output is restored
        # (deploy CLI reads .vercel/project.json).
        timeout-minutes: 10
        run: |
          scope_args=()
          if [ -n "${VERCEL_ORG_ID:-}" ]; then
            scope_args=(--scope "$VERCEL_ORG_ID")
          fi
          for i in {1..3}; do
            if ./node_modules/.bin/vercel pull --yes --environment=preview --token "$VERCEL_TOKEN" "${scope_args[@]}"; then
              break
            fi
            echo "Vercel pull attempt $i failed, retrying in 10s..."
            sleep 10
            if [ $i -eq 3 ]; then exit 1; fi
          done
      - name: Strip deprecated Turbo env overrides (staging preview)
        run: |
          for env_file in .vercel/.env.preview.local .vercel/.env.local; do
            if [ -f "$env_file" ]; then
              grep -v '^TURBO_REMOTE_ONLY=' "$env_file" > "${env_file}.tmp" || true
              mv "${env_file}.tmp" "$env_file"
            fi
          done
      - name: Check signup readiness (staging deploy env)
        run: pnpm --filter=@jovie/web exec tsx scripts/check-signup-readiness.ts --target=stg --source=env
      - name: Build (preview target for staging verification)
        # Skip when JOV-4087 artifact restored successfully.
        if: steps.restore_vercel_build.outputs.restored != 'true'
        timeout-minutes: 10
        run: |
          scope_args=()
          if [ -n "${VERCEL_ORG_ID:-}" ]; then
            scope_args=(--scope "$VERCEL_ORG_ID")
          fi
          for i in {1..3}; do
            if env -u TURBO_REMOTE_ONLY ./node_modules/.bin/vercel build --token "$VERCEL_TOKEN" "${scope_args[@]}"; then
              break
            fi
            echo "Vercel build attempt $i failed, retrying in 10s..."
            sleep 10
            if [ $i -eq 3 ]; then exit 1; fi
          done
        env:
          COREPACK_ENABLE_DOWNLOAD_PROMPT: 0
          COREPACK_ENABLE_STRICT: 0
          NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY: ${{ secrets.CLERK_PUBLISHABLE_KEY_STAGING }}
          CLERK_SECRET_KEY: ${{ secrets.CLERK_SECRET_KEY_STAGING }}
      - name: Package build output for provenance attestation
        run: |
          set -euo pipefail
          if [ -f /tmp/vercel-build-output.tar.gz ] && [ "${{ steps.restore_vercel_build.outputs.restored }}" = "true" ]; then
            echo "Using downloaded vercel-build-output.tar.gz for attestation"
          else
            tar -czf /tmp/vercel-build-output.tar.gz -C . .vercel/output
          fi
      - name: Attest build provenance (SLSA Level 2)
        id: attest
        uses: actions/attest-build-provenance@0f67c3f4856b2e3261c31976d6725780e5e4c373 # v4.1.1
        with:
          subject-path: /tmp/vercel-build-output.tar.gz
          # ponytail: attests the vercel build output; attestation is SLSA L2 (hosted
          # runner + signed provenance). L3 requires hermetic build — upgrade when
          # Vercel builds become reproducible/isolated.
      - name: Deploy (staging preview, prebuilt)
        timeout-minutes: 10
        id: deploy
        run: |
          required_runtime_env=(
            VERCEL_AUTOMATION_BYPASS_SECRET
            NEXT_PUBLIC_BETTER_AUTH_URL
            BETTER_AUTH_SECRET
            BETTER_AUTH_URL
            DATABASE_URL
            SESSION_SECRET
            AI_GATEWAY_API_KEY
            NEXT_PUBLIC_TURNSTILE_SITE_KEY
            TURNSTILE_SECRET_KEY
          )

          missing_runtime_env=()
          for key in "${required_runtime_env[@]}"; do
            if [ -z "${!key:-}" ]; then
              missing_runtime_env+=("$key")
            fi
          done

          if [ "${#missing_runtime_env[@]}" -gt 0 ]; then
            echo "Missing staging preview runtime env: ${missing_runtime_env[*]}"
            exit 1
          fi

          for i in {1..3}; do
            if bash .github/scripts/vercel-prebuilt-deploy.sh deploy_url \
              --env VERCEL_AUTOMATION_BYPASS_SECRET="${VERCEL_AUTOMATION_BYPASS_SECRET}" \
              --env NEXT_PUBLIC_BETTER_AUTH_URL="${NEXT_PUBLIC_BETTER_AUTH_URL}" \
              --env BETTER_AUTH_SECRET="${BETTER_AUTH_SECRET}" \
              --env BETTER_AUTH_URL="${BETTER_AUTH_URL}" \
              --env DATABASE_URL="${DATABASE_URL}" \
              --env SESSION_SECRET="${SESSION_SECRET}" \
              --env AI_GATEWAY_API_KEY="${AI_GATEWAY_API_KEY}" \
              --env NEXT_PUBLIC_TURNSTILE_SITE_KEY="${NEXT_PUBLIC_TURNSTILE_SITE_KEY}" \
              --env TURNSTILE_SECRET_KEY="${TURNSTILE_SECRET_KEY}"; then
              break
            fi
            echo "Vercel deploy attempt $i failed, retrying in 10s..."
            sleep 10
            if [ $i -eq 3 ]; then exit 1; fi
          done
        env:
          # The restored prebuilt artifact uploads but its function runtime is
          # not healthy. Use Vercel's source-build cache until the artifact is
          # proven runtime-closed by canary.
          VERCEL_FORCE_SOURCE_DEPLOY: 'true'
          # The closed staging artifact contains more than Vercel's 15k plain
          # upload limit. Give the archive path the full prebuilt budget.
          VERCEL_ENABLE_PLAIN_PREBUILT_FALLBACK: 'false'
          NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY: ${{ secrets.CLERK_PUBLISHABLE_KEY_STAGING }}
          CLERK_SECRET_KEY: ${{ secrets.CLERK_SECRET_KEY_STAGING }}

      - name: Wait for staging deployment readiness
        timeout-minutes: 21
        env:
          DEPLOYMENT_URL: ${{ steps.deploy.outputs.deploy_url }}
        run: |
          inspect_wait_status=0
          ./node_modules/.bin/vercel inspect "$DEPLOYMENT_URL" \
            --wait \
            --timeout 20m \
            --token "$VERCEL_TOKEN" \
            --scope "$VERCEL_ORG_ID" || inspect_wait_status=$?
          deployment_json="$(./node_modules/.bin/vercel inspect "$DEPLOYMENT_URL" \
            --format=json \
            --token "$VERCEL_TOKEN" \
            --scope "$VERCEL_ORG_ID")"
          deployment_state="$(jq -r '(.readyState // .state // .status // "unknown") | ascii_upcase' \
            <<<"$deployment_json")"
          echo "Current deployment state: $deployment_state"
          case "$deployment_state" in
            READY)
              ;;
            BUILDING|QUEUED|INITIALIZING)
              echo "::warning::Vercel is still $deployment_state after readiness wait (status $inspect_wait_status); handing off to retrying canary."
              ;;
            *)
              echo "::error::Vercel deployment entered terminal state $deployment_state."
              exit 1
              ;;
          esac

      - name: Encode deployment URL for downstream jobs
        id: encode_deploy_url
        env:
          DEPLOYMENT_URL: ${{ steps.deploy.outputs.deploy_url }}
        run: |
          printf 'deploy_url_b64=%s\n' "$(printf '%s' "$DEPLOYMENT_URL" | base64 | tr -d '\n')" \
            >> "$GITHUB_OUTPUT"

  canary-health-gate:
    name: Canary Health Gate (staging)
    needs: [deploy-staging]
    if: ${{ always() && needs.deploy-staging.result == 'success' }}
    concurrency:
      group: production-deploy
      cancel-in-progress: false
    uses: ./.github/workflows/canary-health-gate.yml
    with:
      # Verify the exact deployment produced by deploy-staging instead of the
      # shared staging alias. Merge-queue traffic can retarget staging.jov.ie
      # while this gate is still retrying, which produces false negatives even
      # when the freshly deployed preview is healthy.
      deployment_url_b64: ${{ needs.deploy-staging.outputs.deploy_url_b64 }}
      commit_sha: ${{ github.sha }}
      fallback_health_url: /api/health
    secrets:
      VERCEL_AUTOMATION_BYPASS_SECRET: ${{ secrets.VERCEL_AUTOMATION_BYPASS_SECRET }}
      VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
      VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
      VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}

  alias-staging:
    name: Alias verified preview to staging.jov.ie
    needs: [deploy-staging, canary-health-gate]
    if: ${{ always() && needs.deploy-staging.result == 'success' && needs.canary-health-gate.result == 'success' }}
    runs-on: ubuntu-latest
    timeout-minutes: 5
    concurrency:
      group: production-deploy
      cancel-in-progress: false
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - uses: ./.github/actions/setup-node-pnpm
      - name: Alias verified deployment
        env:
          DEPLOYMENT_URL_B64: ${{ needs.deploy-staging.outputs.deploy_url_b64 }}
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
        run: |
          deployment_url="$(printf '%s' "$DEPLOYMENT_URL_B64" | base64 --decode)"
          test -n "$deployment_url"
          ./node_modules/.bin/vercel alias set "$deployment_url" staging.jov.ie \
            --token "$VERCEL_TOKEN" \
            --scope "$VERCEL_ORG_ID"

  promote-production:
    name: Promote to Production
    needs: [deploy-staging, canary-health-gate, alias-staging]
    if: ${{ always() && needs.deploy-staging.result == 'success' && needs.canary-health-gate.result == 'success' && needs.alias-staging.result == 'success' }}
    runs-on: ubuntu-latest
    timeout-minutes: 10
    concurrency:
      group: production-deploy
      cancel-in-progress: false
    environment:
      name: Production – jovie
      url: https://jov.ie
    outputs:
      # Emitted when promote-production exits non-zero so deploy-notify can
      # distinguish silent-no-op regressions from generic promote failures.
      # Values: "domain_project_mismatch" (public domains are not on the
      # canonical Vercel project) | "no_match" (no Vercel deployment for
      # github.sha) | "verify_mismatch" (production keeps serving the old
      # commit after promote).
      failure_subtype: ${{ steps.domain-guard.outputs.failure_subtype || steps.promote.outputs.failure_subtype }}
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - uses: ./.github/actions/setup-node-pnpm
      - name: Verify production domains are on canonical Vercel project
        id: domain-guard
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
        run: |
          if ! node .github/scripts/verify-vercel-production-domains.mjs; then
            echo "failure_subtype=domain_project_mismatch" >> "$GITHUB_OUTPUT"
            exit 1
          fi
      - name: Promote specific deployment for this SHA
        timeout-minutes: 10
        id: promote
        env:
          GITHUB_SHA: ${{ github.sha }}
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
          VERCEL_AUTOMATION_BYPASS_SECRET: ${{ secrets.VERCEL_AUTOMATION_BYPASS_SECRET }}
        run: |
          # Resolve the exact deployment URL for github.sha via the Vercel API.
          # We deliberately do NOT promote the staging.jov.ie alias: if staging
          # drifts (silent `vercel alias set` failure, stale alias), promoting
          # the alias hits "already the current production deployment" and
          # silently no-ops. That regression kept production 7 days stale in
          # 2026-04. Looking up by SHA means the only way to silently succeed
          # is if we actually promote the right commit.
          set -euo pipefail
          echo "Resolving deployment URL for SHA ${GITHUB_SHA}..."
          deploy_url="$(python3 - <<'PY'
          import json
          import os
          import sys
          import urllib.parse
          import urllib.request

          token = os.environ.get("VERCEL_TOKEN", "")
          org_id = os.environ.get("VERCEL_ORG_ID", "")
          project_id = os.environ.get("VERCEL_PROJECT_ID", "")
          commit_sha = os.environ.get("GITHUB_SHA", "")

          # Distinguish missing-creds from genuine no-match so the shell can
          # pick a different failure subtype. Without this, an unset secret
          # silently triggers the `no_match` Slack alert.
          if not token or not project_id or not commit_sha:
              print("__MISSING_CREDS__", end="")
              sys.exit(0)

          # Paginate over READY deployments until we find the SHA or the
          # Vercel API says there are no more pages. A hard page cap prevents
          # runaway API calls on pathological account state.
          #
          # No target filter: the upstream deploy-staging job uses
          # `vercel deploy --prebuilt` (via vercel-prebuilt-deploy.sh) without
          # --prod, which creates PREVIEW deployments. Filtering by
          # target=production would never match and every promote would fail
          # with no_match. Preview-collision safety comes from (a) projectId
          # scoping the query to this project, (b) exact 40-char SHA match,
          # and (c) the `vercel promote` step itself, which only accepts URLs
          # from the same project.
          base_params = {
              "projectId": project_id,
              "state": "READY",
              "limit": 100,
          }
          if org_id.startswith("team_"):
              base_params["teamId"] = org_id

          next_cursor = None
          found = False
          for _page in range(10):  # hard cap: 1000 deployments searched
              params = dict(base_params)
              if next_cursor is not None:
                  params["until"] = next_cursor
              url = f"https://api.vercel.com/v6/deployments?{urllib.parse.urlencode(params)}"
              req = urllib.request.Request(url, headers={"Authorization": f"Bearer {token}"})
              with urllib.request.urlopen(req, timeout=15) as response:
                  payload = json.load(response)

              deployments = payload.get("deployments", [])
              for deployment in deployments:
                  meta = deployment.get("meta") or {}
                  if meta.get("githubCommitSha") == commit_sha:
                      print(f"https://{deployment['url']}")
                      found = True
                      break
              if found:
                  break

              pagination = payload.get("pagination") or {}
              next_cursor = pagination.get("next")
              if not next_cursor or not deployments:
                  break
          PY
          )"

          if [ "$deploy_url" = "__MISSING_CREDS__" ]; then
            echo "❌ Missing Vercel credentials (VERCEL_TOKEN / VERCEL_PROJECT_ID / GITHUB_SHA)."
            echo "   Cannot resolve deployment URL. Check repo secrets."
            echo "failure_subtype=missing_creds" >> "$GITHUB_OUTPUT"
            exit 1
          fi

          if [ -z "$deploy_url" ]; then
            echo "❌ No READY Vercel deployment found for SHA ${GITHUB_SHA}."
            echo "   Either the deploy-staging job never produced a READY deployment"
            echo "   (build error, timeout) or it sits beyond the 1000-deployment"
            echo "   search window. Failing loudly instead of silently no-opping."
            echo "failure_subtype=no_match" >> "$GITHUB_OUTPUT"
            exit 1
          fi
          echo "Found deployment for SHA: promoting..."

          set +e
          promote_output=$(./node_modules/.bin/vercel promote "$deploy_url" --yes --token "$VERCEL_TOKEN" --scope "$VERCEL_ORG_ID" 2>&1)
          promote_exit_code=$?
          set -e
          printf '%s\n' "$promote_output"

          if [ "$promote_exit_code" -ne 0 ]; then
            # Re-running ship on the same SHA is legitimately idempotent.
            if printf '%s' "$promote_output" | grep -q "already the current production deployment"; then
              echo "Deployment for SHA ${GITHUB_SHA} is already in production; continuing to verify."
            else
              exit "$promote_exit_code"
            fi
          fi

          # Verify production actually serves github.sha. /api/health/build-info
          # returns commitSha as a 7-char slice of VERCEL_GIT_COMMIT_SHA (see
          # apps/web/app/api/health/build-info/route.ts:35).
          expected="${GITHUB_SHA:0:7}"
          USER_AGENT="Mozilla/5.0 (compatible; JovieCI/1.0; +https://jov.ie)"
          BYPASS_ARGS=()
          if [ -n "${VERCEL_AUTOMATION_BYPASS_SECRET:-}" ]; then
            BYPASS_ARGS+=(-H "x-vercel-protection-bypass: ${VERCEL_AUTOMATION_BYPASS_SECRET}")
          fi

          # Prefer the deployment URL when Vercel allows direct access, but
          # also verify the canonical production alias. Protected deployment
          # URLs can return 401 even after a successful promote; jov.ie is the
          # user-facing surface and catches domain/project wiring regressions.
          probe_urls=(
            "${deploy_url}/api/health/build-info"
            "https://jov.ie/api/health/build-info"
          )
          probe_labels=(
            "deployment"
            "production-alias"
          )
          echo "Verifying promoted deployment serves ${expected}..."
          # Track confirmed_mismatch separately from saw_valid_probe so a
          # late 500 can't overwrite an earlier confirmed mismatch. Once we
          # see a 200 with a wrong SHA, it IS a mismatch — routing a
          # subsequent edge hiccup to verify_probe_failed would send the
          # wrong alert path.
          actual=""
          last_status=""
          saw_valid_probe=false
          confirmed_mismatch=false
          # Vercel edge rollout can lag behind a successful `vercel promote`.
          # Keep verification strict, but use a bounded 5-minute window to
          # avoid false negatives on healthy deploys.
          max_attempts=15
          for attempt in $(seq 1 "$max_attempts"); do
            # Skip sleep on the first attempt; wait between retries so we
            # don't burn 10s after the final failing attempt.
            if [ "$attempt" -gt 1 ]; then
              sleep 10
            fi
            for index in "${!probe_urls[@]}"; do
              probe_url="${probe_urls[$index]}"
              probe_label="${probe_labels[$index]}"
              response=$(curl -sS -L \
                --connect-timeout 5 \
                --max-time 10 \
                -A "$USER_AGENT" \
                -H "Cache-Control: no-cache" \
                "${BYPASS_ARGS[@]}" \
                -w "\n%{http_code}" \
                "${probe_url}?_cb=$(date +%s%N)" 2>/dev/null || printf '\n000')
              last_status="${response##*$'\n'}"
              body="${response%$'\n'*}"

              if [ "$last_status" != "200" ]; then
                echo "  attempt ${attempt} ${probe_label}: build-info returned HTTP ${last_status}"
                continue
              fi

              actual=$(printf '%s' "$body" | jq -r '.commitSha // ""' 2>/dev/null || echo "")
              if [ -z "$actual" ]; then
                echo "  attempt ${attempt} ${probe_label}: build-info returned 200 without commitSha"
                continue
              fi

              echo "  attempt ${attempt} ${probe_label}: build-info commitSha=${actual}"
              saw_valid_probe=true
              if [ "$actual" = "$expected" ]; then
                echo "✅ ${probe_label} serves ${expected} — promote succeeded."
                exit 0
              fi
              confirmed_mismatch=true
            done
          done

          if [ "$confirmed_mismatch" = "true" ]; then
            echo "❌ Production verification returned commitSha='${actual:-<empty>}' but expected '${expected}'."
            echo "   The promoted deployment or jov.ie alias is serving a different SHA."
            echo "failure_subtype=verify_mismatch" >> "$GITHUB_OUTPUT"
            exit 1
          fi

          if [ "$saw_valid_probe" != "true" ]; then
            echo "❌ Production build-info probes failed across all attempts (last HTTP ${last_status})."
            echo "   Cannot confirm whether production is serving ${expected}."
            echo "failure_subtype=verify_probe_failed" >> "$GITHUB_OUTPUT"
            exit 1
          fi

          # Unreachable: the loop either exits 0 on match, or both
          # confirmed_mismatch=false and saw_valid_probe=false can't both
          # hold — if we got here, something broke the control flow.
          echo "❌ Verification loop finished in an unexpected state."
          echo "failure_subtype=verify_probe_failed" >> "$GITHUB_OUTPUT"
          exit 1

  sentry-error-gate:
    name: Sentry Error Gate (production)
    needs: [promote-production, canary-health-gate]
    if: ${{ always() && needs.promote-production.result == 'success' && needs.canary-health-gate.result == 'success' }}
    uses: ./.github/workflows/sentry-error-gate.yml
    with:
      deployment_url: https://jov.ie
      soak_minutes: 5
      baseline_window_minutes: 30
      threshold_multiplier: 3
      min_baseline_errors: 5
    secrets:
      SENTRY_AUTH_TOKEN: ${{ secrets.SENTRY_AUTH_TOKEN }}
      SENTRY_ORG: ${{ secrets.SENTRY_ORG }}
      SENTRY_PROJECT: ${{ secrets.SENTRY_PROJECT }}
      VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
      VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
      VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
      SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}

  production-oauth-gate:
    name: Production OAuth Gate (rollback on auth break)
    needs: [promote-production]
    if: ${{ always() && needs.promote-production.result == 'success' }}
    runs-on: ${{ vars.CI_FAST_RUNNER || 'ubuntu-latest' }}
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 1
      - uses: ./.github/actions/setup-node-pnpm
      - name: Setup Playwright (Chromium)
        uses: ./.github/actions/setup-playwright

      - name: Hard probe — Better Auth handler reachable (GET /api/auth/ok)
        id: ba-health
        run: |
          # Plan Phase 11 (Clerk → Better Auth migration): a deterministic 200
          # from `/api/auth/ok` proves the BA handler mounted on prod before
          # the OAuth provider redirect_uri probe runs. A 404 means the
          # catch-all `/api/auth/[...all]` shadowed the static route, or the
          # deploy broke auth stitching. Separate axis from the OAuth provider
          # probe: catches handler-wired-wrong without depending on
          # Google/Apple console state.
          ok_url="https://jov.ie/api/auth/ok"
          attempt=1
          max_attempts=3
          until curl --fail --silent --show-error -o /dev/null -w "%{http_code}\n" "$ok_url" | grep -q '^200$'; do
            if [ "$attempt" -ge "$max_attempts" ]; then
              echo "ba_health=failed" >> "$GITHUB_OUTPUT"
              echo "❌ $ok_url did not return 200 after ${max_attempts} attempts."
              exit 1
            fi
            sleep_seconds=$((attempt * 15))
            echo "⚠️ $ok_url not 200 on attempt ${attempt}/${max_attempts}; retrying in ${sleep_seconds}s..."
            sleep "$sleep_seconds"
            attempt=$((attempt + 1))
          done
          echo "ba_health=passed" >> "$GITHUB_OUTPUT"
          echo "✅ $ok_url returned 200 (Better Auth handler healthy)"

      - name: Probe production Google + Apple OAuth redirect URIs
        id: oauth-probe
        run: |
          # Hits the Google + Apple authorize endpoints with the EXACT redirect_uri
          # Better Auth hands them (https://jov.ie/api/auth/callback/<provider>)
          # and fails ONLY on a real provider rejection
          # (redirect_uri_mismatch / invalid_request). This is the gap the
          # staging canary cannot cover: production runs a different app host
          # + different OAuth clients than staging.
          cd apps/web
          mkdir -p tests/.auth
          echo '{"cookies":[],"origins":[]}' > tests/.auth/user.json
          attempt=1
          max_attempts=3
          until CI=true SMOKE_ONLY=1 BASE_URL="https://jov.ie" pnpm exec playwright test tests/e2e/oauth-providers.spec.ts --project=chromium --reporter=line; do
            if [ "$attempt" -ge "$max_attempts" ]; then
              echo "oauth_status=failed" >> "$GITHUB_OUTPUT"
              echo "❌ Production OAuth probe failed after ${max_attempts} attempts — Google/Apple reject Clerk's redirect_uri."
              exit 1
            fi
            sleep_seconds=$((attempt * 15))
            echo "⚠ OAuth probe attempt ${attempt}/${max_attempts} failed; retrying in ${sleep_seconds}s..."
            sleep "$sleep_seconds"
            attempt=$((attempt + 1))
          done
          echo "oauth_status=passed" >> "$GITHUB_OUTPUT"
          echo "✅ Production Google + Apple accept Better Auth's redirect_uri"
      - name: Auto-rollback on confirmed auth break
        # Roll back ONLY on a confirmed auth regression (oauth_status=failed OR
        # ba_health=failed), never on an infra/setup failure — a spurious
        # rollback of a healthy deploy is worse than a one-cycle delay.
        if: ${{ failure() && (steps.oauth-probe.outputs.oauth_status == 'failed' || steps.ba-health.outputs.ba_health == 'failed') }}
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
        run: |
          echo "Production OAuth gate FAILED. Triggering auto-rollback..."
          ./node_modules/.bin/vercel rollback --yes --token "$VERCEL_TOKEN" 2>&1 || {
            echo "Auto-rollback failed or not supported. Manual intervention required."
            exit 1
          }
          echo "Auto-rollback completed"
      - name: Slack alert on OAuth failure
        if: ${{ failure() && env.SLACK_WEBHOOK_URL != '' }}
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
        run: |
          curl -sS -X POST "$SLACK_WEBHOOK_URL" \
            -H 'Content-Type: application/json' \
            -d "$(jq -n \
              --arg run_url "${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}" \
              '{
                text: "🚨 Production Auth Gate FAILED — Better Auth handler unreachable OR Google/Apple reject redirect_uri. Auto-rollback triggered.",
                attachments: [{
                  color: "danger",
                  fields: [
                    {title: "Fix", value: "If BA handler 404s: catch-all /api/auth/[...all] shadowed /api/auth/ok or auth stitching broke. If OAuth probe failed: re-sync Google/Apple consoles via the /auth-console-sync skill (app host likely changed).", short: false},
                    {title: "Run", value: $run_url, short: false}
                  ]
                }]
              }')" || echo "Slack alert failed (non-fatal)"

  deploy-notify:
    name: Notify production deploy
    needs: [deploy-staging, canary-health-gate, promote-production, sentry-error-gate, production-oauth-gate]
    if: ${{ always() }}
    runs-on: ${{ vars.CI_FAST_RUNNER || 'ubuntu-latest' }}
    steps:
      - name: Resolve failure provenance
        id: failure-provenance
        if: ${{ always() }}
        run: |
          set -euo pipefail

          job_url="${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}?check_suite_focus=true"

          echo "job_name=${{ github.job }}" >> "$GITHUB_OUTPUT"
          echo "job_url=$job_url" >> "$GITHUB_OUTPUT"

      - name: Slack notify (production deploy)
        if: ${{ needs.promote-production.result == 'success' && needs.canary-health-gate.result == 'success' && (needs.sentry-error-gate.result == 'success' || needs.sentry-error-gate.result == 'skipped') && (needs.production-oauth-gate.result == 'success' || needs.production-oauth-gate.result == 'skipped') && env.SLACK_WEBHOOK_URL != '' }}
        uses: 8398a7/action-slack@77eaa4f1c608a7d68b38af4e3f739dcd8cba273e # v3.19.0
        with:
          status: custom
          channel: '#alerts-production'
          author_name: 'Deploy'
          custom_payload: |
            {
              "text": "✅ Production deployed: jov.ie",
              "attachments": [
                {
                  "color": "good",
                  "fields": [
                    { "title": "Branch", "value": "main", "short": true },
                    { "title": "Workflow", "value": "CI", "short": true },
                    { "title": "Run", "value": "<${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}|View>", "short": false }
                  ]
                }
              ]
            }
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}

      - name: Slack notify (schema drift warning)
        if: ${{ needs.deploy-staging.result == 'success' && needs.deploy-staging.outputs.schema_drift == 'true' && env.SLACK_WEBHOOK_URL != '' }}
        uses: 8398a7/action-slack@77eaa4f1c608a7d68b38af4e3f739dcd8cba273e # v3.19.0
        with:
          status: custom
          channel: '#alerts-production'
          author_name: 'Schema Drift'
          custom_payload: |
            {
              "text": "⚠️ Schema drift detected — deploy succeeded but Drizzle schema does not match the database",
              "attachments": [
                {
                  "color": "warning",
                  "fields": [
                    { "title": "Action Required", "value": "Generate missing migrations or remove orphaned schema definitions", "short": false },
                    { "title": "Run", "value": "<${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}|View>", "short": false }
                  ]
                }
              ]
            }
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}

      - name: Classify failure type
        id: classify-failure
        if: ${{ always() && (needs.deploy-staging.result == 'failure' || needs.canary-health-gate.result == 'failure' || needs.promote-production.result == 'failure' || needs.sentry-error-gate.result == 'failure' || needs.deploy-staging.result == 'cancelled' || needs.canary-health-gate.result == 'cancelled' || needs.promote-production.result == 'cancelled') }}
        run: |
          # Determine failure type for accurate Slack notification
          MIGRATION_STATUS="${{ needs.deploy-staging.outputs.migration_status }}"
          PREFLIGHT_STATUS="${{ needs.deploy-staging.outputs.preflight_status }}"
          DEPLOY_RESULT="${{ needs.deploy-staging.result }}"
          CANARY_RESULT="${{ needs.canary-health-gate.result }}"
          PROMOTE_RESULT="${{ needs.promote-production.result }}"
          SENTRY_RESULT="${{ needs.sentry-error-gate.result }}"

          # Treat cancellation of any deploy-chain job as alert-worthy.
          # Without this, runs cancelled mid-flight by merge-queue or
          # concurrency would silently succeed without any notification.
          if [ "$DEPLOY_RESULT" = "cancelled" ] || [ "$CANARY_RESULT" = "cancelled" ] || [ "$PROMOTE_RESULT" = "cancelled" ]; then
            echo "failure_type=cancelled" >> "$GITHUB_OUTPUT"
            echo "failure_emoji=⛔" >> "$GITHUB_OUTPUT"
            echo "failure_title=Deploy cancelled mid-flight" >> "$GITHUB_OUTPUT"
            echo "failure_detail=Deploy chain was cancelled before production promotion completed (deploy=$DEPLOY_RESULT canary=$CANARY_RESULT promote=$PROMOTE_RESULT)" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          # Distinguish the silent-no-op regression (production kept old SHA
          # even though vercel promote exited 0) from a generic promote failure.
          PROMOTE_SUBTYPE="${{ needs.promote-production.outputs.failure_subtype }}"
          if [ "$PROMOTE_RESULT" = "failure" ] && [ "$PROMOTE_SUBTYPE" = "domain_project_mismatch" ]; then
            echo "failure_type=promote_domain_project_mismatch" >> "$GITHUB_OUTPUT"
            echo "failure_emoji=🧭" >> "$GITHUB_OUTPUT"
            echo "failure_title=Production domains are on the wrong Vercel project" >> "$GITHUB_OUTPUT"
            echo "failure_detail=One or more public production domains are missing from the canonical Vercel project or have incorrect redirect wiring. Production promotion was blocked before deploy." >> "$GITHUB_OUTPUT"
            exit 0
          fi

          if [ "$PROMOTE_RESULT" = "failure" ] && [ "$PROMOTE_SUBTYPE" = "no_match" ]; then
            echo "failure_type=promote_no_match" >> "$GITHUB_OUTPUT"
            echo "failure_emoji=🕳️" >> "$GITHUB_OUTPUT"
            echo "failure_title=Promote found no matching deployment for this SHA" >> "$GITHUB_OUTPUT"
            echo "failure_detail=Vercel API returned no READY deployment for github.sha. Production was NOT promoted. This is the silent-no-op state we previously caught only by eyeballing /api/health/build-info." >> "$GITHUB_OUTPUT"
            exit 0
          fi

          if [ "$PROMOTE_RESULT" = "failure" ] && [ "$PROMOTE_SUBTYPE" = "verify_mismatch" ]; then
            echo "failure_type=promote_verify_mismatch" >> "$GITHUB_OUTPUT"
            echo "failure_emoji=🪞" >> "$GITHUB_OUTPUT"
            echo "failure_title=Promoted deployment reports mismatched runtime SHA" >> "$GITHUB_OUTPUT"
            echo "failure_detail=The Vercel API matched a deployment to github.sha via meta.githubCommitSha, but that deployment's /api/health/build-info returned 200 with a different commitSha across all retries. Check deployment/runtime integrity (meta.githubCommitSha vs the runtime VERCEL_GIT_COMMIT_SHA env)." >> "$GITHUB_OUTPUT"
            exit 0
          fi

          if [ "$PROMOTE_RESULT" = "failure" ] && [ "$PROMOTE_SUBTYPE" = "verify_probe_failed" ]; then
            echo "failure_type=promote_verify_probe_failed" >> "$GITHUB_OUTPUT"
            echo "failure_emoji=📡" >> "$GITHUB_OUTPUT"
            echo "failure_title=Post-promote verification probe failed" >> "$GITHUB_OUTPUT"
            echo "failure_detail=/api/health/build-info never returned a parseable 200 across the retry window. Cannot confirm whether production flipped. Check edge/CDN/origin health." >> "$GITHUB_OUTPUT"
            exit 0
          fi

          if [ "$PROMOTE_RESULT" = "failure" ] && [ "$PROMOTE_SUBTYPE" = "missing_creds" ]; then
            echo "failure_type=promote_missing_creds" >> "$GITHUB_OUTPUT"
            echo "failure_emoji=🔑" >> "$GITHUB_OUTPUT"
            echo "failure_title=Promote missing Vercel credentials" >> "$GITHUB_OUTPUT"
            echo "failure_detail=VERCEL_TOKEN / VERCEL_PROJECT_ID / GITHUB_SHA unset inside promote-production. Check repo secrets." >> "$GITHUB_OUTPUT"
            exit 0
          fi

          if [ "$PREFLIGHT_STATUS" = "failure" ]; then
            echo "failure_type=migration_preflight" >> "$GITHUB_OUTPUT"
            echo "failure_emoji=🚨" >> "$GITHUB_OUTPUT"
            echo "failure_title=DATABASE MIGRATION PREFLIGHT FAILED" >> "$GITHUB_OUTPUT"
            echo "failure_detail=Migration preflight check failed - deployment was BLOCKED before any changes were made" >> "$GITHUB_OUTPUT"
          elif [ "$MIGRATION_STATUS" = "failure" ]; then
            echo "failure_type=migration" >> "$GITHUB_OUTPUT"
            echo "failure_emoji=🚨" >> "$GITHUB_OUTPUT"
            echo "failure_title=DATABASE MIGRATION FAILED" >> "$GITHUB_OUTPUT"
            echo "failure_detail=Migration failed on production database - deployment was BLOCKED (no code deployed)" >> "$GITHUB_OUTPUT"
          elif [ "$DEPLOY_RESULT" = "failure" ]; then
            echo "failure_type=deploy" >> "$GITHUB_OUTPUT"
            echo "failure_emoji=🔥" >> "$GITHUB_OUTPUT"
            echo "failure_title=Staging deploy failed" >> "$GITHUB_OUTPUT"
            echo "failure_detail=Vercel preview deployment failed before production promotion" >> "$GITHUB_OUTPUT"
          elif [ "$CANARY_RESULT" = "failure" ]; then
            echo "failure_type=canary" >> "$GITHUB_OUTPUT"
            echo "failure_emoji=⚠️" >> "$GITHUB_OUTPUT"
            echo "failure_title=CANARY HEALTH CHECK FAILED" >> "$GITHUB_OUTPUT"
            echo "failure_detail=Staging health check failed - production was NOT updated" >> "$GITHUB_OUTPUT"
          elif [ "$PROMOTE_RESULT" = "failure" ]; then
            echo "failure_type=promote" >> "$GITHUB_OUTPUT"
            echo "failure_emoji=🔥" >> "$GITHUB_OUTPUT"
            echo "failure_title=Production promotion failed" >> "$GITHUB_OUTPUT"
            echo "failure_detail=Verified staging deploy could not be promoted to production" >> "$GITHUB_OUTPUT"
          elif [ "$SENTRY_RESULT" = "failure" ]; then
            echo "failure_type=sentry" >> "$GITHUB_OUTPUT"
            echo "failure_emoji=📈" >> "$GITHUB_OUTPUT"
            echo "failure_title=SENTRY ERROR SPIKE DETECTED" >> "$GITHUB_OUTPUT"
            echo "failure_detail=Post-deploy error rate exceeded threshold - auto-rollback triggered" >> "$GITHUB_OUTPUT"
          else
            echo "failure_type=unknown" >> "$GITHUB_OUTPUT"
            echo "failure_emoji=❓" >> "$GITHUB_OUTPUT"
            echo "failure_title=Production deploy failed" >> "$GITHUB_OUTPUT"
            echo "failure_detail=Unknown failure - check workflow logs" >> "$GITHUB_OUTPUT"
          fi

      - name: Slack notify (migration failure - CRITICAL)
        if: ${{ always() && steps.classify-failure.outputs.failure_type == 'migration' && env.SLACK_WEBHOOK_URL != '' }}
        uses: 8398a7/action-slack@77eaa4f1c608a7d68b38af4e3f739dcd8cba273e # v3.19.0
        with:
          status: custom
          channel: '#alerts-critical'
          author_name: 'Deploy'
          custom_payload: |
            {
              "text": "🚨 DATABASE MIGRATION FAILED - Production deploy BLOCKED: jov.ie",
              "attachments": [
                {
                  "color": "danger",
                  "fields": [
                    { "title": "Status", "value": "🛑 DEPLOYMENT BLOCKED - No code was deployed", "short": false },
                    { "title": "Failure", "value": "Database migration failed", "short": true },
                    { "title": "Branch", "value": "main", "short": true },
                    { "title": "Action Required", "value": "Fix migration and re-run pipeline", "short": false },
                    { "title": "Run", "value": "<${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}|View logs>", "short": false }
                  ]
                }
              ]
            }
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}

      - name: Slack notify (migration preflight failure - CRITICAL)
        if: ${{ always() && steps.classify-failure.outputs.failure_type == 'migration_preflight' && env.SLACK_WEBHOOK_URL != '' }}
        uses: 8398a7/action-slack@77eaa4f1c608a7d68b38af4e3f739dcd8cba273e # v3.19.0
        with:
          status: custom
          channel: '#alerts-critical'
          author_name: 'Deploy'
          custom_payload: |
            {
              "text": "🚨 MIGRATION PREFLIGHT FAILED - Production deploy BLOCKED: jov.ie",
              "attachments": [
                {
                  "color": "danger",
                  "fields": [
                    { "title": "Status", "value": "🛑 DEPLOYMENT BLOCKED - Preflight safety check failed", "short": false },
                    { "title": "Failure", "value": "Migration preflight validation failed", "short": true },
                    { "title": "Branch", "value": "main", "short": true },
                    { "title": "Action Required", "value": "Check migration journal integrity and database state", "short": false },
                    { "title": "Run", "value": "<${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}|View logs>", "short": false }
                  ]
                }
              ]
            }
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}

      - name: Slack notify (canary failure - URGENT)
        if: ${{ always() && steps.classify-failure.outputs.failure_type == 'canary' && env.SLACK_WEBHOOK_URL != '' }}
        uses: 8398a7/action-slack@77eaa4f1c608a7d68b38af4e3f739dcd8cba273e # v3.19.0
        with:
          status: custom
          channel: '#alerts-critical'
          author_name: 'Deploy'
          custom_payload: |
            {
              "text": "⚠️ STAGING HEALTH CHECK FAILED - Production was NOT updated: staging.jov.ie",
              "attachments": [
                {
                  "color": "warning",
                  "fields": [
                    { "title": "Status", "value": "⚠️ STAGING verification failed before promotion", "short": false },
                    { "title": "Failure", "value": "Canary health gate failed", "short": true },
                    { "title": "Branch", "value": "main", "short": true },
                    { "title": "Action Required", "value": "Check https://staging.jov.ie or the preview deployment logs - production is still unchanged", "short": false },
                    { "title": "Run", "value": "<${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}|View logs>", "short": false }
                  ]
                }
              ]
            }
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}

      - name: Slack notify (sentry error spike - URGENT)
        if: ${{ always() && steps.classify-failure.outputs.failure_type == 'sentry' && env.SLACK_WEBHOOK_URL != '' }}
        uses: 8398a7/action-slack@77eaa4f1c608a7d68b38af4e3f739dcd8cba273e # v3.19.0
        with:
          status: custom
          channel: '#alerts-critical'
          author_name: 'Deploy'
          custom_payload: |
            {
              "text": "📈 SENTRY ERROR SPIKE — Auto-rollback triggered: jov.ie",
              "attachments": [
                {
                  "color": "danger",
                  "fields": [
                    { "title": "Status", "value": "📈 Error rate spiked post-deploy — auto-rollback triggered", "short": false },
                    { "title": "Branch", "value": "main", "short": true },
                    { "title": "Action Required", "value": "Check Sentry for new errors, verify rollback succeeded", "short": false },
                    { "title": "Run", "value": "<${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}|View logs>", "short": false }
                  ]
                }
              ]
            }
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}

      - name: Slack notify (general deploy failure)
        if: ${{ always() && (steps.classify-failure.outputs.failure_type == 'deploy' || steps.classify-failure.outputs.failure_type == 'promote' || steps.classify-failure.outputs.failure_type == 'promote_domain_project_mismatch' || steps.classify-failure.outputs.failure_type == 'promote_no_match' || steps.classify-failure.outputs.failure_type == 'promote_verify_mismatch' || steps.classify-failure.outputs.failure_type == 'promote_verify_probe_failed' || steps.classify-failure.outputs.failure_type == 'promote_missing_creds' || steps.classify-failure.outputs.failure_type == 'cancelled' || steps.classify-failure.outputs.failure_type == 'unknown') && env.SLACK_WEBHOOK_URL != '' }}
        uses: 8398a7/action-slack@77eaa4f1c608a7d68b38af4e3f739dcd8cba273e # v3.19.0
        with:
          status: custom
          channel: '#alerts-critical'
          author_name: 'Deploy'
          custom_payload: |
            {
              "text": "${{ steps.classify-failure.outputs.failure_emoji }} ${{ steps.classify-failure.outputs.failure_title }}: jov.ie",
              "attachments": [
                {
                  "color": "danger",
                  "fields": [
                    { "title": "Detail", "value": "${{ steps.classify-failure.outputs.failure_detail }}", "short": false },
                    { "title": "Branch", "value": "main", "short": true },
                    { "title": "Failing job", "value": "${{ steps.failure-provenance.outputs.job_name }}", "short": true },
                    { "title": "Failing job URL", "value": "<${{ steps.failure-provenance.outputs.job_url }}|View job>", "short": false },
                    { "title": "Run", "value": "<${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}|View>", "short": false }
                  ]
                }
              ]
            }
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}

  # Lighthouse CI - runs after production deploy to check performance on homepage + public profiles
  lighthouse-ci:
    name: Lighthouse CI (Production)
    needs: [promote-production]
    # Run after successful deploy (tests homepage + /tim profile)
    # Informational only -- performance regressions should not block deploys
    if: ${{ always() && needs.promote-production.result == 'success' }}
    continue-on-error: true
    runs-on: ${{ vars.CI_FAST_RUNNER || 'ubuntu-latest' }}
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - uses: ./.github/actions/setup-node-pnpm
      - name: Run Lighthouse CI against production
        run: for attempt in 1 2 3; do if pnpm --filter web exec lhci autorun --config=.lighthouserc.json; then exit 0; fi; echo "Lighthouse production attempt $attempt failed (PROTOCOL_TIMEOUT), retrying after 10s..."; sleep 10; done; echo "All 3 Lighthouse production attempts failed."; exit 1
