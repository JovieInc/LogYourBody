{
  "schema_version": 1,
  "repository": "JovieInc/LogYourBody",
  "target_ref": "refs/heads/main",
  "required_status_check": "CI Summary",
  "merge_queue": {
    "required": true,
    "merge_method": "MERGE",
    "max_entries_to_build": 5,
    "min_entries_to_merge": 1,
    "max_entries_to_merge": 5,
    "min_entries_to_merge_wait_minutes": 5,
    "grouping_strategy": "ALLGREEN",
    "check_response_timeout_minutes": 60
  },
  "auto_enrollment": {
    "workflow": ".github/workflows/native-merge-queue.yml",
    "exact_head_required": true,
    "same_repository_only": true,
    "drafts_allowed": false
  },
  "token_enrollment": {
    "workflow": ".github/workflows/merge-queue-token-enrollment.yml",
    "secret": "MERGE_QUEUE_TOKEN",
    "reason": "merge_group events caused by GITHUB_TOKEN start no CI run; the queue only honours checks from the merge_group event, so the entry must be enqueued by a non-GITHUB_TOKEN identity"
  }
}
